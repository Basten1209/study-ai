# Lec 04. Architectures: Grids

Source: https://notebooklm.google.com/notebook/1974010f-eec5-485b-b074-000c70149926
Notebook: MIT 6.7960 Deep Learning, Fall 2024
Source order: 4
Extracted: 2026-07-02

## Transcript

so today we're going to start off the first of a series of lectures on machine learning architectures um and so you know moving beyond kind of the very vanilla um architecture we've been talking about so far which is the multi-layer perceptron and starting to discuss different ways that we can encode structure or essentially build hypotheses about the space of models that we want to um optimize over directly into the architecture of the model that we're going to optimize um so what that's going to look like today um first we're going to sort of have a broader discussion about why um we want to build better architectures and kind of what makes an architecture better um and then we're going to do a bit of a deep dive on convolutional layers as like one specific um mechanism you can build into an architecture um one that works particularly well for grids um and this might be a review for some of you but um we'll we're still going to spend some time on it then we're going to talk a bit about pyramids and the idea of receptive field then I'm going to go through a bit of a high level overview of a bunch of different kind of architectures and architecture components that have made up a lot of the progress in the last you know 10 10 years or so um and then I'm going to finish with uh a first touch on neural fields and positional encodings though we'll talk a lot more about this when we get into lectures on transformers later on okay so this is what we've been talking about so far the multi-layer perceptron um so this is you know at a high level the building block um is this linear combination of neurons or linear combination of inputs a neuron-wise or a pointwise nonlinearity and then another linear combination and one of the benefits of this like got described in the last lecture is the fact that it can it's a universal approximator you can build any model and prove that you can build any model with this type of architecture um another benefit of a multi-layer perceptron is it's site quite simple right and one of the things this simplicity gives you is the ability to write really elegant theory about this model and this is one of the only models that we actually have really elegant theory for um because the complexity is such that we can make reasonable assumptions it maps well into our kind of mathematical toolbox um and then another huge benefit of the multi-layer perceptron is what it's what we call embarrassingly parallel um essentially this means that this type of architecture um can be really easily parallelized in terms of computation which means we can make use of things like GPUs that enable a lot of parallel multiplications so you know one obvious example of that is that that neuron-wise or that pointwise nonlinearity that's calculated independently for every single um output neuron of that first layer and so that can happen completely in parallel there's no dependence across the um the data points another nice example is that linear combination of neurons um though that um that densely connected layer or that weight that can happen in parallel across a very large in some cases batch of data that's done sort of independently per data point so what are some of the bad things about the multi multi-layer perceptron so first one is that it has very weak inductive biases and so what we mean by this is that um the model itself it's a universal approximator but there's not a lot of structure or intuition baked into this model um it doesn't sort of assume much about the structure of the model you want to learn coming out and so one of the things that implies is that though you can use this model um so though you can use a multi-layer perceptron to approximate any sort of uh mathematical function it might be very difficult to do so given the data that you have um it's very sample inefficient or data hungry this basically means that in order to get this multi-layer perceptron to learn maybe a very complex function you might require so much data that it's not actually tractable and like more data than exists on Earth um to be able to really generalizably and robustly learn um a function with this particular architecture uh that generalizes well and then another downside is that those dense layers those fully connected layers um particularly if you want a model that's very wide because maybe the the function you need to approximate is quite complex those can be really expensive if your input data is an image for example um a high resolution image that you might take on your iPhone today um then you need to flatten that out into sort of one long vector that's going to be thousands of input dimensions and you know thousand by thousand multiplication in a dense way is a very expensive computational cost so why use other architectures well one we might want to address some of those downsides that we talked about but let's kind of take a sort of a meta view of this and and and zoom out a little so if this is the set of all possible mappings from X to Y this is like all functions is contained in this little gray box um and maybe this is the true solution we're trying to learn um this is kind of the the correct model um for our our problem then possibly given any data set that we're going to learn from that data set will constrain the space a bit right so not every model will fit the data that we're using to learn the data and so now maybe you have this subset of all mappings that actually fits your training data um and then within that based on something about your optimization scheme you learn some solution and it will be in that space it will fit the data but it won't necessarily be very close to the true solution so what would happen to this kind of highle diagram if we add more data um anyone what would happen oh so the sub is true yeah exactly so that um that green space that'll get smaller right there will be fewer models that fit all of the data if we have more data so that's kind of nice so if you can add more data then potentially you shrink the space you're optimizing over and then you'll learn a maybe more optimal solution or something that's closer to the true solution because the space is different but what if you don't have more data right that's not an option for you so then another alternative is to define a hypothesis space and this is adding some structure into the model itself and that hypothesis space is essentially going to shine sort of a spotlight on the space of all possible models is the way we've kind of noted it here but it's essentially it's it's a it's a stronger inductive bias or a stronger prior over what we think the model is going to take the form of and then if you have kind of that intersection of the models that fit the data and your inductive hypothesis then then you might actually be able to find um use less data but still find a better architecture right so the idea is we can pin down or we can get closer to the true model by either adding more data or by using a more constrained architecture and you can imagine that the combination of the two would be even better right um so more data helps more architecture or stronger inductive bias helps and those two things combined um is ideal so let's look at this from a very very simplistic uh model this is just a one-dimensional model it's it's a mapping of f ofx where x is a scalar so here if we use for example and this is actually these are these are true outputs so if we use a five layer relu so essentially densely connected layers um with pointwise relu based um activations and we fit that to a single data point right well now the space of all possible models that fit that data point is very very large and the one that actually gets learned is just this flat line so that's not necessarily a very good learned solution as we start adding more data you can see that you maybe start to fit slightly better um in the region where we add data in the middle but it's still not a very good solution and then as you start to add a lot of data what you see on the right is you start getting really really nice fits in the distribution where you have data but you get really poor generalization out of distribution and so the intuition here is really that um that model hypothesis that hypothesis space you build that's really something that helps us generalize out of the distribution of the data we have and so what if instead we had a really strong prior over what the model was supposed to be um so in this case y= ax plus sin bx^2 right that that's very specific it turns out that is exactly the correct model um for this function and you can see that if you only have a single data point that really good model is sort of insufficient right you still aren't able to figure out where in that set of hypotheses um the correct model is but even with very very very few data points we start getting a really nice fit and actually even when we add a lot more data points we get really strong generalization out of distribution but the difference between those two in the middle is actually not that big right so we can get a really nice fit even with just a little bit of data when we have the exact precise correct hypothesis um and so that's what we're showing architectures enable us to generalize outside the training distribution and a good architecture is going to be one that can represent the true function of the data and then is otherwise minimal right we want it to be easy to search over via gradient-based learning we want it to be easy to parallelize fast on GPU etc etc and so here's a bit of a preview um but it turns out that better architectures can approximate important function classes more efficiently so um this is a result um from a paper by another MIT professor Vincent Sitzman um where they introduced this model over here on on the far right called Siren and um siren actually uses a syosoidal um activation space and that fits images more efficiently if anyone's taken signal processing this idea might be a bit familiar um a foria basis is a good basis for representing images and the fundamental building blocks of a foria basis are sinosoids um so if we take a look at what this is if you look at you know Reelunet or a tenant or you know these different models that are being compared here and we're going to look at how they actually try to fit this specific image over time by sampling pixels from that image and then fitting that as the training data um you can see that you start getting a pretty good approximation from Siren Siren much much faster than you do from some of these other models and of course after sampling enough points eventually all the models are are getting reasonably good at estimating but that siren model it's a better hypothesis to start with for the image space and so you get much more efficient convergence and so in this particular instance now instead of looking at a five layer relu we're going to look at a five layer s net where now those activations as nonlinear activations are sinosoids um and you can see here that with a single data point it still doesn't fit super well um but even just with a couple data points we start seeing some periodicity that maybe starts to match the periodicity in the function and even though we're not generalizing perfectly we're still sort of seeing some periodicity that seems to be moving more towards what the true function is so why would you have that periodicity in the output space it's kind of an obvious question sign yes it's a sign signs are periodic very nice um but so essentially we've just added an inductive ba bias in our model architecture that says that the distribution should be periodic and then it tries to find solutions that are periodic outside of the sort of distribution where we have training data cool any questions about this it's really just intended to build high level intuition yeah so I guess I'm wondering how constrained is this to the sub number of pixels because the sinosoid period seems like effect um so one of the things with the 4year basis is that um it you are building out um the basis assumes different periodicities of sign um and so you're reconstructing the image using different periodicities and weights of the sinosoid function so that handles um some of the structure well and it's one of the reasons that we can compress images really well with things like the fastforia transform yeah um cool so let's now talk about convolutional neural networks um so here's an image what's in this image birds okay so what else is in this image sky so if I had a a whole image classifier if I took this image and I ran it through one of the models we've talked about so far what would we want it to say what's the category of this image birds and sky i mean that would be nice we haven't talked about multi output um classification though yeah nature yeah okay so that's that's actually I love that answer so one of the things that's really interesting is like you can really see this trade-off between generality and specificity and so yeah it's like oh well that's too hard i'm just going to predict something coarser grain and you know that's a reasonable fallback um but okay so so what I was kind of trying to get at here is actually for a lot of things we might want to understand in an image maybe a single category isn't isn't sufficient information right this could be flock of birds for example i mean maybe that's a category um but really it's just that a single label is usually insufficient to represent um the semantic concepts that are captured in images and this is actually in the grand scheme of things a pretty simple image i mean think about this versus an image like of Time Square for example how complex that image signal semantically could be so if you were to take a picture for example from where you're sitting I don't know out there in the audience like think about like what would you categorize that picture is it is it lecture hall is it students is it projector is it MIT it's all those things right um but so when we start to think about these more complex um semantic signals and we'll get into a lot more of this throughout the course um one thing that starts to be more and more true is that there's often um there's often maybe different uh categories or different bits of information that are somewhat locally constrained and so you can think about breaking down the problem into something that actually looks at location as well as semantics so one way that we could try to address the fact that this image kind of contains multiple things is we could first subdivide the image into patches and then we could try to classify each patch separately right so you could take this image chop it up into little pieces and then maybe you have a classifier and now you send this patch through the classifier and it says bird and then you could run that classifier over the entire grid right and you take all those patches independently and you could then you know hey bird this one is sky and you could densely populate that entire sort of um uh space and and get out the category per little block or little patch um and so if we think about this and we have some function that's going to crop this up and and and map every uh patch to a category in some way this is actually giving us an image output right it's it's um it's a bit like a low resolution image it's got some spatial um context which is useful um but there are a couple patches here where the objects don't necessarily fit super neatly inside them right so I don't know maybe this one over here that's got like the tip of the bird's wing just in that one edge of the patch you know it gets a little ambiguous do you want that to be that patch that whole patch to be categorized as bird if there's any bird at all do you want it to be what is the majority of the pixels in the patch um and so maybe we want a higher resolution output map right we don't want to just know at this really coarse resolution we actually want a finer resolution of the output so one way you could do that is you could use smaller and smaller patches um but it turns out that as the patches get smaller and sort of at um at the far extreme the patches are a single pixel from a single pixel you don't have enough context to determine what the semantic category is it's not easy to recognize content as those patches get small so instead the kind of winning um idea here with convolutional neural networks is we use large but overlapping patches um and that's really the very you know the very high level intuition behind all of this is that you want context that's local um to be you know you want to be able to look around the pixel you're predicting at when you make a prediction um but you also want to kind of do this in a dense way so the way that would look is okay you have your sort of patch size and that patch size is probably a parameter in your model you want to make sure that context window is kind of big enough that you maybe see the whole object of bird um but then when you're actually predicting you're sliding this um over as you go and then maybe you're only predicting for example the object class of the center pixel but given the context around that pixel and so now your training data would be you know that patch and then the output category of that center pixel for example and now if you actually run this categorizer over your entire image you start to get something that actually looks pretty cool right that starts to look like um semantically kind of the boundaries of one category and another and this problem of categorizing every pixel instead of categorizing a whole image is called semantic segmentation and here we're categorizing every pixel as one of a set of categories um this can be extended and maybe we'll talk a bit later about um instance segmentation which is a similar problem but now you want to identify unique instances of a given category and capture them separately so now you have um essentially the building blocks of a convolutional neural network um and one thing that's nice about this is because you're processing each of these patches independently and identically that means that the actual model itself is invariant to translations of the input so this is an equivariant mapping essentially so since you're processing every single patch in the same way that means that it doesn't matter if the bird is in the top corner the center the bottom left it's still those pixels are still going to be categorized as bird um and another way to think about this is if you took your function and you applied it to a translation of the input it would be the same as if you translated a function of the original input and why would this maybe be a nice thing to have for something like semantic segmentation why is this like make sense as an inductive bias or a hypothesis for images for for categorizing like this yeah yeah yeah so so the answer corresponds to that local context window but um yeah if you took an image of a bird and then you moved that sort of portion of the image corresponding to a bird to a different part of the image or imagine you were actually taking a photo and then you shifted your camera a little bit the object you're taking a photo of is the same right so there actually is this type of symmetry kind of innate to categories um in the real world right right and this symmetry is something we've built into our hypothesis space via the model structure um and so yeah the world is somewhat translation invariant a bird should look the same no matter where it is in an image of course we're kind of glossing over pose which is a a more complicated um factor here but if you have something at the same pose and you move it somewhere else it still maintains the same category so cool one possible version of this um of this kind of model would be to compute a weighted sum of the pixels in each patch and then the weights of those sum that sum that's what we're going to learn and so if you if you do this then essentially that that set of linear weights W is a convolutional kernel that then we can apply to the entire image um and so what does this mean what is this kind of concept of a convolution and and a filter or a kernel um so a convolution is a linear shift invariant transformation of of grid structured input data um and essentially in signal processing convolution is a way that you do filtering so how many of you have taken I don't know a signal processing class or learned signal processing maybe in like an electrical engineering class raise hands cool so a good number of you have seen this before right um and here this this the filter are the weights we use to take that linear weighted sum um and then like mathematically if we want to structure that out here at the bottom um the output for a given location when applying the filter is going to be some bias plus a linear combination of the filter weight for that coordinate times the pixel value for that coordinate in the image and then summed over all the filter weights and all the pixels in the patch um so really what it's doing is for any given p filter like in this case this filter is kind of uh diffuse all around and then you have something dark and something light um so this filter is going to look for the that pattern by look for that's very sort of anthrop anthropomorphized but um you're going to produce maximum output values when you have that same kind of dark to light transition and so what that actually looks like if you kind of run that filter over this image um is you start to get something that's highlighting areas in the image that sort of match the filter the most right um so match it most closely so here we can see that we're getting really um high values so the white values along the boundaries between dark to light and we're actually getting really low values along the boundaries from light to dark because that's the place that's going to be sort of maximally opposite to that filter um so in this particular case what we've got is a filter that's finding us kind of horizontal edges between dark and light values um any questions yeah can you explain what is invariant transformation in this context yeah so again um here if I took this image of a cloudfish and I shifted it over and I ran this convolutional filter over it the output would be this shift it over and so you could shift the input then apply the convolution or you could apply the convolution and then shift the output and the results would be the same so now let's look at this from a more sort of neural networky perspective um so this is a fully connected layer this is what we've seen so far um and here all the outputs depend on all of the inputs um and this can be quite computationally expensive right when things get very large instead of a fully connected network um a convolutional network is essentially a locally connected network um and so often we can assume that the output then of of this model is a local function of the input so here if we use the same weights um you know for sort of these linear um combinations this W matrix if we use those weights the same regardless of sort of where um that initial uh input values are coming from that gives us a convolutional neuronet network and the way that we actually denote that is with this star operation for convolution um if you have taken signal processing actually this is not quite the same definition is familiarly used in in signal processing for convolution usually it has a bit of a like a flipped value um but here in in when we look at talk about neural networks instead of calling this crossorrelation which it might be what it is in the signal processing literature we just call this convolution one of those reasons is because in these networks it's very easy to learn how to flip a sign so it doesn't matter so much is it always a fivepoint star i mean people people do all sorts of stuff uh we are going to use a fivepoint star in our lectures and in the notes um I think it's kind of pretty uh so what does it actually look like so here we have this same linear combination of weights we're running it over the local sort of structure and then you're taking that weighted sum and so the outputs for each of these values are going to be corresponding to just that local input but it's going to be the same weights that are used across the entire input vector um and the nice thing with that kind of thing is that you get this again embarrassing parallelism right because now you can apply that same weight matrix across sort of only uh sort of smaller patches of the input um but you can do that in parallel and if you want to think about um kind of what that looks like in this broader sense if you're taking a fully connected layer that's the same as taking your input vector multiplying it by a dense weight matrix right where every single one of those values um matters and then that's how you get your output and now you could think about um actually a convolutional layer is the same but now the weight matrix has a very specific structure so instead of being dense it's going to be very sparse so in this particular instance where we're only looking at a sliding window of um maybe three elements of that input vector that those three elements are going to be the only sort of like above and below that diagonal those are going to be the only components that have values and the values are going to be exactly the same along the entire diagonal of that matrix um and there's a kind of a special name for this type of matrix it's it's a a specific form of a topless matrix um and so one of the things that's kind of cool here is that it's essentially a constraint on a standard linear layer um and there are fewer parameters that can be learned because you're just learning essentially um parameters for just that local structure so there's less of them and they're going to be repeated and so that means um it's you can have a lot less overfitting right there's less sort of uh open flexibility and you're building some nice constraints into the system you're making it equavariant you're making it translation invariant and then another nice benefit of this is because this thing has this specific structure where you have kind of zeros almost everywhere except for sort of just above and be below the diagonal um this can generalize to different input sizes because you can take that same that same weight matrix and you can scale it right it's not going to change anything about the model you've learned and so that means that convolutional layers can be applied to arbitrarily sized inputs which generalizes beyond the training data um in a really nice way whereas if you took uh like an MLP and you applied it to an image and then you tried to apply it to a larger image you couldn't do it right this isn't something that um that you could apply to a larger image you just wouldn't have weights for some of that size and so you know one way to look at this is like you could take that same filter that we applied to the clownfish and you could apply to any image and you would always kind of get out these edges um these specific things so five kind of high level views yeah if you go back two slides before you scale the the matrix does each pixel color here correspond to one value so we talked about how um the weights right um so w1 w0 w minus one so that's like uh if you're if you're taking this from like a vector perspective right you want to look at the weight just before and just after that means that you're only going to have three values right and those three values if you transfer this into a linear layer those are going to be the values above along and below the diagonal for the whole matrix so you just take that and slide like the ladder out along the diagonal it would be the same like that um cool so some different ways to think about convolution so one convolutional layers are equariant to translation so this is a built-in hypothesis two convolutional layers enable patch processing it it builds in explicit parallelism into our computation makes it computationally efficient three convolutional layers can be thought of as kind of image filters um things that are highlighting uh specific structures in the input data in some useful way that's learned four you can think of them as parameter sharing right um you're using the same parameters sort of across the entire input data in a computationally efficient way and a last point is you could think of them as a way to process variable size tensors because you have this kind of built-in generalization across the input size cool so what happens when you stack convolutional layers um so here what we've got is two sets of convolutional layers so first our input data comes in we have a first convolutional layer then we have a nonlinearity and then we have a second convolutional layer and it turns out that if you think of it this way actually then the entire CNN is a convolutional filter but one that's nonlinear so you can really think of this this way right um and and so here now you know this output depends only on the local structure of that input and that function even though it has that nonlinearity in the middle is the same um for anywhere sort of any of these local patches in the input image does this make sense yeah yeah so I'm I'm just saying here in this particular instance assume now that you have a single convolutional layer a non a pointwise nonlinearity just like we had for MLP and then another convolutional layer um so since we said that a convolutional layer is essentially a constrained version of a linear layer you can essentially take it the same way um but the nice thing is that constrained version of a linear layer it it maintains that local constraint even as you start to stack layers um but one thing that's very clear is as you're adding more depth you're increasing what we call the receptive field of the output so you're adding more uh more layers that means that the the local patch that corresponds to for example XL of J is larger than the local patch of a single one of those same layers right because you're you're taking in inputs from a larger size and then you're doing that again and this is what we call a a spatial pyramid for example sorry can you be a little louder oh yeah yeah so um the only reason this is nonlinear is because there's a nonlinearity in the middle so from between that first layer so you have this first convolutional layer and then see where you have those nodes in the middle what I'm saying is here we've applied an activation a nonlinear activation and then you have that second layer maybe that's something that in future years I'll I'll just put that in the diagram so you can see that kind of nonlinearity so be a little clearer but um I'm just saying it exists but so now you have a convolutional filter but it's nonlinear because you have that nonlinear activation that you've put in the middle what is it called when there's like being established when you're pulling weights dependent layers a receptive field is one term that people will use um so if you're like increasing the receptive field of your model you can do that in a couple ways you can make the original patch size input size larger so the the kernel size the filter size could get larger or you can just add more depth and that will sort of also increase the receptive field uh double side back you represented uh x as like a vector so that's a image that's been flattened into a vector yeah so right now we're talking about vectors because they're really clean and easy to look at um but yeah you can essentially say you take an image and you flatten it out um though I think it's a little more complicated than that because uh you wouldn't take an image and flatten it out and then apply convolution like this because you would have this weird like stripe-wise structure so actually we do take those convolutions in two dimensions yeah um and secondly would you use the same um no and that's why the colors are different i mean you could But usually we don't those weights are learned but they are shared across the entire layer so just to make sure I understand each weight matrix in each layer basically zero except for the K and that does not represent um so the question is like for each of these layers now that we have a convolutional layer um they can be represented as that you know sparse matrix where you only are looking along the diagonal um we don't always necessarily actually compute it that way um uh for efficiency or for parallelism often we just really do compute it patchwise um but your your point was if you're now really restricting the search space by only allowing like a few parameters per layer doesn't that mean that you would um really struggle to represent images or sort of learn um and the answer is uh yes and so that's why often with convolutional layers either we'll have quite a few different filters that are happening in parallel we'll get into it later or we add a lot more layers in a dense way but what we have found experimentally and there's some nice theory around it is that um for things where this is an appropriate inductive bias you can learn more efficiently you get a better fit with less data cool so what if we have color i mean we're really taking a very image perspective here but essentially what we're talking about with color is multiple input channels right so not just spatial extent but you have kind of a tensor that also has something like depth um so if you have multi- channelannel inputs so say you do have like RGB that's what we're showing here um what you do to do convolution over multi- channelannel inputs is you learn a different set of weights for each of those input channels and then you sum over um all of that to get the output value so you sum essentially over the convolutions for each of the input channels to get the output value um you're summing per channel okay so now what that looks like is you would still have a single output value um for each filter but now the filter is going to um have per channel weights that are learned and you could arguably force them to be shared but often we let them be separate um okay what about multi- channelannel outputs so if instead of having um just a single output of your convolutional layer you want to have multiple channels of outputs um here what you actually do is you learn what we call a filter bank or sort of a set of different output um convolution so here instead of just learning a single um you know uh linear layer you're going to learn sort of two in parallel and each of them get to build their own output dimension um and so now you have a filter bank of let's say C different filters um and you get kind of a different output activation for each of those filters from the same input data so then you can generalize this and generally in most of our convolutional layers in in deep learning we have multi-input and multi-output um functions and so here you'll have um essentially uh your output is going to have you know some number of dimensions or some number of feature channels um the input will have some number of input you know dimensions or feature channels and then you're going to have some number of filters that you convolve with that input to get this output and the number of features that you have you're going to have the number of output dimensions um number of features or sort of filters um each of those filters is going to have some kernel size that's K here so some sort of spatial dimensions and then um for each of them you're going to have enough channels to match your input data so let me show a few more examples of what this looks like so here's for example a bank of two filters where you have multiple inputs and multiple outputs so here you have uh two input dimensions you've now learned this linear sum um for you know your height by width um sort of or the this kernel size which in this case looks like 3x3 um so each of these sets of weights f1 and f2 those are learned separately but they're shared across the entire image um and so what those feature maps might actually look like um increas they kind of get increasingly uh complex I would say so here this is your input um these are uh this is using um an Alex net model so a convolutional model um now you take that input after the first convolutional layer this is the number of sort of feature banks that are learned or sort of output channels that are learned so there's um this initial input is convolved with this many different um this many different convolutional filters and so now we're showing all of those different filters what actually activates when you send in this image and so you can see I don't know maybe some of them are capturing things like certain types of edges certain types of structures um and then after that second convolutional layer these things start to get maybe a little bit more complex they can often be quite difficult to interpret but every layer can be thought of as a set of C feature maps or channels and every feature map is some essentially like structural representation it's an image it has um sort of height and width yeah how do you ensure that each uh feature map is uniquely different so that you have a set of ones that can so how do you make sure that all of the different feature maps are different um so that's not actually something that's innately built in um but one of the ways that uh people will do this is um through different types of regularization in the model um so sometimes things like dropout that tries to make sure that you don't sort of um make the the model sort of too brittle to or reliant on different things um but usually the model will learn um each of them uniquely because that gives it more capacity there's nothing in here forcing it to keep them all the same and so why would it not sort of learn to maximize um the the sort of types of variance that it could have but you can have like this depending on the data and architecture and your optimization you can have kind of like feature collapse sometimes where um some stuff just gets ignored that you might not want to be ignored and then you can try to build in different mechanisms to make that not happen all right so we're going to do like a simple little quiz so here's a layer um we have this input it's three channels so RGB by 128x 128 very standard um image size for an input then we have a filter bank full of 3x3 filters the spatial extent of those filters is 3x3 this is often the way we talk about it so we're not talking about the channel dimensions we just talk about the the spatial extent of the size of the filters and then our output is 96 channels by 128 x 128 so by height and width so first how many parameters does each of those filters have nine so why nine okay but how many input channels did we have all good um yeah so so you have every single one of these filters has 27 parameters we put this in because it's a little confusing right we talk about the filters being 3 by3 but the thing is when just this is just convention when we talk about filter size we often only talk about the spatial extent of the filters and the assumption is we don't actually convolve um in channel space though of course you could um that's something you could totally build in so maybe you have something like a hyperspectral input you actually want convolution across the spectral bands um you could build that into your model but sort of standard uh we often use sort of um independent um per input channel weights that are learned and so then we only talk about the spatial extent um so the filter banks are 3x3 all right now how many um how many filters are in our filter bank yeah 96 exactly there are 96 outputs here which means we get 96 filters um and then each of those filters will have those 27 parameters um so you're doing this for each of those 27 um parameter input filters you're running those over the entire image and you are getting now taking a linear sum over all three of those channels and you're getting a single output dimension and so for if you have 96 output channels you need 96 different filters nice cool so high level filter sizes if you're mapping from um some you know X of L so lay this current layer where you have um C subL channels and then N by M output dimensions and you're going to map to the next layer where you have um some C subL + one output dimensions and N by M spatial dimensions and your filter has spatial extent K1 by K2 then the number of parameters per filter will be K1 K1 * K2 time C subell and then the number of filters will just be C subl + one the number of output channels that you have yeah so I normally would like to see square filters when dealing with curious where you have like rectangular filters absolutely normally this filters are square that is that is totally standard they don't have to be right like it's just a convolution can be defined in in a way that is not square but I would say normally we see square convolutions yeah cool so you have say your input image um now you're going to run it through a first layer so essentially for each of your filters you have a a red a green and a blue um filter those get applied and then you know you could think about okay so now if you take just one of those um filters that's going to give you just a single one of those output maps in your in your fil feature maps at layer one a single one of those output mats is going to correspond to one of those filters over RGB and now if you run that a second layer second convolutional layer now you have sort of layer two your whole sets of features now each of those are going to be instead of RGB they're going to be um you know across the entire set of feature maps in your first layer but then you know one of the outputs after that will only correspond to a single one of those C2 filters so you can see how the actual representation gets highly complex highly quickly um even with just a single you know even just one or two layers of depth when you have convolution um it can map map really sort of complex um functions so another thing that we often use when we're actually building convolutional neural networks is this idea of pooling um so here you know what we have is we have you know this this convolutional layer so you have um your convolutional weights you have your bias um and then you have your pointwise nonlinearity but then one of the things that you can do is you can do what's called pooling over those output features one of the reasons you might want to do that is because you can see that the dimensionality can get really large really fast um and so one thing very very common thing is something we call max pooling where here you just take some sort of local window and you just use the maximum value and you use that to represent your output um or you can also think about something like mean pooling um and here you're just taking the average um over some sort of smaller local window so why do you want to do pooling so one thing that's really nice about it is pooling across spatial locations um gives you some stability uh so for example here um if you're taking the max over this window um you're still going to identify that there's an edge there right there's like a there's still this like sort of white value that will get passed through um and that will be stable sort of as the inputs move right so so you'll get a large response um to like something like an edge filter regardless of the exact position of that edge you get some some more stability ility in the outputs um and then uh sometimes we also will do pooling across channels and one cool thing you could think about here is if you pull across feature channels where all the feature channels are looking for those edges but across different orientations of that edge filter um then you can get some new kinds of invariances so for example this would give you a filter that was finding edges and was um invariant to the orientation of the edge seems nice right cool so now if we actually look at sort of a lot of the time what um computation in a neural network looks like a convolutional neural network um we will usually take an input and then we will run it through stacks of this kind of building block right and this building block is some you know computation over some set of filters a nonlinear activation so a relu and then something like pooling and then we'll repeat that over and over and over again um but this is this is is a really sort of large network has a lot of density and as we sort of maybe add more feature channels um as we keep going this could get really big so particularly if you're trying to do something like classification um where the only thing you want to get out is sort of a a low rank representation of the input just something like a word um or like a specific like one hot encoding um here we actually often use something called down sampling where we assume that we don't actually need that highdimensional input structure um at every dimension of the network and so here instead of just pooling we might also down sample and what that looks like um so here if you have sort of pooling right you've now done something like uh your your convolutional layer your activation function and then something like max pooling um then downsampling would look something like this so maybe you would only keep some subset of those um actual values and then instead of doing the pooling across all of them um you can also just do just straight down sampling right um and one way that you can actually do both pooling and downsampling sort of together um is through something called a strided operation so this actually combines something like convolution or pooling and down sample it and the way that it does that is instead of going and sliding your filter by a single pixel every single time instead you increase what we call the stride of the filter so now you know you you operate your sort of convolution and then instead of just moving one you might move two or you might move five um in this particular example here we're showing a stride that's quite big usually um for these models we often have that stride be somewhat overlapping right so you you don't want to sort of lose any of the context from the original input so we'll have that stride be something that's overlapping but maybe your input kernel is like seven by seven your stride might be five something like that and so here you're kind of combining that uh that pooling that convolution and the downsampling into a single element um and you can also think about doing something that we call um using a dilated filter and essentially all this is is it's just a filter where we've set some of the values to zero and sort of baked that into our model and this gives you a larger receptive field so what I was talking about before you kind of get more information from a larger spatial context or spatial extent with fewer parameters there's less sort of flexibility there's less parameters that are learned but you're kind of getting um that larger uh context window um and that's kind of a trade-off that you can um optimize for so yeah oh sorry um so when you down sample you cannot go back to the previous input dimension unless you give information across the network right um we'll talk about that in a little bit so the question was um if you down sample you can't get back to the original input dimension unless you sort of add some more information that's not necessarily true um but you might not do it very well so we'll talk about that a little bit yeah what are the current norms for when it's acceptable to use um stride striding and might not do um what are the current norms uh for when you would or would not use a stride um so I would say that this search space of possible architectures is vast right and even if we only consider the building blocks we've talked about so far which is like linear layers and convolutional layers and then you can have varied input sizes varied depth various numbers of channels all of a sudden you already have a really large search space um there's entire fields of research called like neural architecture search that actually specifically try to optimize um over the space of architectures to find the best possible solution it's pretty computationally expensive to actually train those um but uh I would say that one of the things that's kind of proven out um in the literature over the last uh sort of maybe 10 years just experimentally is there seem to be some kind of standards that seem to be uh trends that are long long held so for example in the original AlexNet paper the filters were quite big right they had pretty large spatial extent and then there were less depth in the layers one of the things that we've seen more and more is now we actually often see filters that are reasonably small um seven or five um spatial extent uh but maybe then they're actually stacked quite deep so then you end up with really large receptive fields even though the initial filters were um spatially quite small um and then with striding again it's just that trade-off between the amount of computation and the receptive field so um there's different kind of optimalities and it can be really one of the things you can do is you can go look at a bunch of kind of the standard architectures and often they'll have these architecture diagrams you've seen them before probably where it's like you know uh the layer and then it'll have just like a layer size for each thing and you can look at like what are kind of the the similarities across the structures of these different models does the complexity of the picture matter at all for example the first example you gave which is time square versus the nature um yeah experimentally yes uh so if you have like really non-complex pictures like sefar for example these like 100 byund like really simple photos um then the the network itself um often needs to be much less complex if fine grained feature details actually matter a lot which happens for us a lot with the species images for example um then one of the things that actually tends to matter more than almost anything else is the input image size that you use so a lot of these models the input image size if you scale it up then the computation scales up sort of um quite a lot with that um so we'll often down sample images to some smaller size before we run them through a network as part of the pre-processing but we find that um increasing the scale of that input image and then maybe downscaling some of the computation is sometimes a better trade-off if you really care about fine grain features you don't want to kind of lose that that um detail so normally in processing um uh we design filters for a given purpose and in this case the weights that would be such a filter which you mentioned earlier model learn problem so how would you include do you or do we include biases in how we train our models so that we can design our models then do a certain purpose no different than design for example for signal or how does that how does the yeah it's a great question so so the question was um in in signal processing we're often very carefully handcrafting our filters so that they are capturing a certain dimension of the signal that we want to capture Um whereas here we're talking about filters that are actually being learned and what's the kind of trade-off like when does it make sense to try to actually sort of craft um the filters to match something about our our intuition about the problem so what you're talking about is actually sort of this massive paradigm shift that happened maybe 1015 years ago in the field where we went from um most of the work was on what we called feature engineering um in computer vision or in deep learning where you would be building these really handcrafted sort of structures that then you would combine together um into some larger system that would recognize stuff and when we started to see that filters just like these models being learned end to end were more predictive that was it was a bit of like a catastrophe emotionally for many researchers right because you were like "No like I've I spent my life learning how to craft these filters in this really you know specific way like I don't want to just cram a bunch of data through a structure and have that be learned and then have it be better." And it turns out that actually um the trade-off often comes down to how much data you have so if you don't have enough data to learn the filters well then more handcrafting more knowledge more sort of inductive bias um tends to be better right because you actually can put more constraints on the system so that you can optimize better with less data but if you're in a space where you can get sort of huge amounts of data to learn from often we don't know as much as we think we know about what optimality might be in terms of the filters that are learned especially when you think about it can be really intuitive to think about crafting a single filter but how would you handcraft like the seventh convolutional layer in a you know 128 layer network for a single channel right it it very rapidly gets to a place where the system is so complex that it's very difficult to build intuition about what it should be for us cool um so receptive fields so uh one of the things we talked about is if you let the sort of input size of the patch get too small you maybe won't be able to actually identify the things you care about so in this particular instance say maybe we have uh the input patches this entire bird and then you know we're running it through convolutional network relu convolutional layer and then and then we get some output um and the output for that patch is bird right so here what we would say is the receptive field of that x2 um output is the input patch there because receptive field essentially just says what parts of the initial input have any influence on this part of the output because we're doing this in a way that's um translation invariant we're doing the same computation across different patches um outside of that receptive field this model doesn't have any input information so if there was something just outside of that field of view that was really informative and we it wasn't actually in here then there's no way for the model to get information outside of that receptive field you can also talk about the receptive field of um input layers right so you can say okay like in this first layer the receptive field of that component of the output of that first layer is that part of the input um and what that um kind of looks like you know as you kind of pass through the network um you can think about looking at the activations and you can see that um they go from being very fine grained or very detailed in the early layers because maybe you're running them through um uh convolutions that are much more local and then as you get sort of further out or further along in you know Alex VGD16 reset 18 what you start to see are um feature activations that are kind of much more diffuse um and this has to do with that receptive field right because now each one of these output dimensions is actually collecting really complex information and mapping it through a bunch of different layers um from this input so uh maybe you're getting output feature activations that are more semantically meaningful but less um less sort of affected by uh like subtle texture or variation in the initial input image possibly is it similar in computation to how you did with blurry uh so the question is if it's similar in computation to how you do gaus and blurring um I think at a like at a high level like the intuition is a little similar but computationally it's quite different yeah in that last Are those ones six and eight or the ones on the the right are those uh the same pixel size as the initial inputs or are they down sampled and you just resize them to all be the same we've resized them all to be the same size but it actually is very dependent on the architecture so ResNets the the actual um spatial extent once you get to block eight is much lower and then there's a lot more channels versus um the Alexet one yeah cool so in the last little bit I want to talk through at a pretty high level um a bunch of these different highle ideas maybe some of these like what what one of you mentioned um some of these like things that have tended to um continue to be used over time so we talked about um one very very classic version of a convolutional neural network is um is something like a classification network and most classification networks have something like this structure right we'll have filters some sort of pointwise nonlinearity and then some corum of dance sampling and we run through a bunch of different layers like that to get out some sort of output classification but another really common structure in um using convolutions is what we call an encoder decoder architecture um so here what you're doing is you're taking that input and you're doing convolution nonlinearity subsampling or downsampling to get down to some very lowdimensional representation for example this zspace vector and then in the decoder component you're going to take that lowdimensional representation and you're going to now do convolution nonlinearity upsampling and now so in that upsampling component you're actually just increasing the spatial extent of each of the layers going forward and the training signal here is you want the decoded output to match the encoded input as close as possible right um now this is also used um so this is used as one way to kind of get a lowdimensional representation of an image um this type of encoder decoder architecture this is built into things like variational auto-enccoders masked autoenccoders are increasingly um widely used so those use um those use uh attention as opposed to convolution as a way to kind of share information over space um and you know you could use you can take these different components and you can break them out so we talked about um kind of differential programming um if you train an encoder decoder architecture you can use the encoder to build some representation of input images but you can also use the decoder to generate images um so these components can be used separately and can be valuable separately um but another way you might want to use this type of encoder decoder structure of architecture would be to do something like um get out semantic um segmentation because you want an output space that is that has spatial extent doesn't necessarily need to be the exact same resolution as the input though it can be um but you want something that has a spatial structure in the output space as opposed to just like a a single um category now this seems nice right but it turns out that this is both a blessing and a curse this bottleneck in the middle it's forcing the model to learn something that's kind of semantically useful but it also means that um often it's not possible with an architecture that just has downsampling and then upsampling to get a really precise um output that's the same size as the input because you've lost a lot of the spatial structure you've lost a lot of the information about the fine grain details in that spatial structure and so an alternative would be to take sort of um an approach to doing image to image like semantic segmentation where you don't lose any spatial structure so here you do you know convolution relu convolution softmax is a very very simple version of that um but every single one of these inputs because you're keeping the entire dimension um this can be really computationally expensive uh so so this the size of this model would be really huge so to kind of bridge and get the best of both worlds where you're kind of keeping a lot of the high resolution structure but you're also kind of building models that are computationally efficient and that are forced to learn something that's kind of semantically um meaningful is the unit so probably many of you have seen this or different variations of this but the the big intuition here is we have what are called these skip connections um across the different uh um encoder and decoder structures of this architecture so you actually have a connection that takes whatever the size is and it just takes that via the identity and lets um lets it be sort of skipped over towards the output and what this means is the model is not forced to kind of explicitly try to shove a bunch of fine grain spatial detail into some very low rank non-spatially dimensional embedding which is somewhat impossible it's able to kind of learn the semantic details through this information bottleneck but then it also gets a lot of the spatial detail through those skipped connections and this is really powerful unit is a really old architecture and it is still one of the most widely used architectures for semantic segmentation it's used all the time in things like um remote sensing uh like land cover mapping for example and this idea of a skipped connection this actually is a really useful um computational tool and it's also one of the fundamental underpinnings of what we call the ResNet architecture um so ResNet was invented by Kiming H who is also a professor now here at MIT um and this this architecture is kind of amazing um we still use this all the time and it's like 10 years old at this point uh but really um one of the important things that was recognized is essentially so one one kind of cool intuition about a ResNet is every single layer has this skipped connection so it has this identity pathway that lets you sort of choose um whether you want to run the input data through that layer or just kind of skip it right just take this um identity mapping and just pass it through to the next layer so one kind of cool intuition about this is this actually means that this is a model that can learn something about its architecture it can learn an optimal depth right because you can essentially say I want to just pass this through maybe three of these layers and then the rest of the time I'm just going to take these skipped connections out to the output um and so then the model itself can implicitly learn the optimal depth for the task that you want to learn so um one kind of cool intuition I think about resonance is that they kind of give the model the flexibility to kind of learn something optimal about the architecture without needing to test out sort of explicitly training a bunch of different architectures side by side and these residual connections um essentially are just you know x out equals function of x in so mapping it through that layer plus the input dimension itself and if you want to actually change the dimension the dimensionality so you want to have something like down sampling within a reset then you can also have another sort of set of linear weights that down samples in that skip connection cool this is a really really powerful um tool yeah so with these skip connections is the skip becoming another channel that's also like an out output channel in the middle or is it like actually choosing one um it's it's uh so it's kind of choosing one or the other um the so the residual connection is essentially uh you you assume that the output of your layer and the residual connection are the same size and you're just adding them together um but uh there's different implementations of this that will um have some sort of like learned weight so okay maybe you learn to completely ignore that identity um that same type of skipped connection um is kind of also something that gets surfaced via self attention in transformers um which we'll talk about more in the transformer architecture but essentially just this ability to learn how to combine information is valuable um if you if it is just an addition or a concatenation or you know however you want to do that skip connection one of the cool things about um linear layers or convolutional layers which are constrained linear layers is um essentially even if you just add them together there's some version of the next layer that could basically learn how to kind of ignore certain dimensions that that um input but uh yeah like for example here if you do actually want to change the dimensionality that W layer that could be learned to be all zeros right so you could just not keep any of that at all if that's what the model wants to learn cool so so far we've talked about two-dimensional or threedimensional grids but one of the things that of course comes up all the time in the real world are um additional dimensionalities one really simple one is time so here you know if you think about convolutions in time so maybe one version of this is you actually have uh just a single scaler valued input maybe something like temperature at a temperature sensor and then you could think about doing convolutions over time so instead of doing convolutions over a spatial dimension you could take convolutions over a temporal dimension and build some representation with time as one of the dimensions of that convolution um and now if you actually have something like video so for example now you actually have um an image with red green and blue channels and then you have these different frames of an image over time um you can think about building like what we'll call sort of another you know tensor representation or this um this block representation where you have um the spatial extent and here we're now using that third channel to represent um time but then you also have RGB for each of these right so it's actually a four-dimensional input and then you can think of building these 3D convolutions that actually convolve over space and time um and then you know your output would be maybe a lower dimensional representation that would actually have those space and time dimensions for each of your tensors um and this is a type of architecture this type of 3D convolutions is often used when you're trying to do something like action recognition in video um now one thing that this gives you is it makes you shift invariant in time right but you can imagine actually that for some types of recognition tasks you don't want to be shift and variant in time you don't want like picking a cup up to be the same action as putting a cup down and I mean they're assume ignoring the motion between if you had like really low frame rate if you had like this frame and that frame and then maybe you had that frame and this frame you want the fact that one happened before the other to matter that translation invariance is not something you always want um and so if you don't want to be shift invariant there's a couple different approaches um that we can use so one use an architecture that's not shift invariant so something like an MLP there the position in time would actually matter um but like we said these are it's it's quite difficult to get these MLPS to learn really complex functions of that almost infinite data so another option and one that is really kind of shown to be a winner is this idea of adding location information to the input to the convolutional filters through something that we call a positional encoding um so here um what we do is you have your input signal and then you also have this separate positional encoding which is something you construct this is not something that's learned it's actually constructed and added to the input and you learn an additional weight that incorporates that positional encoding as part of your convolution and so there you're kind of doing the same thing that you might with a normal convolutional layer but you're adding this additional information from position and so here we might want to introduce the concept of what what gets sometimes called a neural field um so a field uh from physics can be a varying physical quantity of um both spatial and temporal coordinates and a neural field is a field that is parameterized maybe fully or in part by a neural network right so um so there's different ways that you can think about doing this um and representing these neural fields um but one way to think about it um so siren is an example of one of these neural fields that that model we talked about earlier this is a convolutional neural network that's applied per pixel to map from a coordinate grid so now the input is the positional encoding in an image to a color so you're actually learning to model a single image by learning a functional mapping from a position in the image to a color right and the way that they're doing that is through these sort of syosoidal things and so here what you actually do is you take the input is actually just the position so in some coordinate space and and these um these could be explicit they could be um kind of batched up or they also could be continuous right because the idea is this is something that's mapping from some sort of uh position to an output so now you run that um these inputs through a model that's just taking X and Y and then you're outputting sort of the color of that um of that value and this can take continuous coordinates as input which means that we can then do something like uh think about uh learning these functional representations of input space as opposed to only grid-wise um representations and this kind of idea is the fundamental underpinning of of a quite a popular architecture recently um called Nerf uh which is essentially a convolutional network applied to a map from a fivedimensional coordinate grid which is about position and direction of the sort of the camera relative to the scene and then it gives you an output of color and volutric density and this allows us to then actually generate um what that same object would look like from different positions than what was in the original training data uh and so what that actually looks like is you can you know take some images of a scene and then generate these um essentially walkthroughs of of scenes and these have actually gotten better and better over time and one of the ways that this is really one of the fundamental underpinnings of this is this idea of building these appropriate positional encodings cool uh nerfs are really fun to play with uh recently some people at Woods Hole made a made a Nerf for under the sea that handles the way that light bends underwater very neat um so uh concluding remarks here um convolution is really just a fundamental operation for image processing or actually for processing anything that has some sort of structure so this might be something like time or even time space and um and multiple input dimensions so say you wanted to use convolution over you know hyperspectral satellite imagery taken once uh a week from the entire earth um essentially what it means is you're just chopping up the image into patches and then you're applying the same function to each patch but you're doing it through this like slide and sum operation and this concept appears in basically almost all modern architectures it appears in CNN's but also things like transformers um though then the patchwise operation isn't necessarily convolution use attention instead things like nerfs and more um so today we talked about building better architectures and why we talked about convolutional layers pyramids and kind of receptive fields we went through at a very high level some of these like different ideas that built out different types of architectures and then we talked about neural fields and positional encodings so we'll talk a lot more about positional encodings in um the transformers lecture um any final questions and if you are going to leave during questions just be quiet so that we can address them nerfs for upscaling could you use nerfs for upscaling um yes yeah yeah so um one of the ways that people have used nerfs is to kind of increase the resolution of like let's say like a point cloud representation not exactly like the original nerfs architecture but there's been adaptations of it that try to increase spatial resolution yeah any other questions yes uh and then quiet please you guys thank you so much yeah so um something like strided convolutions can actually that's actually explicitly doing some form of pooling and downsampling together um so one way you could think about it is like pooling um is essentially the same as as running a convolutional layer but now you're not learning weights you're just taking for example um like the the maximum function or that average function that's like the function that's applied on that local patch right so essentially it's just a constrained version a non-learned version of a convolutional layer yeah yes train yeah um so uh the way that you train it is you take a bunch of different images of a scene from different positions right so often the training data for nerfs will be someone taking a video like walking around an object for example and now you have a bunch of examples where now you're you're going to estimate the relative position to the scene um and then now you you can estimate the the position and the direction from the input image and that's your now for any given point that's your like training data and the output is the color of the image at that pixel is that generalizable or it's just used for like picture to video for that um it it can be used for a lot of different things um so I will say that only just recently like last week I saw the first kind of like large scale paper that was taking um this concept of Nerf which gives you kind of this creation of video that you can interpolate between and then actually trying to move that directly to something that was 3D modeling the scene because the output of Nerf is not a 3D model of the scene it's it's what that what the image would look like taken from a different direction which is not the same as a 3D model of the scene and actually a lot of um a lot of real world applications we would like to actually be able to use um sparse moninocular image representations to get out a robust 3D model of the scene and so they've just started to kind of bring those two things together um in recent work um from Ben Mildenhal I think was was the person um I will say uh these are also really not super robust like you need a lot of input data so classical Nerf like if you only have a a few sparse images of a scene um it can be really hard to fit a Nerf appropriately um and the other thing is it's it works really well with stuff that is like not moving but if you try and do it like a something like a waving tree um because the the actual thing itself won't uh the math doesn't necessarily optimize because the p the thing that you're taking images of is changing position over time that makes it really hard to fit these models yeah thank you so much yeah yes what about problems where the input is not just a picture but a picture and constants like u scaler numbers that also contribute to how the picture should be understood right so maybe like additional metadata um so for example I might have a picture and a GPS location where the picture was taken um so there's a lot of different ways that we try to incorporate that side information into into these architectures um and you can see lots of different examples but some really obvious stuff is you can just sort of run your image through that first convolutional layer and then you can have a sort of a separate type of convolution or representation of that positional encoding and you can either concatenate it to the entire input image you can introduce it slightly later in the network um there's a lot of different ways you can kind of append additional information um into the network during training yeah yeah cool thanks guys oh yeah you say there's any like significant difference you have a place length video um you like stack you just add additional channels for each uh like frame versus just doing a 3D comp um is there a big difference so if you add if you add channels for um all of the different frames in the video uh essentially that that is 3D convolution yeah because you're you're sort of taking those those channels and you're maybe running um you know your patchwise operation almost like a sliding window over time yeah I guess like you don't add another test dimension so you so sorry I'm a bit confused so if you have an image model that's taking an RGB and um height and width now you take that image model and now you're just saying like stack it over the different frames or I guess how would you not add another dimension if you have a time dimension like just RGB channels you just add more like RGB RGB for each frame oh I see um you could do that it would get really expensive um and I think one of the things uh that we definitely see with things like video um very rarely is that are those com convolutions in the temporal space worth it computationally so if you actually look at a lot of the state-of-the-art models for video um often they are downsampling in the temporal space because you can actually um you can actually recognize like most actions uh through like what we call like maybe a bag of images so you don't need every single image and the difference between the individual frames is really small um and so uh like Elioha of has a nice paper where they talk about the complexity of actions um by essentially taking any action recognition problem for with a video clip and then using the number of frames from that video clip that you need to be able to accurately recognize that action as a representation of the complexity of the problem and most actions are it's a single frame you can recognize it from a single frame so because of the computational complexity and the fact that often we want these models to maybe run in real time a lot of times the state-of-the-art models for video are doing something what we call like fast and slow where they will have like a really lowdimensional like subsampling um spatially and maybe even also temporally that that's running more frequently and then some sort of um secondary channel that's taking high resolution input so like maybe the full um image frame size or something quite close to it but it's only doing that periodically and either it's doing it periodically kind of in some pre-defined way or it's actually deciding when to run an image through that high resolution like channel so they'll take like sort of this two-pronged approach where like one model is maybe modeling something like the motion and the other one is modeling um some fine grain semantic details yeah video is fascinating and quite complicated and uh expensive expensive to store expensive to train models on expensive to run inference on yeah it's a mess
