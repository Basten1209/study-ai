# Lec 17. Generalization: Out-of-Distribution (OOD)

Source: https://notebooklm.google.com/notebook/1974010f-eec5-485b-b074-000c70149926
Notebook: MIT 6.7960 Deep Learning, Fall 2024
Source order: 18
Extracted: 2026-07-02

## Transcript

so today we're sort of moving on from generative models and we're going to start talking about um out of distribution generalization Um and this is I think one of the biggest open challenges for modern machine learning models Um the ability to generalize to data that is not explicitly covered within the training distribution Um and so just a bit of motivation here Um we've seen a lot of success in machine learning right Um we have incredible models for image classification And um this is kind of like a aggregate of the imageet data set I would als I would kind of argue that in many ways um things like simple categorization that the types of categorization we often try to do on on data sets like imagageet is is pretty much solved right Um there are of course like a few corner cases that are pretty difficult or sometimes even sort of erroneously labeled or ambiguous that are still kind of hard for modern models within ImageNet but mostly it's a solved problem Um we've seen pretty great success on image generation We've been talking about that the last couple days Um strategy games things like machine translation um robotic manipulation We're starting to see success in all of these Um but then there are a lot of cases where machine learning still isn't very successful and sometimes it can be surprising right So so I think one kind of interesting example here is is in the space of sort of self-driving cars right So we are starting to see self-driving cars be deployed I think you know Whimo in particular is starting to actually have fleets deployed in many different areas Um and of course there's complicated dynamics around like why we're not seeing self-driving cars at scale Uh regulation plays a whole huge role in that Um but I think one of the main reasons is because we we really don't have a good mechanism to guarantee the robustness of these models Um we can't guarantee that they are not going to make potentially harmful mistakes Um and this kind of really begs the question of you know are these machine learning systems really ready for the real world Um and and kind of what dimensions of deployment in the real world uh are we still missing like what do we need to be focusing on to be able to actually do this effectively Um and I do think there's an interesting sort of point here Um which is you could argue that a lot of machine learning is actually actively being deployed in the real world I think the GPT models are is a great example of that right Like people are using AI more and more day-to-day Um but if I'm trying to sort of characterize the types of AI that are ready for deployment versus the types of AI that even if we try to deploy them we try we invest you know billions of dollars into figuring out how to make them deployable we're still not seeing that progress um or actually really seeing it happen Um I think it kind of comes down to risk Um and so a lot of the cases where we see AI models deployed are what I would kind of characterize as high precision human verification systems right So you need the model to be good enough to be useful So it should work most of the time but you also have a human verifier in the loop um that is making corrections when something's not corre not right Right So you have this kind of verification loop that's always happening with human intelligence Um chatbt is a great example of this Right you prompt it with something you get a result maybe it's useful you probably edit it you probably like interact with it in some way Um if it's just absolute trash maybe you giggle about it you share it with your friends and then you prompt it with something completely different right So there's this human brain that's in the loop kind of verifying that these things work well And things like hallucination aren't so much of an issue if a human is making the corrections themselves It's when we start trusting these models without being human verified that we start running into issues Um and I think another interesting example of that is things like um a lot of the modern machine learning systems that are actively deployed in healthcare are what we call like decision support tools right So they're a tool that um maybe a radiologist uses to try to highlight or potentially give some um advice on what what maybe a diagnosis could be and then the the human is actually liable to verify that and make a decision And interestingly there's been a bunch of fascinating work that's come out recently that really argues both sides of whether or not this is working Um there's research that showed that these decision support tools are really great for trying to make sure that um patient outcomes are beneficial that um doctors aren't missing things And then there's conflicting research that explicitly says that the deployment of these decision support tools is actively harming patient outcomes because doctors are becoming compliant They're not paying attention and they're missing things in that long tale of the distribution that are actually quite important Um so kind of the jury is still out and fascinating if you look at the regulation around what it what it actually takes to deploy a decision support tool in a hospital the metrics are kind of shocking It's I I I'm not going to quote myself on the details but essentially it's like you have to prove some percent accuracy on some small population of of data but it's not nearly what you would expect I remember being quite surprised on the order of like a hundred or a thousand patients um to be able to deploy a decision support tool And you don't have to go through anything like clinical trials because you're not actually making the decision You're just trying to support the decision of an expert Um so really when we have this case this is where we start to see things working well It's kind of the standard machine learning setting and it matches a lot of the underlying assumptions we make in machine learning um even theoretically uh that the data that we are training the model on is going to be iid independently and identically distributed distributed to the data we're going to use the model on right so you have some distribution over categories of interest data different sort of representative like visual characteristics within a category um and you're going to sample from that at random for training and then you're going to do inference on the rest of the data Training distribution equals test distribution This is never true in the real world Um so basically there has never been any such thing as a representative data set of all reality And one of the reasons that this is true even with our best efforts right things like these these huge data sets lie on you know billions of image data sets that try to just tackle this problem with pure scale right We're going to train on all the data we have and hope that that is close to being representative and iid to the data we want to use the models on It still doesn't work because the world is changing right So if you capture all the data today it's not going to necessarily be representative of a month from now a week from now Things happen things change Um and so we often have something like this right There's there's some fundamental difference between the training and test distribution once we move to a deployment setting Um and so here you know maybe some version of that would be something like all right we've we've trained a model on Cambridge so this is like this data center and now uh we're trying to use that model in in a different season Now there's snow on the ground and and the model starts to fail And there's a lot of different ways that we can think about how those distributions can change Um so the data itself can become perturbed Um so that could be adversarially We'll talk about that a bit in a bit Um but it also can be things simple things like ah the manufacturer of the cameras for Google Street View cars made an update to their system and now there's some statistical difference in terms of the sensor itself Um you can get a different label distribution So you know maybe um the set of sort of categories of interest has changed you end up um with something that used to be really rare becoming much more common or something just disappears entirely And then there can be lots of other types of shifts Um these can be sort of shifts in sort of the size of the sequences or the graphs you're modeling weather uh geospatial shifts from like different countries or cities source of measurement That's like the the sensor example I said So what actually goes wrong when when we have these sort of distribution differences Um so I thought this was kind of a cute example It's a little hard to read actually probably because I took a shitty screenshot Um but so this is a a tweet by Hard Mario that was basically talking about um uh essentially there was a failure mode in Roombas where they kept rolling over dog poop and then smearing poop all over people's houses And so in order to try to avoid this happening they went out and they collected a very large diverse data set of dog poop So I'm sure that was someone's like research internship Um and then so you know they're talking about oh okay in order to do this right like the way that we're going to generalize to this like sort of new out of distribution thing which is like dog poop that we didn't expect to be on the floor in someone's house We're going to collect this representative data set of dog poop And then the response was from Dimmitri and he said "Man I wish they'd also created a diverse data set of rugs so it didn't confuse black stripes with cliffs and I could finally get my whole house cleaned." So this is kind of pointing out like you can you can collect data for the distribution shift you do anticipate but there's always going to be things you don't anticipate right And then you end up with Dimmitri who bought a Roomba and basically gets like one tiny square of his rug cleaned a million times Um yeah so it's this is a silly silly example but the point here is really that we cannot possibly characterize and anticipate every way that the world is going to change Um and so this idea that we can collect representative sufficient data to just ensure that where we're going to deploy the model is iid from what we train the data on is quite difficult um this is reflected in quite um concrete problems and when you're talking talking about AI safety the safety of things that are deployed particularly in high-risisk scenarios things like medicine or self-driving cars Um this was a a a paper that was explicitly characterizing what they thought were the largest problems in AI in AI safety and robust robustness to distributional change was was one of them Um there was this really interesting workshop that happened back in 2022 at Princeton that they called the reproducibility crisis in machine learning based science Um they looked at about a thousand different papers across a huge diversity of scientific disciplines and they essentially found that um they diagnosed a bunch of different potential uh reasons why they weren't actually seeing reproducibility or sort of generalizability in in the papers that they were um that they were analyzing And one of the biggest sort of failure modes here was data poisoning So basically cons the way that they constructed the evaluation for these machine learning systems was in some way artificially iid that in a way that wasn't representative of reality Um and this is something that I actually run into a lot I I um I review a lot of papers for uh my sort of other half discipline which is like ecology or environmental science Um and in those papers often um what I see is a mismatch between the data that people are de developing and training and evaluating machine learning methods on and the claims that they're making So it's fine to sort of have a limited data set but then you have to understand what that means about what you can claim the effectiveness of the model is going to do Right If you have built a system that identifies all of the individual macaks in a national park you can say something reasonable about your ability to monitor macox in that national park going forward as long as you've evaluated your ability to understand the changes in time for those individual macox right And so somehow the way you evaluate these models needs to be representative of the actual deployment use And I think this takes a lot of really careful construction and thought Actually the way we think about designing evaluation systems for machine learning models is really non-trivial And if you use any sort of off-the-shelf tool things like Rooflow it just automatically assumes that random splitting is fine And you get often very sort of f falsely high expectations in terms of the performance of your machine learning model because you've got data poisoning essentially strong spatial temporal or contextual correlations in your training and test data that make the model think it's doing well when it's actually not So for today um we're going to be talking first about specifically adversarial examples um and thinking about how you might uh train um to address adversarial examples So this is sort of small perturbations um ways that you think about robustness on the level of individual data points Um and then after that we're going to be talking about distribution shifts So actually just these large scale strips from um entire distributions of data and uh some simple mechanisms that people use to try to address them Though I do want to point out that both of these are pretty active open areas of research So I can talk about progress but we don't have solutions for these So first um if we're talking about adversarial examples um so this is like a assume we have a simple machine learning classifier right Um we have this image of a pig The model says "Yep that's a pig 91% confidence." What an adversarial example does is it adds some very small amount of random or not not random but what looks like random noise um to this image And you could see like it's 0005 times this sort of uh thing that looks random Um and so when you're adding this noise which is explicitly not random it's actually optimized to be maximally adversarial you can actually change the prediction model to be something completely different maybe even with higher confidence than the init initial prediction Um even though to me this image looks identical right I can see no actual visi visible difference between the two images Um and so this kind of points to the brittleleness within um often machine learning models right They can be mostly accurate but they can be really brittle to things that um a human visual system is not brittle to at all And this is a big challenge right So um you can imagine that uh if you could do this sort of adversarial attack in something like self-driving cars you could actually get models to explicitly change their decisions about street signs Um and um that would require that you're able to actually generate something that in physical reality is not just related to individual images It's actually in physical reality But that can also be done So let's see There's a I have a movie here but I wonder if it'll play Huh Yeah there we go Um so this is an example So here's a classifier right It's classifying banana If you put a toaster in there it doesn't change the prediction But if you put this very specially constructed adversarial sticker in there it really convinces the model that it is in fact a toaster And so if we watch that again um so the interesting thing here is that I would argue from a classification standpoint like this is really ambiguous You would expect a model would actually maybe even reasonably predict toaster they're about the same size whatever Um whereas here this looks nothing really like a toaster to us Um and so the interesting thing here is that this kind of demonstrates that it's not something about sort of high level image characteristics that you can actually really convince these models or trick these models even in physical reality Um and there are fun examples of this even in 3D Um so this is an adversarial 3D printed example of a turtle that most image classification models will categorize very confidently as a rifle Um and you can actually sort of show uh there's like a fun video if you go look this up Oh here we go Perfect Um yeah So it stays the prediction stays rifle even if you move it around right Like it exists in 3D Um and you're all you're doing is changing the texture Um and they basically showed that uh their 3D turtle was really consistently predicted as either rifle or sometimes as something else but it was never actually categorized as a turtle And you can do this type of adversarial attack for speech as well right Um you can add just a little bit of sort of very explicitly targeted noise in a way that perceptually if we listened to these or even visually within the waveform looks like it's made very very little difference and it will actually um the model will predict that the translation of that speech is completely different So what does this mean about our models Um are they useless Right Like what about them is so brittle Um why does this happen and can you prevent it Um so a bit of a history of adversarial examples Um so this has been something that people have been looking at for quite a long time Um some of the first works in the space were from back in 2004 Um and there they were looking at adversarial um examples for things like simple linear classifiers And one of the sort of original motivating examples was a spam filter Right So if you tried to build some classifier that tried to categorize whether an email was spam or not could you basically from a red team blue team perspective try to actually build emails that would explicitly get through your spam filter Um and uh this has sort of only gone on from there Um there's been a significant amount of work This this was review back from even 2018 But now um particularly maybe in the context of uh today being voting day in the United States um there's a lot of work in this type of adversarial um dimension looking at misinformation filters now So how do you sort of how do you try to detect and categorize active misinformation or deep fakes um on the internet on social media platforms and then the the sort of active antagonism of trying to actually develop um deep fakes that are undetectable by these algorithms So it's important and it's difficult So we'll talk about um how do you actually do this in maybe the simplest sense How do you create an adversarial example So at first we're going to assume at least for now that we have some fixed machine learning model right Um and so now what we're doing is we're actually optimizing over data related to that model So what we want to do is we want to find a small perturbation to a given image that doesn't change the meaning to a human but does change the meaning to a machine learning model And so the model is going to output some you know probability of given theta So like of some given um machine learning model uh weights and architecture um some probability of y the category given x right we might take some softmax to get to get that prediction Um so what an adversarial example does is instead now you try to solve this optimization problem that finds the maximum um overall possible small like delta so small changes to x um that you now instead predict a different pro a different category So some target category that is not the original category And you can do this for any category right You could find the noise that would make this pig look most like an airliner but you could also find the noise that made this pig look most like I don't know your friend Bob Um and so you pick some wrong class and then you have some given input image And then you find a small perturbation um that actually maximizes this And this will be within the constraints of some set of uh quote unquote allowed perturbations Um and so uh a really common thing here is actually um that we look at this um taking like an infinity norm over the the perturbation So um some intuition here is like instead of just looking at sort of the aggregate difference between um the image and the the new one um instead you're actually constraining it such that the largest change for any given pixel has to be less than a certain amount Right So the infinity norm essentially is saying like you're going to actually um just keep the maximum change for any pixel small Um and that that does tend to actually result in things that are unperceptible to humans Um cool So how do you actually do this Um so how do you you know sort of given some machine learning model that you assume is not changing how do you find that maximum perturbation So um instead of trying to sort of solve this as an optimization problem you can actually also uh basically flip SGD on its head and you can do this thing that people call projected gradient ascent um where we're updating that data perturbation over time And so first you would take a step in the direction of the gradient right Um and then you project the result back into your sort of feasible set So you're you're constraining the amount that these images can change by design right You're making sure that it's obeying that um that optimization constraint that delta can't be too large And then you just repeat this over and over again Um and this is quite simple and it and it actually works quite well Um so how do you actually defend against this Is there is there any way to sort of defend against these sort of active adversarial examples So um another way to think about this adversarial example um instead of sort of trying to yeah uh for the previous slides um how do you actually what kind of algorithms can you use to actually find delta So you need like for example access to the gradients of the original model Yeah Yeah So so this um projected gradient ascent assumes that you have the model weights and the architecture so you could actually calculate the gradients Yeah Is there a method that doesn't need the actual gradients Um so the question is uh if there's methods that don't need the actual gradients of the model um yeah I mean you could do it in a brute force way too right Like you could just think about trying a bunch of things and just picking the one greedily that sort of the result was worst Um might be more expensive So this is just maybe a more efficient way to find something Um yeah And then I there there are like other methods that sort of try to do this um maybe uh in different ways right So not necessarily assuming that the model can't change um but that's of course a bit more complicated Okay so a different way to think about finding an adversarial example is instead of sort of trying to explicitly um maximize the probability that your image is predicted as a given label you can also think about it as just maximizing the loss between the your model's sort of um prediction of the image plus that adversarial amount and the correct label Y So here you're not sort of saying I really want this to look I want the model to think it's this specific category You're just saying okay what's the noise that will make this sort of most wrong that'll make the loss highest for this example Whereas you know in in standard training you're trying to minimize that loss sort of all over all the possible thetas in the model itself Um so then what you can think about doing is like if you have standard training where you're using SGD and you're trying to minimize the sort of loss over sort of all of the different data points in your model you can think about um training adversarially right Like trying to actually get your model to be robust to the types of perturbations that we're um expecting this adversarial model might have this adversarial approach might have right So here now you have kind of this this two loop or this sort of nested optimization So now you're trying to find the model such that for every image in your data set the worstcase perturbation is still not too bad essentially like you're trying to um during training find the worst case perturbation and then find the sort of model that for all of the different like um all the different data points in your training data set um is still sort of as good as possible even in those worst cases Does that make sense Um so so there's this also can be called sort of adaptive data augmentation Um and it's kind of like an interesting example of sort of finding these like hardest cases Um so we learned uh a while ago about like um hard example mining right Like it's it's sort of maybe most informative sometimes if you show models explicit examples of things you know are going to be hard and you want it still do well Um yeah what happens if there are like two similarly hard examples but like very different from each other So the question is what happens if you have two hard examples that are different from each other Um in this case the like the maximally hard example or for the turtle was rifle but like you know besides rifle there's like apple that is like on like a similar scale as being like the rifle example but basically you've only trained for like an adversary example where it would be like something related to a rifle right okay I think I I think I get what you're asking No no no I I I I actually think it's a pretty preient question It's just worded a little complicatedly So um so essentially the question is like what if the data changes in a way you didn't anticipate Um and so this is really explicitly constrained to be robust to the types of perturbations that you've defined as sort of within your reasonable set of perturbations right Um and so the cases we talked about were something that looked like very sort of small amounts of noise added to images Um but we'll we'll talk about in a bit like most predurbations of data are not of that structure in the real world right Like often if you have something that's out of distribution in some way um adversarial in some way it's not um it's not sort of specifically the static noise And for example this type of approach would probably not work very well for those stickers because that's a different type of perturbation of an image Did you have a question I was basically going to ask fair criticism is that like this won't help your self-driving car detect a pedestrian in a crosswalk It's like that's not a probation that would be modeled mathematically Yeah Or I mean pedestrian in a crosswalk probably you could reasonably say you could you could think about ways to augment your data to capture that in a bunch of different ways but then what you end up with is like weird stuff right Like the there's this classic example of like a bunch of stop signs being transported by a truck in the back of a truck and there's a self-driving car behind a truck full of stop signs and it's just freaking out right Because like what is it supposed to do Um and so like yeah like like this is kind of again like I said before it's just really hard to anticipate every way the world is going to change So if you could do that then you could reasonably try to be robust either just through sort of standard training or if you didn't have the right training data but you knew something about the way the world might change you could try to be robust explicitly to those types of things You could use things like empirical risk minimization but that is often not the case in reality And so a lot of the work in this sort of adversarial example adversarial robustness they have to make assumptions and usually those assumptions are violated in real world unfortunately Yes Um is there anything stopping this from being like a like a continuous loop of like minimizing loss and finding an adversarial example that maximizes the loss and just continue doing that back and forth or can a model like eventually be trained so that you can So for a reasonable set of um perturbations and assuming that you know like maybe you try a bunch of different hyperparameters it is usually is often possible to get these to converge but it can be difficult right because you basically have two different optimization objectives that are conflicting and so they can be pretty finicky to train Yeah cool Um so now if you're actually doing that type of adversarial training um what you actually do sort of in the training loop is you would sample some data point X with the ground truth Y you would compute now the approximate optimal adversarial perturbation right and you could do that with that projected gradient assent um and then you would compute the gradient given that perturbation and then you would update your loss given that gradient for the parameters right so you're it's essentially like a for every for every model you're like train for every data point you're training another model Um and we'll talk a bit about it next lecture but this also is sort of similar to things like metalarning right where um potentially in your outer model training loop now for every data point you actually have an inner training loop and then so it's also quite computationally expensive to do this Cool So what do these examples tell us Um what does the fact that these adversarial examples exist tell us about the models itself Um so arguably I would say it tells us something about the input features sort of the structure the statistical um characteristics of the data that we're training the model on that become critical for the model's decision So there's an interesting example here Um this is training data trying to teach a model to categorize fours versus nines And the interesting so this is like emnest essentially but the interesting point here is that what they've done is they've added spurious correlations that are very small So for every four they've added um a white pixel in the top left corner and then for every nine they've added a white pixel just one next to that So now what ends up happening is if you do these adversarial perturbations that are very targeted you basically just change the location of that pixel you get the exact opposite prediction And this really gets at this kind of question of like what are these models learning and they're going to learn whatever signals are there and ideally whatever signals are easiest to pick up So this becomes just like a binary classifier right If you consistently have these correlations the model just ignores the rest of the image It learns all it has to look at is just that top pixel and it's a perfect categorizer Um and so what this can kind of tell us is that for any given model um the sort of features that are captured in that model some amount of them will be useless some amount of them will be robust Um so these are features that are correlated with the label even if they've been perturbed in some way Um so you know here they're showing things that are quite semantic right things like um specific attributes of like you know a cat ear a dog nose or like the sort of structural shape of an animal Um and then there's another set of features that you could argue are not robust right And these are going to be features that are correlated with the label in the training data set but can be flipped with perturbation Right So perturbing these non-ro features um can actually change your prediction And so many features can be correlated with the label in your training data set and thus be predictive And they might also be correlated with the label in your test data set and be predictive And so they can help with accuracy right Like like your accuracy is good but they're features that are beyond what a human would use or sometimes maybe in some subtle cases things that we might use based on maybe like stereotypes but if we think rationally about it we might not want to use them So um there's a lot of examples of these types of stereotypes or biases in training data sets um that are sort of against women or gender minorities right like if you show chat early chad GBT was very very popular you know oh if you if you asked it to find generate im or not chbt um diffusion models if you asked it to generate images of a doctor gave you a bunch of white men if you asked it to generate images of uh you know a homemaker it would give you a bunch of women just like biases that are built into the data sets that might be correlated with society but unfortunately are things that we if we think about it from an ethical perspective we don't want models to make decisions on right um and this gets really complicated there's this entire sort of deep literature on um fairness in machine learning models and how you think about equity when the data we're training models on is just as biased as our modern society right Like and these models tend to exacerbate societal biases Um so correlative predictive features are not always something we want the model to use both because they can make it non-rose but also because it can exacerbate bias And where do the correlations come from They really come from the data right So um as a fun example uh dogs and cats Turns out if you put a dog in a bow tie the model will predict it's a cat because there's this weird bias where people put bow ties on cats more than dogs Um or this is a really fun one Um if you if you look at all the images of fish in the imageet training data set you get like grumpy dudes in sunglasses maybe very happy dudes in sunglasses They're going fishing and they love it But um the model's not learned fish it's learned like fish in the hands of a person Um and then you know like basically this really is just rooted in how we create data sets right Um so in an ideal world we would have representative real world images that sort of captured the distribution of everything we would ever care about We would have expert annotators who were then able to build perfect annotations on top of that and then we could use that to carefully and meaningfully construct a benchmark We could evaluate machine learning performance and you know happy sun and rainbows But in the real world we often have images scraped from the internet Um and then we have some form of automated or crowd-colcted labels And then that results in sort of these noisy complicated bias annotations And that might create an easy to optimize benchmark but it's not necessarily something that is rooted in reality And um one argument is that all these models are learning are shortcuts It's all kind of different types of pattern matching And so shortcuts here are features that are correlated to the label in the training data but maybe not correlated with the label under realistic distribution shifts And it turns out that these models will use them and will not generalize if then those features are no longer correlated So um and and interestingly there's a dimension of imbalance here So uh in some of the things that I try to do I'm doing species identification um and static cameras And what we get is um dayight bias for nondal species So like uh raccoons show up a lot at night And so we still have some images of raccoons during the day during training but the vast majority are at night And so the models learned that it's like ah nighttime kind of like fat cat blob thing probably a raccoon And if you show it daytime raccoon images even though it's seen them during training the performance is just much worse because this like uh correlative predictive factor is not there Um and so the models will use these shortcuts when they're provided because we train them based on likelihood right And this is almost this is often related to the data itself right And so interestingly adversarial examples those types of like perturbations we talked about um can transfer across models trained on the same data set So like the same types of shortcuts will often be learned even if you use different architectures because it's trained on the same data set And uh what can these shortcuts look like Um yeah things like this is a picture of a green hillside and this this model is explicitly saying a herd of sheep grazing on a hillside but there's no sheep Um this one says a flock of birds flying in the air These are instead uh these really nice treebased goats I don't know if you guys have seen these They're amazing Goats that climb trees in the Mediterranean Um and then you know things like this uh it's a people holding goats but the model predicts their dogs right Because it's seen less of these examples of people holding dogs And these can be these are all kind of silly but they can be pretty problematic as well right So um there were some there was some early work that was showing that um you could do a lot of like really nice predictive um diagnostics on X-rays and then there was this pushback work that showed that actually what the models were learning was to detect where the X-ray was required acquired So which hospital and then calibrate the predictions according to the distribution of diseases at that hospital which is again probably the type of like useful but unwanted correlation Um or similarly like it's learning that if an image has a ruler in it an algorithm is more likely to call it tumor malignant Um and so you can imagine now that essentially what this is meaning is like you're much less likely to detect the examples where a doctor hasn't already thought that it might be malignant and is like measuring it with a ruler So not all predictive patterns are desirable and we talked about those fairness examples as well And there are tons of examples of this right Um and you can think about this in interesting ways So here um this is like you know essentially like going from iid to out of so um independently identically distributed to out of distribution Um you can see domain shift adversarial examples types of like distortions um pose warping texture change background shifts All of these will often result in a human looking at these images and saying it is the same category but the model ends up uh not predicting the same category So this is things that we want the model to generalize on but it doesn't Um and then there's the opposite too which is um things where the machine learning model is very convinced it's the same category but a human would not be Um and this happens for transformer models as well right So um this was a a case where uh that people had trained transformer models um to look at like reasoning um but it turned out that they pretty explicitly couldn't generalize out of the distribution of the number of the sort of sequence lengths that they were trained on So adversarial training when you're sort of training it to be robust to these types of non-roust features and now here you know we're assuming that we know the dimensions of robustness which of course we we don't often know um they do tend to teach u models that have outputs that are hopefully um stable under these types of adversarial perturbations So you get invariance to these non-roust features Um and of course like this type of just like uh noise addition is not necessarily representative of every possible way that this data can be preser perturbed But there are some nice papers that show that if you train in this adversarial way it can be much more expensive But you can get features that tend to be um more human intuitive like now I I'm always a little bit iffy on the use of uh things like these activation heat maps as like um concrete uh explainers of what a machine learning model is doing But these do look interesting So um here if you look at uh like a primate um here's just some sort of uh if you train with sort of an L infinity or an L2 norm you can see that the features are like a little bit more semantically meaningful like they look a little bit more like the object of interest um and similarly similarly if you're trying to build um these sort of adversarial examples for robust models now if you've trained it to be adversarial adversarially robust then the thing that your the the data that your model actually generates to be adversarial um has to look a little bit more like the semantic category you're trying to make it adversarial to So here if you start from these pigeons and you try to find the adversarial example that makes them look maximally like dogs like this kind of looks like dogs It just really doesn't look like pigeons anymore at all And so these are things that you know a human is sort of more okay with these types of mistakes And there are some papers that show that adversarial trend models um maybe build representations that uh transfer better to other data sets um at least specifically um with some of these like sort of uh the data sets shown here Cool So adversarial training you think of as like another very targeted form of maybe regularization or you know a different type of demand adaptation but one that's quite targeted to try to make the models robust to some explicitly constructed form of perturbation So now what if we're not talking about perturbation on individual data points but we're in instead talking about perturbations over entire distributions Right So here instead of having the sort of same distribution for training and inference and now we have a pretty different distribution for training and inference Um so this is basically like the world I live in Um so here this on the left that's a map of an estimate of alpha diversity globally Um which is an estimate of essentially the number of unique species that exist in in any given place Um and then on the right this is a map a heat map of uh the three billion existing species occurrence records in the global biodiversity information facility And you can see that they are clearly not super well aligned right They're almost anti-correlated Um so you can imagine that if this is my training distribution and somehow that's like the distribution of the data maybe I want to use the model on that that's a pretty significant distribution shift And then add on top of that the fact that all of it's changing all the time and it gets pretty messy Um and this was the motivation actually behind the creation of this wilds benchmark which I built with many other very talented people Um specifically it was reflecting the fact that a lot of the literature around distribution shift in the machine learning community were sort of these these types of not realistic distribution shifts right These like adding noise to things or taking emnest and like rotating it by 45 degrees or like making it a different color you know like not necessarily super representative Even some of the examples that were designed on real data like I don't know if you've ever heard of the water birds data set It's basically a data set of water birds where like the training data has water birds not in the water and the test data has them in the water You know it's a background shift problem but it's pretty artificial right You've constrained it so that the categories are balanced that you only looking at one dimension of shift and the real world is quite messy So this is a benchmark that looks at distribution shifts over a pretty diverse set of real world domains Everything from um you know uh different sort of scans from hospitals to um different locations of species categorization um generalizing like mapping of poverty across countries um looking at uh like comments across demographics And essentially what this benchmark um highlighted was that when you move out of distribution in these realistic ways um basically model performance degrades significantly Um and the way you actually kind of think about measuring this is you can try to explicitly construct benchmarks that look at performance over realistic distribution shifts So here we looked at the gap between a control so in distribution test data and note that this is not actually identical It's just randomly sampled um from within the training data locations and then you have some new cameras that are sort of held out that are out of distribution And you can see that macro F1 performance degrades by like 16% Which is pretty significant And if you're thinking about sort of what are the factors that could lead to this there's a bunch of different things right It could explicitly be things like background It can be things like pose of cameras right So the top and the bottom are sort of more horizontal The one in the middle is posed a little higher So you're getting sort of different angles on species Um here you know you can see some of them have passive infrared at night Some of them have color So like the dimensions of shift in the real world can often be quite complex But not just that visual shift we also have pretty explicit what's what can be termed subpopul shift So essentially for any given location or any given camera it has its own unique distribution over the categories of interest It makes sense right Like that camera is placed in a microhabitat That microhabitat has a local set of species that's interacting with it Um and it turns out that this is basically true for every ecological problem I've ever worked on Um so different ecosystems will have both subpopul and visual distribution shifts no matter what Um this is looking at detecting individual trees from aerial data across the national ecological observatory network Trees both look different and are at different densities Um you can similarly try to think about um categorizing uh detecting and counting fish in different rivers across the Pacific Northwest um or things like uh categorizing um urban trees um across uh different cities in North America And you can see that both visually and from the sense of subopul So here the colors are kind of mapping the 20 most common genre of trees and the differences in colors between cities kind of capture that subpopul distribution And it turns out the performance often has a very strong correlation with subpopul distribution similarity Right So this is if you're looking at training on one city for urban forests and and testing on another um you actually get these kind of blockwise structures where cities that have similar training distributions will gen like you'll generalize better within the set of cities that have similar training distributions It's kind of intuitive Um these models again are trained on likelihood So you're almost building a prior based on the subopul distribution within any given training set And if you're matching that more closely your model's more likely to kind of predict well Um and there's some nice examples of a sort of simple simple approaches you can try to do to actually um match the expected um distribution of your test data even without additional training So things like just if you can build or if you can represent a prior if you have some way to construct a prior over the set of categories you expect to see then you can just do a really simple sort of Beijian remapping of the predicted probabilities to um kind of take into account that prior I'll talk about that in a little So what do you do about distribution shift So one thing that people have looked at is this um that's quite similar to the adversarial example we talked about before is something called distributionally robust optimization So so far we were sort of saying we were allowed to perturb each data point by some limited amount But an alternative is that you can actually think about perturbing the entire training distribution um by some amount together Right And so now what this looks like is um like distribution robust optimization essentially just assumes that the worstc case data distribution shift might occur So you know you might um have some different sort of samples in terms of the densities you might sort of take some of the data away etc etc Um and so they they think about ways you can perturb the distribution of your training data set And then they take kind of a simple similar approach to what we did with the adversarial examples but now you're trying to find the model such that in expectation over sort of all of the possible distribution shifts on a data set Um you're still going to minimize the loss Um and so you're basically again finding that worst case scenario and trying to make sure that the model is still robust to that um for any given sort of wor for any given distribution But again this like assumes that now you can only move the data distribution sort of a certain amount and measuring the difference in pixels is one thing but actually trying to think about measuring the distance between probability distributions can be quite complicated right so there's like kai square distance people use things like waserstein distance maximum mean discrepancy um but how you actually measure how far away two distributions are um if they're sort of simple like measurable distributions in one dimension is one thing If you're trying to measure the difference in distribution between one million image data set and another million image data set it gets a lot more complicated And a lot of times what people will do is they'll actually leverage learned representations of those data sets and then try to make some metric for the similarity across like maybe the clustering the representation space This stuff is pretty tricky Um and so it's not definitely something that works super well in practice Um so when you're actually trying to think about what this means for generalization essentially DRRO optimizes for this sort of set of possible training data sets and distributions And so if you have some underlying data distribution P and your empirical training data is P subn so some subset of that or some specific like characterization of that then if you've built some distance metric and you say that the new distribution of where you're going to use the model is within some distance of the true distribution um then you're guaranteed to perform well on the true distribution you're guaranteed to generalize But of course I'm sure you can see the challenge of the limitation here which is that you have to be able to measure the the distance between distributions and you have to be able to sample these hypothetical distributions in a way that's realistic And of course the more complicated your data structures are the harder that gets Um so interestingly this has been applied pretty effectively when you're talking about some of these class imbalance um challenges So here you can use DRRO and so you assume your population has some number of subgroups So population here could be any given category So let's say the raccoons and now maybe our raccoons have two subgroups like nighttime and daytime images of raccoons Now you can sort of traditionally you'd think about trying to minimize the empirical risk or sort of the average error across these different subgroups assuming you have access to the actual labels of those subgroups which maybe for day versus night we do because we have a clock for our time or for our camera Um but it turns out that if you have sort of a minority subgroup um if you get 50% error on that minority group in terms of accuracy um that's only a 10% overall error So your model might learn it's effective to just ignore the minority group Um so you can ignore the minority group and still get decent loss And so the idea is that DRRO can learn to sort of effectively automatically rewe your data to pay more attention to those minority classes Um and so uh what that might actually look like is it's forcing the model to play pay place less emphasis on kind of noisy features and more emphasis on useful and predictive features Um and it is also seems to be explicitly like empirically at least um shifting the distribution so that they're more separable Um but uh there's some really nice work actually that shows that you can do this and there's sort of adaptations of DRRO to do this even if you don't know what the subgroups are or how many subgroups there are Um so people use DRRO a lot for some of these fairness type of problems right You want a model to learn to recognize doctors and you don't want it to be biased against female doctors or black doctors even though the training data is biased against those groups And so sort of in that like worstcase distribution shift you could imagine you might only sample kind of those minority groups and then your model performance would be significantly worse if you aren't sort of making sure that you're robust to that So what do we do about distribution shift First you can try distributionally robust optimization or there's many other sort of ways to think about um building robustness sort of extensions of this etc that take an algorithmic approach um to trying to reduce um to trying to make models more robust to these types of distribution shifts or make them more fair Um you can also do things like explicitly learn priors over the distribution shifts that you see So for example subop subop shift So this is a case um this is an example um by Oshin McKay where they looked at species identification and they explicitly learned separately a modular like spatial temporal prior over the places and times that then they used to remap the likelihoods of any given probability or any given species um based on where the image was taken Makes sense This is what ecologists do right often they're categories that look really similar And if you um if you just sort of saw the image without any context about where when it was taken it might be hard even for an expert to be able to distinguish them But if you have that that um that additional information it's quite useful And so this is just addressing this kind of generalization problem as like an image categorization with this kind of specialization that's coming from a prior based on space and time So you can think about capturing some of the information you might have about the structure of your domain or the distributions um sort of spatially temporally etc within your domain and actually building that directly into your model as a way to to do a better job and those models improve species accuracy by like 10% on a naturalist You can think about um mechanisms like domain adaptation So this is actually um an entire sort of category of models that tries to learn given um a source domain and a target domain where maybe you don't necessarily have labels for your target domain how to align their representations during training so that you can enforce generalization or adaptation to a new target domain even if you don't have a lot of data or labeled data I'll talk a little bit more about that next lecture Um this is pretty interesting you can come up with creative ways to try to diagnose the failure modes of your model which often teaches you more about the biases in your initial training data than anything else but they can be really interesting And so there's ways to think about doing that by just striding your evaluation So breaking down evaluation per category along different dimensions like night versus day or different sort of uh sexes of the participants assuming you have the labels to be able to do that break that down and look at the um relative sort of maybe call it fairness across the category set Um this is an interesting example coming from Alexander Mandre's group where they actually use um monitor modern generative AI models to generate hypothetical counterfactuals to try to understand or diagnose um failure modes in in training Um and so here for example what they found one one interesting example that they found through this diagnosis and then they um actually were able to demonstrate it on real data is that these models are really good at recognizing plates if they have food on them but if it doesn't have food on it it's consistently being categorized as like trays or buckets So you can try to diagnose the failures and that might be so that you can try to then collect training data to be robust to those failures It also can just be useful if you're trying to think about how and in what way you should trust a model in deployment Um but I would argue that probably the best thing you can do in any of these cases is just get training data that's representative of your test domain So in practice a lot of these algorithmic approaches these sort of creative like ways to try to train these models to be robust um to be effective usually they're just significantly worse than just more training data that's representative of your test domain So from like a practical perspective if you really need something to work well you have to invest in the right kind of training data And if that's something that you just cannot get like for various reasons various constraints you know maybe the species is super endangered or you're trying to get a model to work on a planet that we've never landed something on or we're trying to get models to work under the Arctic or in the deep sea where we sort of just are not actually um able to kind of collect large scale training data then you're probably going to have to have a human look at a lot of the data still So this kind of like where do we actually see these things work in practice often it's it's it's really just where we have in distribution training data Um and so this is a really active area of research Um and you know for any of you that are working on trying to bring deep learning to your own scientific challenges or problems I would totally recommend that you sort of look into this literature when you think about how do I get the model to work where I want it to work But I also think that if you have limited time and effort sometimes it's just worth it to label more data Um at least practically that's what I've found Cool Um so in summary uh out of distribution digitalization is a big challenge It's still a big challenge Um but digging into it helps us understand what neural networks learn and it also really helps us try to understand this very tight and complex relationship between the training data and where we can effectively use models Um and then we talked about both adversarial examples and distribution shifts um today There any other questions Yeah Has anyone looked at like activation functions Like do activation functions change how easy or difficult it is to to pull one of these Um so you mean like things like grad cam Oh there's like a stark nonlinearity between like when it's going to when it's not Gotcha Um the question was have people explored um how the choice of activation functions something like a relu or a gu or something how that affects generalization or sort of robustness Um I'd bet but I don't know any literature on it Um I think my intuition says it wouldn't have a huge effect Um but it would be interesting to dig in and try and see And it might have a bigger effect on simpler data sets than more complex ones So there might be papers that show that that has an effect on sort of like emnest style training or cfar style training that that then might not actually generalize Um versus like all of the other different complicated ways that models and architectures affect performance Um but that's totally just my like best guess Um and I'm actually kind of curious I'm going to go look into it Does anyone else Does anyone else know have people looked into robustness and or Yeah Yeah Okay So this you talk about is more like supervised fine tuning So adding a bus finding robust examples but would that be more cost saving to just like reinforcement learning like when you when you add that noise and then add whatever noise happens and then just just label it but just like human feedback would be much more cost saving instead of like this example by example Right Right So the question was um instead of trying to do this type of robust training um to like hypothetical um different um perturbations both to the images or the distributions um does it make sense to use sort of other types of direct feedback um like active sort of human intervention things like human in the loop learning or possibly something like reinforcement learning or like you know DPO might be an example right direct preference optimization um so yeah people totally do that it can be much more efficient if you have a specific target data set in mind This idea of like how do you kind of get to a good result on that data quickly is again like a pretty active area of research and people do lots of different um things to do that effectively So domain adaptation that I mentioned is one fine-tuning if you're talking about supervised fine tuning assuming you have labels already or you can try to be more efficient in collecting the labels to get to that good result on that data Um but I think a lot of the work I talked about today was really with this goal of being generalizably robust like robust to any imagined hypothetical change Um and of course then the limitation is you don't know the way the data is going to change And so if you know exactly the way the data is going to change you have a target in mind then optimizing for that efficiently is totally possible Does that like distinction make sense So it's maybe the difference between um trying to be robust to distribution shifts versus trying to adapt to a specific distribution shift or specialize to a specific distribution shift Yeah Any other questions Cool All right Take the extra time and go vote
