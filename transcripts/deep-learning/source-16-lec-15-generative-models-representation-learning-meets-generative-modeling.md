# Lec 15. Generative Models: Representation Learning Meets Generative Modeling

Source: https://notebooklm.google.com/notebook/1974010f-eec5-485b-b074-000c70149926
Notebook: MIT 6.7960 Deep Learning, Fall 2024
Source order: 16
Extracted: 2026-07-02

## Transcript

we're going to continue on today and this week with generative models so today we'll be on variational autoenccoders and we're going to give a perspective that generative modeling and representation learning are very tightly coupled if you can solve the generative modeling problem it should help you solve the representation learning problem and vice versa and variational auto-enccoders are going to be one model that really puts the two ideas together and solves them both at the same time uh and this lecture is a little bit uh different than some of the last few in that rather than covering a lot of different topics in uh some superficial level of detail we're going to just go deep on VAES on variational autoenccoders and we'll also talk a little bit about the types of representations that are learned by generative models at the end and this idea of disentanglement okay so this is a picture I've shown a few different times uh throughout all of the representation learning and generative modeling lectures in this class is how I like to think about the two problems where representation learning is going from raw data to some um nicer structured abstracted representation and generative modeling is going from a nice simple abstracted representation to um to data and I've said that these two things can be kind of thought of as inverses to some degree uh not necessarily inverses in the sense of a mathematical function inverse because um the direction from data to embeddings is many to one and the direction from embeddings to data might be one to many it might be stochastic so it's more like a stoastic inverse but they kind of operate in opposite directions and in this lecture we're going to see a model that explicitly models both directions simultaneously okay so representation learning mapping data to abstract representations generative modeling mapping abstract representations like a text instruction for example to data so make an image from some text okay so um the perspective that we're going to look at today is going to be generative models as transformations from one probability distribution to another probability distribution so the we'll have a prior distribution which will be the um like the the noise that we talked about last class so the prior distribution is usually something that is very simple in structure and we're going to map that prior distribution to a complicated data distribution okay so we'll always have our latent variables or our noise space Z which is often going to be Gaussian indicated by a cartoon here as a circle because it's something that is meant to be simple if it's actually a normal distribution then in high dimensions that will be roughly like um a soap bubble that's the way to think of it like most of the probability density of a Gaussian in high dimensions is right near the surface of the hypersphere and the data distribution will be something more complicated and so we're going to find a mapping from data that lives in this space and this according to this distribution we're going to geometrically push and pull it around uh which is what the layers of neural net do until we get it into this this shape so remember those um diagrams I showed before of the distribution of input data points going layer by layer and getting remapped into a new distribution this is like a cartoon version of that diagram okay so data is X latent variables are Z and in the representation learning lectures we looked at this direction i think I used the same cartoons so we had uh a mapping f which we called the encoder from data to representations and in the generative modeling direction it will be a mapping from uh representations to data uh okay and we'll call the function that generates the data a generator G so G is a mapping from Z to X and F is a mapping from X to Z okay so these are sometimes called generators other times they're called decoders so we have an encoder that maps data to representation a decoder that maps representation to data okay so we've seen that a few times now let's put these two things together it looks like they're basically solving you know related problems and one model that puts these two things together is of course the autoenccoder maybe you already had kind of noticed that so an autoenccoder maps from data to some vector space lowdimensional representation Z and then it decodes that vector back into the data in such a way that you can reconstruct the data itself okay so the second half of the autoenccoder the decoder it's doing functionally basically what we've described a generator a generative model as doing right so the second half of the of the autoenccoder the decoder is a mapping from some Z vector to data and now the question is can we use that decoder as a generative model as a generator to sample new random photos okay in order to do that we need to be able to first sample a Z vector and then decode it into an image so the first question for you to think about what is the issue with this formulation okay that's better i don't know why that happened uh okay so the we have a mapping from Z to data X and to sample a new random photo from the decoder we would first have to sample a new random Z vector so uh what's the difficulty here how would I do that um anyone can think about what would be difficult about sampling from this uh some distribution over Z yeah let's go here what distribution what distribution like I never told you what Z is yeah in the back I think I saw a Yeah right how how do you know what is the distribution over the encodings of an autoenccoder okay so so that's the problem so typically an autoenccoder will learn a mapping to some distribution some um set of embeddings right but there's no guarantee that that set of embeddings is going to form a simpler shape or an easier to sample from distribution than uh the original data itself so the problem is I can decode um z vectors that are corresponding to the data that I trained on so if I have f ofx where x is a real data point I can decode that by doing g of f ofx uh but I don't know how to sample new valid fs of x right it might be like some weird distribution full of holes and if I go to a point that has never been trained um that's never been observed via f ofx then I won't know how to decode that okay so the whole point of a variational autoenccoder is to fix this problem is to make it so that the latent space the encodings of an autoenccoder form a simple distribution that you can sample from that's that's all of the math and algorithms that we'll develop in in this lecture are going to be simply to do that simply to make it so that this becomes a simple to uh sample from distribution okay so we're going to go through VAE um from a few different perspectives we're going to kind of build up uh the kind of mechan mechanistic intuition of what's going on but we'll also look at the theoretical kind of probabilistic modeling uh way of motivating these things and try to understand at least some of the layers of um analysis that I'm going to provide so it might be that you get lost in some of the math but then always remember to kind of pop back out so the first perspective I want you to understand is that a VAE is an autoenccoder with one change which is that rather than the encoder mapping to a complicated shape it will map to a simple shape it will map to a Gaussian okay so a V is just an autoenccoder that maps uh that where F maps to a Gaussian and that means that I can sample new images by sampling from a Gaussian and running the Gaussian random vectors through a decoder that's the first level of understanding of a VAE okay question is the reason that we want the latian specifically because it makes it better to generate models or there intrinsic good space yes so why do we want the latent space to be a Gaussian so the level of understanding that I want you to have right now is that if the latent space were a Gaussian you could sample from it and then it becomes a generative model sample a Gaussian from Z decode becomes an image now you also asked is it that a Gaussian is a better you know that leads to better representations like if I just want to think of this from a representation learning angle uh is mapping to a Gaussian distribution a good idea and that's also true but it's a little bit more subtle so we'll get to that uh toward the end okay so VAEs are great representation learners and great generative models or they're good at both of those things that's why we're getting to them after we've already covered basic representation learning and basic generative modeling because the VA puts both of them together and it's kind of um it encompasses all the models we've seen and many of the models we've seen like diffusion models can be cast as a certain kind of VAE uh or autoenccoders are like a degenerate VA so VAEs are kind of they're the pinnacle of generative modeling and representation learning okay but in practice they're not as popular these days as diffusion models and other things but mathematically they're a framework that encompasses is almost everything that we've seen okay so what is the probability interpretation of a VAE so now we're going to take a step back and we're going to talk about mixtures of Gaussians because it turns out variational autoenccoders are fitting a mixture of Gaussians to a data distribution so mixture of Gaussians so what is a mixture of Gaussian well we talked about we've you know we've seen Gaussians right so a Gaussian uh fit to data with a max likelihood objective is just going to uh be a density model where we try to increase the density that we apply to where the data lives so there'll be like a gradient that pushes up the density here and the parameters are just the mean and the variance we'll shift around the mean and the variance of this Gaussian until we get maximum likelihood applied to the data points and that's what we learned about last Thursday okay so that's one Gaussian mixture of Gaussians is just well in order to model a complicated distribution that does not have a Gaussian shape I can use a bunch of Gaussian distributions and sum them up okay so a Gaussian is like a little think of it like a little circle I mean it could be an ellipse but most of this lecture will just assume isotropic gaussians which means that the coariance is is one or is the identity in fact uh so we're going to assume that uh just for simplicity that there's no ellipsoids or coariance structure it's just circles It'll um you know you can always turn these into ellipses but uh in the pictures I'll just draw circles so what does it look like to have a mixture of Gaussians that is fit to some distribution like this funny shape that I was coloring in blue on the previous slides just looks like this i'm going to say I can approximate that shape with a bunch of circles right if the circles are small enough and I have enough of them then I can approximate any shape um so probably you could prove I I I haven't thought through this but probably a mixture of Gaussians can you know to arbitrary precision approximate any any density something like that okay so here's what the mathematical model is it's just a mixture so a sum uh with waitings w uh these you know I think in traditional models these would be positive but you can imagine these to be negative or positive more generally and um every component is going to be a Gaussian a little blip of Gaussian probability centered at um a mean mu and coariance sigma okay so the mean of the first mixture component is here and the coariance is uh in this case it's just isotropic two-dimensional Gaussian so uh we're just going to have a co coariance is equal to the variance like it's just one number that characterizes that shape it's the radius of that circle okay so that's the mixture of Gaussians and the parameters of the mixture of Gaussians are the mixing weights W uh the means and the variances or the coariances of all of the Gaussians in the in the mixture and in general in high dimensions the coariance will be you know D byD uh dimensional matrix where D is a dimensionality and the mean will be a D- dimensional vector okay so VAEs are a mixture of Gaussians but they're not just any mixture are actually an infinite mixture of Gaussians so mixture of Gaussians is a classical model the kind of new thing with a VAE is it's an infinite mixture of Gaussians so how could I parameterize an infinite mixture of Gaussians okay so mixture of Gaussians I have uh weight so that's one parameter i have mean that's D parameters and coariance that's d^2 parameters and I have k of those so I have like d ^2 * k plus d * k + d parameters to specify k gaussians so to specify infinite gaussians I need infinite parameters right so clearly that that's not going to work that explicit parameterization of infinite mixture components so there's a really cool trick for parameterizing an infinite mixture with a finite number of parameters and that is to parameterize that infinite mixture as a mapping from some underlying base continuous distribution to mixture components via a function G that has a finite set of parameters and in this course G will be a neural network but in fact a VA doesn't have to have anything to do with neural networks g could be any type of mapping but in this course and in traditional literature VAS are always using um neural nets like you know um but yeah really you could apply this to other models too okay so here's how it works we have PZ this is a unit Gaussian distribution we sample a random vector from that space we pass it through our generator or decoder G and that outputs a number which we will interpret as the mean of one of my infinite mixture components so I'll index the mean with this symbol mu and we'll have the neural network G or the function G actually also output the variance so it'll output a set of numbers d a dimensional vector for the mean and a you know d square dimensional vector for the the coariance or in this example you know two numbers for the mean and maybe one number to characterize the coariance because it's an isotropic gaussian okay so if I sample different points in the zp space I will end up um parameterizing different Gaussians and because Z is continuous I can sample any any point on a continuum and I can there thereby get an infinite number of possible or continuous possible set of um Gaussian mixture components okay and G just has weights and biases it's a neural net okay so that's a finite number of parameters to model an infinite um mixture okay so this has parameters theta and now uh we have you know fit some weird complicated distribution with um an infinite mixture parameterized by a neural net with weights and biases theta theta okay so what is the density implied by this mapping so it's going to be not a sum because we have a continuous base so we have to switch to continuous math and use integrals okay so the probability of some data point X so the distribution uh over X implied by this model is going to be an integral over all of my mixture components each mixture component is just going to be a uh normal distribution Gaussian right mean given by the neural net and and variance given by the neural net that will place a little blip of density over over x and I and my total probability will just be um marginalizing over over z so integrating over z right so here's the finite set of gaussian it's the sum over my mixture components but now I have an infinite set of mixture components and I'm going to um take the integral over all of those components and those that integral is over zspace Okay okay so this is the infinite mixture of Gaussian model these are the weights pz is my prior distribution it's usually just a unit Gaussian and think of that like the weights of the mixture components and then these are all the mixture components and it's an integral okay any questions about that so this is the probability model that underlies VAEs for a Gaussian VAE you can actually have non- Gaussian so you could have other mixtures it doesn't have to be a mixture of Gaussians it could be a mixture of uh lelass distributions or pan distribution whatever you want right so it doesn't have to be gaussians but usually we'll work with gaussians okay is it convolution or is that the wrong way to look at it is it convolution I suppose it could be interpreted as convolution like you're you're convolving over zspace with huh um I yeah I'm not sure that's the most useful way of looking at it but you could you could probably cast as convolution in some sense Yeah okay so I I think it's uh easier to think of it as marginal likelihood marginalizing over some latent variables so that's what we'll see on this on this slide so we're going to understand the z variable the zspace as being latent variables that describe all the attributes of the generated data but in an abstract fashion and all those uh latent variables will um we'll hope that they have a simple distribution uh and they'll be unit gaussians in the um VAE but in the example I gave last class I said that I could write a Python program that will take noise from simple distributions and generate images and these simple uh distributions are my distribution over the latent variables and they have interpretations so uh the graphical model if you've you know if you've done probability with graphical models it would look like this we sample a Z and then condition on the Z we sample an X we only observe the X the latent variables are not observed our data modeling problem is to fit a function fit a distribution over X um that it is uh represented as a mapping from some underlying variable Z but we don't observe Z we have to maybe infer Z from X okay so the generator will be think of that as a conditional distribution the probability over X given um some setting of the latent variables and if I want to if I want to um model the likelihood of a data point X I can integrate over my latent variables Z so this is just marginalization from probability right px given Z * PZ is PX and Z then if I integrate over Z then uh by the rules of probability I get uh px okay so this is called this is the likelihood the probability that the model p theta places on the data is called the likelihood and I'm trying to do max likelihood learning so I want to find the probability model P theta that maximizes the likelihood of the data that assigns the most density to the data and to comput the likelihood I have to marginalize over my unobserved latent variables so sometimes we call this marginal likelihood so a latent variable model I'm going to fit it to data by maximizing the marginal likelihood that it assigns to data when I marginalized over all the latent variables okay because for a given data point there might you know to make it high probability uh there might be a lot of different settings of the latent variable that can give it um some plausible like high probability okay okay so here is the entire um like objective function and hypothesis space of a variational autoenccoder or uh in this case just an infinite Gaussian mixture model and the variational autoenccoder can be um is one one type of approach that that uses this model so the objective function is going to be to do maximum likelihood so I'm going to maximize the likelihood that my model places on the data and the hypothesis space is going to be an infinite mixture of Gaussians i'm going to try to find the you know means and the variances and the weights of Gaussians in an infinite mixture and to parameterize an infinite mixture I'm going to parameterize uh a mapping from some underlying prior space Z to the U data space X uh via the math that we just looked at on the last few slides okay so right this is marginal likelihood marginalizing over the latent variables Z and the likelihood term um for PX given Z is going to be Gaussian and the prior term for PZ is going to be unit Gaussian mapping from unit Gaussian to complicated data distribution in that fashion and this model is going to give me two things it's going to give me both a density that's that's the main thing right the uh I'm trying to learn the parameters theta that will define this density function uh that maps from x to um non- negative numbers and it will actually also give me a very easy way of sampling from that density so it gives me those two things so in order to sample from u this density I can do the following i first sample a random unit Gaussian so that's sampling my Z vector right and then I decode it through G okay so in order to create a sample X I first sample a a random unit Gaussian Z um or in fact this sampler is written here is just going to be the mapping p of x uh given z so it's going to say given some z uh what how do I draw a sample over the possible um x's that uh follow the density px given z so I will give my take my Z and I will have my neural network predict what is the the mean of the mixer component and then I will um transform I will take a sample with some uh variance by multiplying my variance by um a unit Gaussian so this is just a way to sample from a Gaussian whose mean is g of mu and whose variance is g of sigma by transforming a sample from a unit gaussian okay so x will be distributed as normal distribution whose mean is g of mu and whose variance is g of sigma okay so the difficulty here um let's see sorry the variance is g is is sigma squared I I suppose sigma is the standard deviation not the variance I always mix up those two okay so um yeah the the variance did I get this right is is stand variance is square of standard deviation okay yeah so um so the difficulty here is is this term right here so if I want to maximize the likelihood that my model assigns to the data well the likelihood is written as an integral so if I were to try to solve this with back propagation or something like that that means that every time I'm going to perturb my parameters to place more probability more likelihood on the data I would have to compute this integral and this would be incredibly expensive so we don't even have a closed form for that integral so we would compute that integral by approximating it with the set of samples by approximating the integral somehow to get a good approximation to an integral i need to maybe take a lot of samples over the over Z right and this means that every time I'm going to um run my model forward see what density it places on my data I actually have to run it forward a lot a lot of times because look at the neural network G appears inside the integral so to to get a good approximation to the likelihood my model places on the data I have to run my my neural network forward as many times as necessary to approximate this integral which might be a lot of times so one step of backrop is like running my neural network forward you know many many many times to get a good estimate of this neur uh this integral and then I still have to backrop that okay so this integral is difficult to calculate it's an intractable integral so what what am I going to do oh and just um notation wise there's something that you'll might encounter in the VA literature which is called the reparameterization trick which just refers to this in order to take a sample from a um from this distribution px given Z which is a normal distribution it suffices to take a sample from a unit Gaussian and transform it by offsetting it by the mean and then multiplying it by the variance okay so then X is just a deterministic function of G with the only stoasticity coming in through this um this unit Gaussian noise inserted in the side it's just a little trick just to clarify so you you say variance but there's only sigma not sigma square is it standard deviation yeah sorry I I um I got mixed up in in the terminology so sigma is the standard deviation sigma squar is the variance and yeah apologies for mixing up those terms so is this it should be standard deviation or is this a typo and it should be like sigma square no I believe that it it should be multiply by the standard deviation yeah I think this is not a typo it's just I misspoke y Okay anybody can confirm that that that's the proper way to transform a a unit Gaussian into a Gaussian with some some variance okay good so you multiply by the standard deviation square root of the variance okay okay so that's the um that's the problem that we're trying to solve and the rest of the VAE trickery is going to be how do you calculate this integral in an efficient manner so you could pause here and you could say you understand what VAEEs are doing they're modeling the they're maximizing the data likelihood by fitting an infinite mixture of Gaussians to the data and doing optimization to increase the probability we place on X with respect to the parameters theta which are the weights and biases of the neural network G that's one layer of understanding but then there's this integral so the rest of the lecture is actually how do we approximate that integral okay okay so I'm going to use the notation L to um represent the objective function which is the data log likelihood so L is is equal to this this equation down here so that equation is the um is the marginal likelihood so it's going to be the probability of X given Z * Z integrated over Z is PX log of PX is the log likelihood i'm trying to maximize the log likelihood my model places on the data and um this outer sum over n data points is going to be well my training set I'm going to be fitting my model to a training set of n observations so maybe I have n photographs and I want to learn a generative model of them so I since these are uh this is log likelihood when I have multiple different um data points I maximize the joint probability by maximizing the sum over the log probabilities right okay so this is this is the objective function for maximizing likelihood using the mixture of Gaussians okay so I'm now going to introduce um a series of tricks i think around three tricks for approximating this integral so the first trick is going to be the most vanilla trick that we use all the time which is to approximate an integral i can use some sample i can use sampling in some form and the nice thing about this integral is it's actually in the form of an expectation okay so this marginal likelihood marginalizing over the latent variables is uh equal to the expectation of how much likelihood probability at density I place on X for each Z uh in expectation over random samples from the prior PZ okay so right that that's just that's the definition of expectation and it's an integral uh with uh with uh if I sample from this distribution it goes outside the expectation this is just a definition okay so how do we approximate expectations you can approximate expectations by simply taking a finite set of samples and averaging them that's the trick that comes up all the time in math right if I have an expectation I approximate it with a Monte Carlo estimate which is a set of samples and then I take the average as opposed to having to compute the entire integral okay as M goes to infinity this becomes um this is an unbiased estimator of that of that integral so as M goes to infinity then I get a perfect estimate okay so how do I do that i sample from my prior which is unit Gaussian that's easy we can um sample unit Gaussian vectors and then I uh sum up and take the average of uh the density that the model places over X for each Z and remember that PX given Z is a very simple distribution that's just a Gaussian distribution right that was that was here so PX given Z is just a Gaussian distribution parameterized by my neural net output mean and variance okay so this is trick number one and now that we have done that trick we actually can train this I can actually write pietorch and do this because I got rid of the integral I don't know how to write integrals in pytorch right but now I got it into a form where we can write it in pietorch is everything is just uh countable discrete uh math okay so I approximated the integral with an average over average over m samples from my prior and I looked at how much probability each of those samples places on my data I um I added all up And then I will it's a log probability so right I'm trying to um estimate log of p theta x okay so the so log of this is p theta x this is my approximation to p theta x um and then I will uh be my total probability over all my training data points is um a sum over the n end data points and I'm going to try to find the parameters that maximize the probability I place over all of my data points okay um ah I see I think I no oh yeah this is fine okay good yeah no I think actually I think there's a typo here um so the that one over m should should be inside the log I suppose yeah that's that's probably an important typo okay so log one over m so uh there's a lot of equations in this lecture so be sure to try to like go through and think about them yourself and there might be there might be typos so exercise for you to solve okay so what does that look like so here is actually um a collab that's running this optimization to uh fit a mixture of Gaussians to this data distribution on the right and rather than showing random samples so rather than randomly sampling from the prior PZ I'm sampling along a grid of points just for visualization okay so we can say for this Z that maps to a Gaussian at this location now at initialization which is the frame that you're currently seeing I've initialized the neural network so the weights are near zero and so the Gaussians all kind of all the mixture components are mapping to around zero okay so here's what it looks like when you train this system okay so the different points in Zpace learn to map to different parts of the the data distribution so they cover the data distribution they maximize its likelihood okay so that's what it looks like this is SGD on the on the equation above okay yeah question c why don't we multiply by it yeah great question so that's just because of how expectation works so um expectation is defined as this integral and uh the probability Z comes in through the sampling procedure so higher probability um samples according to Z will be over represented in this sum so you don't need to double weight them yeah that's why um I was just a little confused on how are you computing P theta of X given Z you said it was just I mean like on the previous slide right it's like a normal distribution with the mean and var or standard deviation or variance provided by the forward pass through your model so then do you just draw a sample from the normal distribution defined by the parameters that your neural net you don't so you don't have to draw a sample because this thing has a closed form analytical form right so what is what is n of um x with mu and and sigma as your or sigma squ let's say as your mean and your variance this is equal to some normalization times e to the like x - mu over 2 sigma squar right that's just the analytical form of the density yeah so this thing here it's just a shorthand for some exponential of the difference from the mean you know divided by the variance in the approximation step how does the error scale in the approximation step how does the error scale with M i don't know um that that's a great question so um I think that it's going to depend on yeah the data and the parametric form of P and all these different things uh but that I would just say that's like a a good question to like do some analysis on and I'm sure people have like in general how do Monte Carlo estimators scale with respect to the um number of samples and and there's probably some classical results I don't remember yeah yeah okay yeah okay so that's all all well and good um so actually I'm going to ask a question kind of related to that like how many samples do you need to get a good approximation um so I'll come to your question in a second so how many samples do you need to get a good approximation so this worked i've it you know do we need any more tricks is this enough so here I have I took you know some number of samples let's say M is like 100 i don't remember the exact number well what is what's going to happen as I scale my latent dimensionality and I I'm going to need a highdimensional latent space in order to be an effective model of a high dimensional data space so for two-dimensional distributions like showing here it turns out that I can just sample like a hundred of these samples m and get a good fit to my dens my my data distribution but for highdimensional data I need a high dimensional latent space or a higher dimensional latent space so let's say that my Z vectors are 100dimensional do you think a 100 samples M are going to be a good approximation to the integral over this 100dimensional space what do you think thumbs up if you think it will be okay okay no thumbs down if you think it won't be okay yeah thumbs down okay so why how many samples roughly would you say you need to have a good approximation to an integral over an D-dimensional space this kind of gets at your question I think uh yeah in the back responding to both his question and your question Monte Carlo estimator has variance 1 m so it goes down 1 over<unk> of m excellent okay thank you Monte Carlo estimator has variance 1 over m nominally non-dimensional but in practice somewhat dimensional nominally not dimensional so so actually there's no dependence on the dimensionality d I guess but in practice there is okay um so I'll say My my rough intuition would be that you need like exponential samples like in order to cover a d-dimensional space um you know you know in each dimension is going to be you're going to have to fill it up with another fixed set of of samples so you should have something like exponentially more samples to cover a dimensional space but um yeah formally I'm not sure what so how actually how does that work i'm I'm I'm interested yeah central limit theorem yeah okay um Okay well i think exponential in dimension is definitely correct i think it's exponential in dimension or there so there should be some term in terms of um the number of samples you need to get some quality of fit should rely depend on the dimension I think it's exponential in dimension but there's probably more to discuss maybe we can take that offline uh but I'll just say that in practice this doesn't work in high dimensions and I think it's because you need exponentially more samples to get a good approximation to the integral in high dimensions but we we should talk about that more offline to see if that's really true okay so the next trick of the VAE is we're not going to just sample randomly we're going to sample intelligently we're not going to just do naive Monte Carlo uh approximation to the integral we're going to do a much more intelligent approximation to the integral and that one is going to be called important sampling okay so what is the idea of important sampling the idea is that let's say that I want to estimate the likelihood that my model places on some data point X that little X up there okay what I'm going to do with important sampling is say I could say that that's the expectation of px given z um with respect taking expectation over z and I could randomly sample the z's to to calculate that expectation um but what will happen if I randomly sample the z's then almost every single um px given z for most of the z's will place nearly zero probability on that data point x right most of these mixture components don't lie near the data point x okay uh just in practice this is what might happen if I've spread out these mixture components to be a good fit to the data density okay so the observation is that with random sampling maybe I have to take like around seven samples to get one Gaussian component that's your x and almost all the other components are adding zero roughly zero so it's like this p of x is equal to the average of you know px given one sample plus px given the other next sample plus px given the next sample but most of these px given a sample are zero so almost all the terms are nearly zero except for the sample that except for the uh the z's that map to gaussians that are that place high density over this this x so in this expectation most of the terms are going to be zero for any given point x so we can exploit that by just trying to say let me only take the sample z2 and that will be a good approximation to this expectation and this is called this is the idea behind important sampling so in order to approximate this expectation which is that integral I can simply multiply by q uh by another uh density q and I can multiply by qz divided by qz so qz divided by qz will be one as long as qz is non zero everywhere which is just a technical condition we need to make sure it has like support everywhere okay so then I can uh that's valid right so I'm just multiplying by one but the interesting thing here is now this appears as an expectation with respect to Z sampled from QZ as opposed to Z sampled from PZ and if qz is a um distribution which has placed high density only on the z's that actually contribute to the sum that actually have high probability of the data under that z high px given z then this will be an expectation which requires fewer samples to get a uh a good estimate of the true integral okay so so this is called important sampling i'm sampling from a distribution that kind of gives more importance to the uh the points that matter okay so I won't prove this but I'll just assert that the um the optimal uh distribution to sample from for performing this estimation in a certain sense I think the distribution that um like minimizes the the variance over my estimator of this expectation is um to sample z's from exactly the density pz given x okay so what does that look like so it's like uh if if I knew PZ given X it's like here here's X so given X these are the Z's let's say that place high probability on X it's a distribution it might be a complicated distribution but uh most of the density like this is the high density region of PZ given X is is is in a small region and if I could simply sample from those points it would suffice to get a good estimate of what would happen if I had sampled from everywhere because most other points will place zero density and they just won't contribute to that that integral or roughly zero density okay okay so important sampling is like now I just sample once from this distribution and I get a as good an estimate as having sampled seven times with random sampling okay so here's the entire transformation i'm going to estimate the likelihood that my model places on the data via this expectation but I'm going to switch it to this other expectation where these these two these two expectations are exactly equal to each other right that's because these two integrals are exactly equal to each other if Q has non-zero support everywhere uh but this one can be approximated with a Monte Carlo set of um a Monte Carlo approximation to this expectation is going to be uh a better estimate lower variance than this expectation okay so here's important sampling random sampling I take random samples from from Z from PZ and most of them don't contribute to the sum important sampling i only sample from the points that contribute to the sum i only sample from the points that the model uh thinks will you know place high probability on on X so the probability of Z given X is the points that the model thinks are the Z's that will place high probability on X okay great so if I knew PZ given X then this would be what I want to do i just sample from PZ given X and I get a better approximation to that integral and um this will then work in high dimensions because sure high dimensions the volume is exponential in D but the area of um zspace that actually maps to any given point X might be very small under a particular model P okay but the problem is I don't know PZ given X that might be another complicated distribution and I now have to come up with a way of modeling that distribution so here's how you do it the next step in the VA the trick the third trick is you're going to try to model or predict the optimal sampling distribution the sampling distribution PZ given X given given X you're going to train a model that will take X's input and output a density over Z and that will be a model of PZ given tax okay so now we get to where it starts to look like an autoenccoder it probably didn't look at all like an autoenccoder until now but what does that look like that's an encoder that's a mapping from X to a distribution over zpace so our model of um our model which we'll call Q which is trying to approximate P P theta Z given X so we're going to try to approximate that with another prediction called Q that density is going to be just for simplicity modeled also as a Gaussian so we're going to take our X we're going to try to predict what part of zpace would P place high probability u on X so for what part of Zpace is P px given Z high probability we're going to predict that by modeling um a distribution over zspace okay and we're gonna make that a Gaussian it will be we'll take our a neural network f it will output a mean and a variance or maybe a co-variance if we wish and uh that will be our model for uh pz x and that model is called q and q has parameters s which is a new set of weights and biases of this encoder function okay okay so now we're going to get to the full picture so we have we have these two pieces we have the decoder function which is this probability model probability of X given Z and we have this encoder function probability of Z given X we're going to put them together now so uh how am I going to um actually write down the objective function for the encoder so the objective we're going to say we want it we want the encoder to model a distribution pz given x and uh but it's going to model that distribution with another distribution parameterized uh by mean and variance that defines a gaussian and that's going to be qz given x and we're going to try to make those two distributions the same so we're going to use the kale divergence to try to minimize the difference between those two distributions just a particular uh divergence measure of how different are two distributions is we want to make them the same okay so this is our objective for the the problem of the encoder the problem of the encoder is to minimize uh jq actually I think I have a negative sign so it's going to be to maximize jQ okay so I'm just going to rewrite the kale divergence uh there's a little bit of kind of algebra that you'll probably want to look at in more detail on your own on your own time but this is one way of writing out the def this the definition of kale divergence okay it's going to be um yeah this equation it's going to be an expectation sampling from um the density that qz given x uh all of these terms okay so um the learning problem for Q is to maximize uh to minimize the K divergence or maximize the negative KL divergence okay so this is the learning problem for Q is trying to predict the distribution over latent variables that will explain a given data point X we call that inference sometimes infer the latent variables that explain the data X okay uh I can take this equation and I can notice that if I'm maximizing with respect to the parameters of Q well the parameters of Q don't appear in this term so I'm going to just remove that term they don't affect the um the maximization problem they don't affect the arg over over the parameters s the parameters of the encoder okay so now uh and I'll we'll use this term a little bit later but this equation here will be J we'll come back in a minute okay so for our learning problem for Q is to optimize this and this is an expectation so I could optimize that again by just taking a um Monte Carlo estimate to the expectation by sampling a bunch of different Z's and uh taking the average over uh all of the values within the expectation okay so that's the learning problem for Q for Q which is parameterized by a neural network F map from X to an inferred distribution of related variables that can explain X okay the learning problem for P this was our original learning problem we wanted to learn a generative model G parameterized by a neural net G where G places Gaussian probability over the data space X and G uh defines an infinite mixture of Gaussians by mapping from by taking an integral over all possible points in zpace okay okay so for a learning problem for P which I'm going to call JP it's to find the parameters theta that maximize the likelihood that my model places on the data and that and with important sampling um that means I'm going to sample from my uh important sampling distribution Q which we learned the optimal important sampling distribution Q up here we know that it should be PZ given X but we're just going to try to learn PZ given X by fitting a distribution Q to PZ given X and this is then my important sampling for estimating the integral that defines the likelihood of the data under the parameters theta of my modeled neural net G okay so I know there's a lot of terms and in math going on um this might take you time to work through step by step all of you know this derivation is in the reading for today so this these these slides follow that almost exactly and additionally the problems that you'll get to work through some of this yourself so you know hopefully you'll enjoy that um but I know this is a little more mathematically uh notationheavy than some of the other lectures okay so now putting everything together there's one little final step which is it turns out that um if we approximate log of expectation by taking sample like a Monte Carlo estimate by taking samples um from the thing that we're taking the expectation over if we do a Monte Carlo estimate of this and then we take log of that it turns out that's a biased estimator of log of expectation so Monte Carlo estimate of log of expectation is a biased estimator and we don't really want that we want it to be the case that as samples go to infinity we get the true answer and that won't be the case here so we're going to do this little trick which is that we're going to pull the log inside the expectation so that then we just have expectation of some function and then a Monte Carlo estimate of that expectation is an unbiased estimator of that expectation okay so we're allowed to do that by Jensen's inequality that says log of e of um some function is equal is greater than or equal to expectation of log of that function so this is just an identity again from prob you know a lot of you will have seen Jensen's inequality it comes up a lot when you're working with probabilities and now what we have is that um this is going to be a lower bound to our objective J so this is a lower bound i can rewrite it so I take the log I you know move around the terms a little bit this is just rewriting some algebra and this thing here um is called the evidence lower bound okay it's called the elbow so you'll come across this term if you're working in in VAE or other types of what we call variational inference and it's a lower bound to the thing we actually care about which is the likelihood of the data under the model uh and it's a lower bound due to Jensen's inequality on this line uh and this is something that is now um an we can approximate this expectation as an or this expectation as um with samples and it'll be an unbiased estimator of that value so we're going to have an unbiased estimator of the lower bound we can maximize the lower bound on the the likelihood the model applies to the data and this will learn a model that places high likelihood on the data maximizing a lower bound will um increase my objective J okay so evidence like this is the evidence um that the uh the model provides for the data anyway yeah don't worry about the name too much but uh it's called the evidence lower bound is just terminology elbow okay so it comes in two terms that both have uh kind of an intuitive form uh and I'll get to those on the next slide but the interesting thing is once we have written things in this form what you can notice if you go back a slide is that this objective is actually exactly equal to the objective that we had for Q okay so so Q appears over here in this KL term so what is this saying this is saying that in order to learn the parameters of the encoder that give me the best important sample uh importance sampling distribution uh for estimating the uh likelihood the decoder places on the data uh it actually uh works out that I can just um maximize this evidence lower bound with respect to the encoder and decoder parameters jointly okay so the VAE objective now um can be written in this form as maximizing this lower bound to the thing we actually care about and I can approximate that with sampling it will be an unbiased estimator of that um of all of these expectations and integrals it has two terms it has the first term which is how much probability does the model place on the data and another term which is well I was estimating how much probability the model places on the data with respect to the wrong not actually taking samples overall all of Zpace but with taking samples with respect to this other distribution so I have to somehow account for that so I'll penalize the difference between this other distribution and my prior the unit Gaussian PZ okay so this this term is coming out because I had to multiply by this ratio how much pro you know how much different are my samples under PZ from my samples under QZ okay so that pops out into this this uh this term here so now the encoder is trying to make these things as similar as possible and the decoder is trying to uh just learn parameters theta that will maximize the probability um placed on the data okay so here here's the picture um I'm going to start with a data point X i will encode it that will define a distribution over my latent space i'll take a sample from that distribution that's one important sample for estimating the probability my model places on the data and then I'll decode that and see what probability the model did place on the data that's like one important sampling step of um approximating the lower bound to the probability that my model places on the data okay so now we come back to autoenccoder a lot of math but at the end of the day the VA is a very simple model that looks just like an autoenccoder so here's how it looks like an autoenccoder we start with data point X we encode it we get mean mu Z and variance sigma squar Z we then take a sample from the distribution uh QZ given X so that taking a sample that means we're just going to add noise okay so remember that mu Z plus unit Gaussian epsilon time uh standard deviation uh for the encoded point X so sigma Z is going to be the standard deviation of the encoded point X this is one sample from a normal distribution centered at muzz with variance sigma z^ squ okay I think I'm getting the squared versus not squared term correct now um so that is one sample for important sampling for estimating um the density that my model will place on the data so then I will decode that to observe under a normal distribution what is the probability that my model placed on that data for that mixture component which is a good approximation to the entire integral okay so remember the normal distribution is just analytical form so I can calculate how much probability um G placed on the data point X okay so this looks just like an autoenccoder except one step which is this noise I added in the latent space right otherwise it's just encode decode but I added a little bit a little bit of noise to the latent space because that's my sampling that's that's necessary because I'm I'm I'm sampling to approximate this uh expectation okay so here's the two terms of the elbow that I talked about before uh the first term it looks like a reconstruction error okay why because p theta x given z is going to be um a normal distribution over x uh centered at a mean which is the decoded g theta of z okay so a normal distribution over x is just going to be a log normal distribution will be just the squared error right if I take log of e to the^ square distance it's just negative square distance so how different is my decoded point from my input observation x that's this likelihood term and these other terms over here are just because the Gaussian distribution has to be normalized and the variance appears in the normalization and dividing by two sigma squar is just also part of the definition of the Gaussian probability okay so just for simplicity imagine that we're going to model we're going to use a VAE where we won't actually model the covariance or even the variance of the Gaussians this is a valid modeling choice i could just fix my circles to have radius one i can say I'm going to try to fit an infinite mixture of Gaussians where I don't get to change the variance of the circles but I'm still going to try to find the max likelihood fit that's a perfectly valid modeling choice and if I did that then sigma becomes one and this these all these terms all become constant and then you can see that really all I would be doing is trying to minimize the difference between my um data point X and encode decode of the data point X okay so then it would be exactly the autoenccoder objective of minimizing the mean squared error between my reconstructions and my input observation okay so VAEs often will also model the variance of this Gaussian but vanilla autoenccoders don't really model the variance of the Gaussian just kind of implicitly set it to be constant okay okay the other term the KL is saying um well I need my um my QZ given X distribution i want it to be close to a unit Gaussian so we say that you know the the posterior of the the inference direction should be near the prior PZ okay so what is this this term also has a very simple interpretation so here's what it looks like this is the KL this is the KL divergence between two Gaussians so luckily the KL divergence between two Gaussians has a closed form an analytical form and it it looks like that so remember the this is the kale divergence between the Gaussian defined by qz given x and the uh prior Gaussian which is a unit Gaussian so this is the kale divergence with respect to a between a Gaussian that has mean meu and variance sigma squar and a unit gaussian that has mean zero and variance one okay it has this form in the readings we posted the original VAE paper and in the appendix there's a nice derivation of that form okay so what does this look like well let's again imagine that we're not going to model the the the variance we're just going to set the variance to be one constant we're only going to get to uh update our neural net to improve the density by changing the means that it that it that places over the data okay so these terms uh would then can be ignored they go away this is just a constant so what is that that's going to be I want to um minimize the means so basically if all these terms are constant you know that one half is just a scaling in order to uh increase the evidence lower bound in order to place higher probability on the data increase the lower bound to the likelihood I place on the data I'm going to try to decrease the means so all of this term is saying is squash the means toward the origin so I'm going to try to reconstruct my data that's the first term that's the data reconstruction term that's an autoenccoder and I have one extra term which is just do that in a way where all of the encodings are near the origin so what does that look like that means that this funny shape that I could have gotten this is an autoenccoder i just encode I decode I minimize reconstruction I might get an arbitrarily complicated latent space in fact here if my latent space is two dimensional on this cartoon I could just have F be the identity and then my latent space has exactly the same shape as my my input data space right if G and F are both the identity it's just like I encode to the same shape I decode back to the same shape that would you know satisfy the reconstruction error but it but it wouldn't be a very good um representation of the data it would just be the raw data itself once again so the variational autoenccoder simply says no you got to squish that latent space toward the origin okay so this means that in a normal autoenccoder I need to make sure the dimensionality of the latent space is low so that I'm doing compression and forcing it to do something non-trivial in a variational autoenccoder it helps to have a low dimensional latent space but it's less critical because there's this other term this KL term which is trying to compress the distribution toward zero compress all the means toward zero and uh equivalently we can understand that to be I'm trying to make the distribution of encodings Z have the shape of a unit Gaussian but when I'm not modeling the variance that just means squishing the means towards zero okay so I just squish everything down why does it not collapse to a single point so what do you think what if I if this if I'm trying to squish the means towards zero why don't they just all collapse to zero what do you think what prevents that yeah I heard heard it i think I heard an answer no push everything to zero if you push everything to zero that's trivial it's trivial and what will happen to the the reconstruction you cannot you cannot yeah so that's the tension we're going to try to push everything to to zero that's that's this term if I'm not modeling the variance but I also need to satisfy this term which says that you better be able to reconstruct your input point and if everything goes to zero then there's ambiguity so in order to reconstruct the input I need to I need to preserve information i can't compress too much i can't squish everything down too much so you want to spread out in order to be able to reconstruct the data and that's the fundamental tension of the variational autoenccoder pushes everything toward the zero creates this compact shape it's like I have some like play-doh and I'm trying to squish it down but there's also this physical force which is preventing it from going to like a black hole right it's like it's like no no I don't want to do that because I need to I need to still be able to know where I came from to predict you know to reconstruct the data it's it's like that i'm pressing it down but there's some tension eventually it gets to this unit shape um and that's that's what the math works out to so here's what that looks like so now here I'm showing the encoder mapping at initialization the neural net is initialized with um weights and biases that are near zero so everything maps to to zero and then the decoder so initially all of my mixture components are near zero okay and here's what it looks like to train so what you'll notice is I'm giving a I'm getting a good coverage over the data distribution okay that's my infinite mixture of Gaussians but I'm visualizing it with a finite set of samples along this grid and what is the encoder doing it it's like I want to spread out these um these Gaussians so so that if I go from X to Z and reconstruct I'll know where I came from I'll have enough information but I I also want to be near the unit unit sphere unit Gaussian so this tension will kind of make it pop out and then like kind of squish back together and you can think of this almost like some sphere packing problem there's probably some interesting connection to to like a geometric way of understanding this that if you have a bunch of spheres and you want to fit them into you want to make them not overlap if they overlap what's the problem that means there's ambiguity if two different data points map to the same point or overlapping Gaussians then there's ambiguity you don't know which one it came from and you can't reconstruct so it's like you don't want them to overlap too much but you wanted them to squish down at the same time and there's this tension and then the solution is that they'll kind of spread out to evenly tile the the unit Gaussian okay okay so here's what the densities look like if I plot them as a heat map so remember these are the Gaussian components but actually they're continuous they're little blips of you know Gaussian probability so here's what it looks like as a heat map it really did learn a good density model and here's the density um over the latent space it becomes a kind of a unit Gaussian in two dimensions that looks like just right like a little blip of bell-shaped probability okay so VAE is really just three tricks approximate an infinite mixture with samples sample efficiently by not just sampling at random but with importance samples so approximate with important samples and predict the optimal important samples to take optimal important sampling distribution with another neural network and then jointly optimize the whole thing and it looks like an autoenccoder okay so um in case some of that was a little bit too abstract I just wanted to give you the step by step like what would you write in PyTorch to make this happen it's actually very simple so to optimize a VA I'll sample a data point X from my training set or maybe a batch of data points i'll encode the data with a forward pass through my encoder for each data point that's been encoded I'll add some noise to my encoding okay that's my important sample then I'll decode the data by passing it through the decoder and I'll compute the losses which will just be the squared error and the KL term and then that will give me a scalar loss value at the end of the day and I'll just back prop through that computation graph and typically for the VE even though I would really want to take a lot of important samples to get a better estimate usually people just take one they do forward one sample decode backward and that seems to be sufficient how do you back prop through sampling so that's why that reparameter how you back prop sampling that's why that rep parameterization trick was important because the stochastic element comes in through the side of the graph and you don't actually have to backrop through that sampling procedure you're just backcropping through the mean and the variance of a distribution and the actual noise is coming through the side and doesn't affect the optimization a question in the back in practice is it important to get the variance inside the latent like sigma from the neural network or is it does it also just kind of the same because it would be natural to think like oh you should do an autoenccoder and you should be smoother but maybe it doesn't matter for different great yeah good question the question is um for the encoder is it important that the um the noise you're adding actually is dependent on the the variance predicted by the neural net so could so you're kind of asking if for qz given x which you're modeling would it suffice to just uh model the mean and set the the variance to be one or some constant and I think indeed um often times you can get away with those types of things like you can just take an autoenccoder add a little noise to the latent space and do nothing else and it might kind of be fine or maybe you add this kale penalty to like say take an autoenccoder penalize the means for being far from the origin and add a little case and it'll kind of give you a VAE and it might be fine uh but you know your mileage will vary and you can always also not model these things with Gaussians but more complicated distributions so there's a lot of variations on on this but the simplest vanilla version might be good enough and I think it captures the full idea like the simplest vanilla version is just smooth out the latent space and make it dense by penalizing the encodings from being far from the origin and smooth it out by adding some noise so that like um it has finite information capacity it can't kind of overfit or have too complicated a a decoding mapping okay anyway more to say about that yeah question yeah so right so why why is pushing the latent variable distribution to zero better so it comes out as that uh is what KL divergence between a unit Gaussian and another Gaussian will do um but I think more intuitively it's just it's this kind of sphere packing idea that I um I want it to be the case that every single point in zpace if I sample from that point it will lead to a valid image or a valid decoded data point X and if I don't pack things in if I don't squish if I don't squish all the means towards zero then there might be gaps right So like here there might be these gaps at the end of the optimization there's a little gap here that might I mean this is only a finite set of samples but maybe there's no uh these data points here like they can't be reconstructed at all or equivalently on this direction if there's gaps here if like if these means all went out to infinity so it's like oh I have high capacity I can kind of memorize everything they're going to go out to infinity so they'll all be infinitely far apart and then I can uh you know more easily decode where they came from there's no overlap at all it's like perfectly information preserving it's invertible well that wouldn't be good because there'd be all these gaps where if I now take a sample from my Zspace I might end up in a gap this never so the decoder has never seen that sample and it will not know what data point to reconstruct so you want there to be no gap so that any point you sample in that zspace will uh map to a valid um data point X uh according to your distribution of training points X okay that's the intuition I have okay so in the last um five or 10 minutes I want to talk about what kinds of representations are learned by a VAE and other generative models and um so the so we're going to go back to this example here and we're going to ask are the latent variables Z that the VAE learns actually related to what I gave as an intuition and I said I could write a Python program that takes grass a normal distribution maps it to grass color a boui distribution maps it to the turns of the river well is that what actually happens uh when I train a model without kind of hand defining it okay so I'll take my data I'll learn my encoder and my decoder my decoder can generate samples and this is a good generative model of my data but now let's look at the encoder is the encoder separately useful is a VE a good representation learner okay so so we'll visualize it like this we're going to take our our latent space and we're going to move in axis line directions in the latent space and see what images those map to okay so if I move along one axis of latent space it ends up causing this kind of smooth variation in the output space where the curvature of the river changes if I move along another dimension of latent space it ends up changing the color of the grass oh sorry okay so indeed my learned latent distribution ends up having different dimensions in latent space controlling different types of visual variation on this data and those types of visual variation actually do relate to the underlying um parameters of that that define causally how the data was generated like there is a causal process a Python program I wrote that takes these attributes and turns them into rivers and grass and so forth and one of those attributes is the grass color uh is a normal distribution so there's one dimension of the latent space that generated the data that should control grass color and the VA actually recovers that so it kind of identifies that true factor of uh that true causal factor in the data okay but this is not always going to be the case the other factor of variation that I'm showing you here which is the curvature of the river actually does not correspond to any of the uh underlying variables in my Python code because the Python code took a vector of Bernoli coin flips to determine the shape of the river and there was no single variable no like Gaussian variable that that defined the kind of overall shape of the river it was just a sequence of Berni steps but somehow the VAE learned this kind of abstracted like intuitive uh physics of the world didn't actually learn the physics of the world that I wrote in Python to generate this data it came up with this abstracted abstraction which is that there should be a factor of variation in this data that controls uh the curvature of the river that's a good explanation so a VAE um like other representation learners can learn um different independent factors of variation that affect the like primary uh independent you can learn different latent variables different dimensions of latent space that control different independent factors of variation we call that disentanglement that if I vary the river curvature dimension it does not affect the color and if I vary the color it does not affect the river curvature dimension this is a disentangled uh representation where there's independent independence between the effects of those two factors uh but those two factors don't necessarily map on to actually uh the causal mechanisms underlying how this world was defined they might be more abstracted from it okay so um VAEs are not the only models that have this property of learning organized latent spaces that are good maps or good representations of the world uh in fact this is an example of a GAN and it does uh has just the same types of properties so we covered GANs very briefly last lecture and um we it's not GANs didn't have any encoder that maps from data to to latence so VAS do have an encoder they can go from data to the representation of the data in this disentangled space the GAN doesn't have that but nonetheless the latent variables if you sample random latent variables or sample along a grid of latent variables they do show that they actually model structure in a similar way to the VAE and here's just an example from a a model called big GAN where there's one direction in latent space that controls the orientation of the bird and one direction that controls the background color and you'll see this over and over again in um all the types of generative models that we um have talked out there are um there are some kind of latent variables usually like in diffusion models maybe the noise um or there are other intermediate variables like the activations of the network as it processes as it as a decoder processes the data uh the activations on some layer i can look at directions in that activation space and these tend to um learn you know structured representations of the data that that is the mechanism by which the model can fit to the world effectively it has to learn some kind of you know factors that can parsimoniously explain the data if it only has so much capacity so many dimensions this is it kind of somehow uh pops out that this is a good representation of the world that allows you to generate photos that maximize likelihood okay so generative modeling uh forces you to explain the world explaining the world uh is done well by actually capturing uh independent components and uh things that we have physical names for like background color but sometimes things that are more abstracted like low frequency curvature of the of the river um yeah this one maybe just a kind of fun visualization to end on this is again that same um map and here I'm coloring the input points according to their coordinates within this space and here's what happens as I train it so what you can kind of see is that this thing gets the the you know the points in this space get remapped to the sphere but not like in a disorganized way it's all it's smooth it's a smooth map and that's that's kind of why um you know it's it's meant to give an intuition for how these dimensions of the latent space might um map to smooth properties of the input space because on this simple example all it's really doing is squishing the thing down but there's also an important point which is that it didn't quite fill this gap here you know if it really was going to squish it into a sphere a Gaussian then it should fill that and you you'll you'll you'll see this when you're training generative models that in the latent space things will be smooth and nice and there'll be a seam because it's like you're mapping this complicated topology onto a sphere and you're going to have cuts and seams and you see it right here so if you're navigating through VA latent space or diffusion model um activation space on some layer of the network you'll you'll come across these weird jumps where it's like smooth moves nice variation and then suddenly it switches to a different category and that's what's going on here okay also these tendrils like you you can't this is all smooth transformations how do you cut you can't really cut so you get this tendril of probability okay so these were the models that we've seen in the last two lectures we covered a lot so we talked about you know uh a bunch I'd say think the most popular right now is diffusion models the most like complete like full picture that encompasses everything is VAES the most cute and fun is GANs uh the ones that are popular in language modeling is auto reggressive and they all have different trade-offs Some of them have latent variables that are explicit some don't some give you a density or energy function some don't and some give you a way of sampling efficiently and some don't uh so that's for your own reference to uh review okay thank you
