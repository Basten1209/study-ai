# Lec 05. Architectures: Graphs

Source: https://notebooklm.google.com/notebook/1974010f-eec5-485b-b074-000c70149926
Notebook: MIT 6.7960 Deep Learning, Fall 2024
Source order: 6
Extracted: 2026-07-02

## Transcript

okay hi everyone so we're going to get started um I'm the third instructor for this this course so I'll just quickly introduce myself uh I'm Philip Pisola i'm I also just go by Phil so you know feel free to call me by my first name just fill is fine um and I am a professor in EECS do just I love deep learning deep learning is for me the most um beautiful kind of math and uh intelligence that that I could imagine i I think that it's a very popular subject obviously but sometimes deep learning gets uh a little bit of flack because maybe it's too hyped or because you know people feel like too much money is in it so there's always this kind of weird push back to deep learning and I just want to say like I just love it it's so cool and it's okay to love it like we're going to try to convey why it's so cool so deep learning to me is just the most my favorite subject of all and it's great to get to to teach you about it okay so there's a lot of beautiful things about deep learning you've already heard some of them and today we're going to hear one more which is uh graph neural networks uh this is a continuation of the sequence that we have going on architectures uh so we have four lectures we have the one that Sarah gave on Tuesday on convolutional networks now we're going to see graph nets that are kind of a generalization of convenets along with a lot of other architectures and then we'll see transformers which are a special kind of graphn net and we'll see recurrent networks and memory based architectures as well um over the next few weeks okay so the outline of of this talk is going to be first talking about what types of problems are appropriate to use graph nets on and then we'll talk about the main way that you know graph nets work we'll present that as kind of a message passing algorithm but you'll see that it turns out to be uh very familiar it's just another neural network that has a forward pass and a backwards pass and then the last section of this lecture will be on approximation power so this will be continuing on from the approximation lecture that Jeremy gave looking at what what types of functions can graph nets approximate to what degree are they universal what is their capacity to uh represent different types of functions and one of the kind of interesting things I want to point out here at the start is that graphn nets are not universal in the same way that MLPS are and in fact that's where the power comes from and that's that's what Sarah mentioned at the beginning of last lecture as well that the idea of architecture design is uh to make systems that are maybe better to run on hardware on GPUs to make systems that are easy to paralyze and so forth but another big part of architecture design is adding constraints so making that making a function approximator that actually can't fit certain types of functions because we want to rule those types of functions out so graphs are a uh graph nets and graphs are a language for introducing constraints that limit your system from doing things you don't want it to do and this is one of the perspectives on on architecture design that we'll keep on coming back to we we want to be able to fit the data but we want to constrain oursel to fit it only in the right ways so universality is actually not not what we're after uh in architecture design but within the family of functions that we do care about we do want the the architecture to be able to approximate them and we'll see what family graph nets can approximate okay quick reminder on logistics is that problem sets are due next Tuesday so hopefully you're making good progress but if you haven't started like start now it's you know it's a twoe problem set there's a lot of stuff in there okay okay so let's talk about what types of problems graph nets are amendable to and maybe before I tell you some examples that I have in mind um I could just ask the audience so uh graph nets are going to be appropriate for problems that naturally um can be specified with a graph or data types that exist on a graph so in your in your work in your um fields that you're coming from what are some problems that you can think of that are wellstated as graph problems okay yeah let's go up here okay so one of social networks social networks are are graphs uh you know people and their friends or their connections okay another one yeah hidden marov models are uh one kind of probabilistic graphical model and and this idea of graphical models for representing probability distributions is is a classical idea and graphn nets are appropriate for representing things like this uh okay back here gene interactions yes I'll have some examples on that uh so you know whenever I have different different genes that are interacting with um each other or with you know proteins that they uh get trans transcribed from those genes then these could be thought of as like nodes and edges that represent relationships let's do just one more cross referencing different websites so um things like page rank uh hyperlinks between different web pages like Google started as a graph search company essentially they represent the web as a graph of hyperlinks and then they try to uh evaluate where the important nodes are in that graph okay so whatever problem you have you know if you're have studied physics or chemistry or computer science I'm sure that you have seen a million graphs that's one of the basic data structures we use for representing um the interesting interesting types of data in the world okay so graph nets are an appropriate technology for problems that naturally exist on graphs and there's a few different things we might want to um ask about a graph so one thing is you might want to take a node in a graph maybe this is a social network graph this is a user in that social network you want to classify something about them you want to say you know is this is this person um somebody who likes um jazz music or not maybe that's the the prediction problem that your social media company wants to make okay so that's like a node classification problem we can ask a question about a node um other times you might want to ask a question about the entire graph like what is the average number of friends that people have within this social network maybe that's a property that I care about so we can ask questions about nodes about edges relationships about the entire graph uh I'm going to give a few different examples just to just to motivate why graphs are so important um so another one might be this i think this is from Pinterest um so it's users and the kind of the boards that they're interested in so these uh kind of topic areas they're interested in so assigning you know you to what are the topics that you're interested in on this um on this website that allows you to see content under these topics so this is like a graph between people and their their interest okay um these types of graphs are often used in recommener systems i want to recommend what you might be interested what you know what next video on YouTube would you want to watch so it's like a bipartite graph between users and their interests or users and content okay so I think we had the gene uh expression uh idea previously but in general for biology and especially chemistry molecular biology uh we have graphs which are molecules right molecules are well represented as graphs there are atoms and bonds between the atoms and if we want to do things like predict which where where would the binding site be on that molecule for some drug or would this uh molecule be toxic to a human these are graph prediction problems i'm trying to ask ask a question about that graph which is the molecule structure okay uh so in biology and chemistry graph nets are used extensively uh here's another example where we have the graph of interactions between different drugs and the drugs uh effect on on the human uh for some kind of medication so we might have in this example two different drugs that can um you know have a positive effect on you but if you combine them they're negative these are interactions between drugs that can be can be bad right you always have to read the bottle on the on the pills you're taking like you know don't take this if you have this other drug right so uh that can be represented as a more complicated graph now in this graph we have uh edges between uh pharmaceutical interventions like the drugs that you're going to take and also edges between the drugs and the proteins that they affect and so different types of edges and different types of nodes can exist in a graph and we are going to be wanting to learn u properties of these edges and these nodes so that we can make predictions about what combination of drugs might be toxic okay um so graphs also come up in just kind of classical computer science algorithms so in shortest path algorithms in uh graph search of various forms tree search algorithms we often represent classical discrete uh optimization problems as some kind of search over over a graph so here's one example this is from um Google maps and they're trying to find the shortest path you know the the fastest driving route between you know your your house and where you want to go so you probably use Google Maps to do this all the time so how do they do it well this is a huge huge graph it's a whole world of of roads so it's it's slow to use classical algorithms like Dyster's algorithm and additionally there might be a lot of uh constraints beyond just searching um over the edge lengths of that road network there might be it might try to optimize for um other properties like how safe or scenic is the road or avoid highways or there's different uh constraints that you can add on top of this it becomes a very difficult graph search problem to solve with classical methods that might be um too slow and so one use of graph neural networks is to find heruristics and approximate graph search algorithms that can solve these problems more quickly so Google and this um in this example is using a graph neural network to do faster search than classical algorithms and we'll talk a little bit more about that as well okay by learning heristics okay another one i don't think anybody mentioned physics but that's another place where uh graphs come up all the time so one of the popular ways to simulate fluids or particles or atoms is by thinking of them as a set of nodes with edges between the nodes that are um indicating the physical forces so two particles might have an edge and that edge is describing the kind of repelling force between those two particles uh because we know that you know two atoms can't occupy the same space and they might have electrical forces that push them apart so in this example it's a particle system that simulates fluid dynamics and and this is represented as a graph neural net that takes in the graph of all of these uh water particles and the edges between them are going to learn to push apart those those nodes essentially okay so just yeah more examples and there's some paper references if you want to read up on these u in more detail and then yeah most abstractly um just a lot of optimization problems especially discrete optimization combinatorial optimization take the form of a graph problem where we might have uh a set of uh decision variables we're trying to optimize we're trying to find what is the setting of these x's that will satisfy a system of constraints so on the right hand side here we have a linear program it's u we're trying to minimize this linear cost function subject to some constraints and that can be set up as a graph of constraints that um will penalize a certain kind of interaction between the decision variables X and we want to find the setting of the decision variables that satisfy those constraints and so we can phrase this in the language of graphs and there are graph neural networks that can help us solve these types of problems uh more efficiently and something I want to point out here is that again just like in the shortest path example I gave we do often for these problems have classical algorithms that we know will find the solution and so one question you might have in mind is why would we bother with a learning algorithm why would we bother with a neural network that's going to do something that we already have the kind of optimal classical algorithm for we know exactly how to solve a uh a linear program on the right we know exactly how to solve for shortest path in a graph on the left and the reason is essentially that these classical algorithms can be slow and if we want to um use a learning based system we can potentially learn a faster way of finding an approximate solution and the trade-off is that the classical algorithm will find will be guaranteed to converge to the true solution uh but a but it might be slow and the neural net the graph net might be able to use heristics that work really well on the data distribution we train it on and so it will solve the problem more quickly on the data distribution that we're training on but it won't be guaranteed to find the right answer on other data distributions so you're kind of learning something that is more specialized to that data distribution okay cool okay so now let's get into um the technical content of how these graph nets work how do they represent data and this is how we'll represent graphs for this lecture we'll say that a graph is a set of nodes every node is going to come with a attribute vector so this is going to be like our input uh to our graph processing uh algorithm we will have a attribute vector for each node if these are molecules that attribute vector might tell us what is the atom is this you know carbon is this hydrogen right so in gray we have this this vector which is like the initial data our observation about that that node in the graph and what we're going to try to learn is a mapping from our graph nodes to representations of the nodes that are going to be more informative about the properties that we care to predict okay so this is just like we can do you know take an image patch and embed it into a vector we can also take a node in a graph and embed it into a vector that tells us something important about that node maybe classifies uh is it a binding site for the the drug of interest okay additionally we will represent the edges between the graph with a adjacency matrix and and this will be an additional input to our graph neural network so we'll have a set of nodes which are represented by vectors of observed data and an adjacy matrix and then we'll do some computation on that to make a prediction about the nodes we can also make a prediction about the entire graph itself and that's sometimes called a graph embedding so this is like graph tovec right we have on the left side we have node tovec every node gets embedded into some feature vector that tells us something of interest about it on the right we're going to just embed the entire graph into a feature vector that tells something something of interest about the entire graph for example is this molecule toxic or not okay so we can ask questions about nodes in a graph or about the entire graph itself okay so GNN's graph neural networks are GNN's they learn a function from these adjacency matrices that tell us the edge structure and the set of graph uh node attributes that are observed to a set of uh embedding vectors or predictions about the nodes or the uh entire graph itself okay is do I see a question in the I think I see a question here yeah yeah so the question is what is the relationship between node embeddings and graph embeddings and we'll see more details on this but the node embedding is going to be a a vector that we learn for each node in a graph and the graph embedding is going to be one one vector for the entire graph we'll see examples of this okay and then I saw one other question good okay in the back uh per task are you learning either the node embedding or the graph embedding or do you typically jointly learn so are we going to learn either node embedding or graph embedding or are we going to jointly learn them uh you could do either one you could do both and uh yeah we'll in general we might learn a set of node embeddings and then sum them up at the end to get a graph embedding that would be one one way of combining the two okay one more question then I'll move on yeah over here sorry in the middle yeah so uh so I think if I understood the question um if how much do we care about the dimensionality of the adjacency matrix uh if it becomes if we have a really big graph with a lot of connections just to become intractable and I would say that's that's more the that's not really our choice as modelers that's going to be the the property of the data that we're working with so yes if we have a graph if our data type that we're working with our molecules are just very densely connected and this will be something we have to deal with and that will be computationally more expensive to deal with um but that's we can't we don't usually try to simplify the the input graph we're going to take the input graph as given and ask a question about it okay so before we get to how graph nets work let's let's first ask what if we just wanted to make a prediction about graph structured data with an MLP we've already seen MLPS so MLP is another name for that you know fully connected neural network um what if we just use this fully connected neural network how would we take uh as input the properties of the graph so one one thing that we need as input is the adjacy matrix that tells us which nodes in the graph are connected to which nodes and and and just to quickly show you what that looks like um let's see where what happened to my mouse how do I find my mouse okay well anyway you can see the adjacency matrix up here on the left and this is just telling us uh which nodes in the graph up at the top right are connected to each other so one means there's an edge between the node index I and the node index J in that matrix and a zero mean there's means there's no edge so what what is the uh the fourth row of that adjacency matrix which node in the graph at the top right does that refer to blue blue okay I heard it yeah so blue right there's three ones and that's the only um node in that graph that has three edges so that's how you would know that that's that's the relationship okay so the adjacency matrix tells us the connective connectivity of the graph and we additionally would have a set of attribute vectors for every single node the observed vector uh for that node and we could just put these into an MLP by simply you know creating a serialization of our adjacy matrix and all the the node attribute vectors and just one really big vector and just put that into a multi-layer perceptron right so can anyone think of why that might not be the best strategy what might be an undesirable property of just serializing our adjacency matrix and our node um attributes uh let's go over here uh so adjacent matrices can only consist of ones and zeros whereas NLP can take in real numbers okay yeah that's that's so adjacy matrices only consist of ones and zeros maybe we're not making use of the full capacity of an MLP to take in real numbers that that's what good idea depends on the index you put on the notes so not is not going to work yeah that was uh so the the answer uh and this is the answer that I was had in mind myself is the uh adjacy matrix represented as a vector is going to depend on how we assigned indices to the nodes in the graph and that decision was kind of arbitrary like we decided that the fourth row of the adjacy matrix the index four is the blue node but why why didn't I say that in index 3 is the blue node so generally for graph structure data we want to be processing it in a way that is invariant to how we chose to index into the nodes the structure of the graph the structure of model of molecule and and in many types of graph data don't really depend on um the ordering of the of the nodes okay uh so in math terms we would say that we want a function which is permutation invariant to permutate permuting the indices of of the adjacy matrix uh and permuting the uh the ordering of the node attribute vectors that we put into our our system okay so here is a permutation matrix P applied to the rows and the columns of our adjacy matrix and the graph that is described by this permutation uh is the same structurally as the graph that's uh described by the original adjacy matrix we've just chosen a different ordering of of the indexes to label the nodes okay so uh the first matrix P permutes the rows and this and the prpose permutes the columns uh additionally if we have a matrix of node attributes so we have you know four nodes and so we'll have a four by D-dimensional matrix X of the node attributes this is the observed data at each node um we can permute that with our permutation matrix P and in order for the graph net f or the function f to be invariant to these types of re uh reindexing of our graph we would want that permuting the adjacy matrix and permuting the nodes the node attributes will result in the same answer as any other permutation so this property is called permutation invariance uh very related to what what we heard about on Tuesday where convolutional networks are invariant or equivariant to translation graphn nets are going to be invariant or equivariant to permutations of the inputs okay so um permutation equavariance is almost the same thing it just says that if you permute the way that you have indexed into the data then you should permute your outputs so if I shuffle my my nodes uh then I should end up shuffling my predictions okay so same idea as translation invariance versus equariance I would want to be permutation invariant if um I'm trying to let's say predict something about the entire graph so no matter how I index those nodes then the prediction about the graph will be the same and if I want to predict something about every node like I want to output node embeddings then I want to be permutation equariant so that if I shuffle the nodes my predictions about the nodes get shuffled in the same way okay just like I want translation equivariance if I want to have uh the output of my convolutional network uh have the property that if I shuffle or translate the input image I will translate the dense map uh set of predictions u in the output I'll translate the feature map that comes out okay so this is the kind of fundamental symmetry that we're going to be kind of exploiting with graph neural networks that'll be appropriate for processing data that uh for the you know problem of interest is actually permutation equariant or invariant yeah question I think I understand why we need to make it invariant but uh is that under the assumption that all graphs could be made into this invariant form some problems where the the nodes are just not able to become invariant yeah we we'll see at the at the end of the class some cases where you wouldn't want permutation invariance um but if for example like you live in a world where you know molecules always come in some orientation like the heavier like the blue is a heavier atom and it always like lives on it has a position a canonical position that you're always going to observe then you'd want to use that information but in in our world um yeah actually yeah I'll have to think a little bit more about a good example but we're going to see one at the end of the class okay one more question yeah why is the output a single vector is that have anything to do with the entire graph itself or is there a reason why that outputs a single vector yeah for permutation in uh invariance the output could be more than one vector it could be any dimensionality it just means that if I permute the inputs then I get the same output no matter what uh and that that's going to be most appropriate if I'm making some you know prediction about the entire graph if I'm making predictions about a single node in the graph I kind of want my my prediction for the nth node in the graph uh to always be in the nth output uh row of my pred like output matrix if I'm predicting uh a matrix as output or a set of vectors vector embeddings per node as output okay so let's again make the connection to graph neural networks uh sorry to convolutional neural networks so confets are graphn nets applied on a grid right we said that confinets are appropriate for grid structured data and if we're processing an image which is one of the main applications of convolutional networks then we'll have a grid of pixels or patches and we can think of that as just a graph of nodes and the edges between the nodes are the kind of the weights of this this the convolutional filter right and in a graphn that it's just the same idea except generalize to non-grid topologies so if I want to you know process something about the node in the center of that green region I'm going to look at all my neighbors and aggregate information from all of them just like in a confnet we're going to run a filter that's going to look at all my local neighbors in a patch around a pixel or around a node on a grid graph and aggregate information to make a new representation of that location in the in the um in the image okay so so so one question for you so graph nets look a lot like compets but now they're on arbitrary graphs as opposed to just on a grid so what are some of the interesting differences between a compet what is something that uh what is the new problem you might encounter with a graph that you want to encounter with a comp net yeah vertex is not the same shape all the time yeah great so the neighborhood of a vertex is not the same shape all the time you need you need a architecture that can handle having variable numbers of neighbors it's not going to be something that's always like a 3x3 patch and this is going to have hardware implications right it's going to mean that I can't just have like baked tongue to my hardware that I'm going to look at you know a 3x3 grid i'm going to have to make it that flexible enough that if the edge the node has a lot of edg uh neighbors then I will be able to aggregate over a bigger set any others yeah yes excellent there's no structure in the neighborhood all should be treated equally so in a uh convolutional network there is this kind of spatial uh dimensionality to it where left and right have a different meaning than up and down and I might want to treat left and right differently than up and down and in a graphn net uh we actually are going to represent the operation that kind of uh updates the node representation given the neighbors in a way that is um doesn't depend on spatially where those nodes land uh so the the structure is more abstract than the the spatial structure of a continent okay so there are a few important differences but there's also a lot of commonalities so here's a few of the commonalities one is that both of these architectures are based on local operations okay so we run local convolutional filters to update our representation of the data at that location and we run graph local um computations where every node in the graph is going to get input from just its neighbors and the way you make a global decision or you propagate information across the entire graph or the entire image is just by stacking those local operations so uh you can filter an image over and over again and the receptive fields gets bigger and bigger and bigger and that location in the image aggregates information from farther and farther away it's the exact same in a graph net if I update a node based on its neighbors then if I repeat that operation I will every time I repeat it I will hop one more neighbor away and eventually I'll cover the entire graph and I'll globalize uh another idea that's shared between these things is that just like a competent is chopping up the image into patches and operating on each patch independently and identically with the same parameters a graphet will also chop up graphs into little local regions and operate on each each local region independently and ident and identically and it's going to you know share the same uh parameters to update uh each region that each each region of that graph uh and another interesting property is that confidence can operate functionally on any size input image so they just slide that filter over as much as they need to go and a graph net can operate on any size graph you just continue running that local operation at every node in the graph okay so these are that's a big difference from MLPS which require a fixed dimensional input okay one question are we sharing the weights across different depths or only yeah so we're going to get into that in a minute so are we sharing the weights across depth and the answer is that's an option you can or you don't have to okay but we'll see that in just a second okay okay so here's one more uh just pointing out the exact relationship between a graphn net and a convolutional network so um this is a CNN uh visualized like a graph net so every single node in the graph is going to be connected to its neighbors and every node will have this gray um let me let me try to get my mouse back okay there there we go i don't know why my mouse is not showing up okay anyway it's it's it's fine so the the gray columns are meant to be the node attribute vectors but what would the name of those gray columns be if we were working with a CNN anyone want to say i think it's on the slide embedding embeddings or channels yeah so in a CNN every point in the grid has this column of channels underneath it and that's equivalent to the node vectors in a graph neural network so maybe some CNN review questions very quickly in this convolutional network what is the kernel size 3x3 okay great and what is the stride the stride is three so why why is the stride three right because I didn't draw um edges between uh like I didn't if you look at the pattern of edges it's like I'm taking that pattern that template and moving it over by three to get the um the next filters application okay cool so CNN's are GNN's but now let's look at GNN's more generally and get into the exact equations of how they operate okay so the uh the way I'm going to present this is that there's two steps one step is we're going to uh pass messages uh from each node to its neighbors and the next step is we're going to aggregate all of those messages to update the representation of of each node so every node has a vector associated with it and that vector is going to be modified kind of layer by layer through my graph net in order to come up with a better embedding that then makes a prediction at the very end of the day just like in a compet layer modifies the set of channels underneath every spatial location okay so here's the general form of one layer application of a graphn net okay so we're going to look at a really tiny graph here i'm going to going to highlight that middle node and ask how does that middle nodes node vector get updated uh after one application one layer of processing so what is one layer of processing so we call this uh in in each round k in each layer k we're going to do two things we're going to do aggregate and then update so the aggregate function is going to be a function that takes the node vectors denoted as h of all of my neighbors and it's going to produce a message m uh from that set okay and importantly aggregate is going to be a function that is permutation invariant okay so uh if I reindex into my neighbors then I shouldn't get any different output message m okay so the kind of italic n is the set of neighbors of a vector of a of a node v and I'm going to take the attribute vectors h from all my neighbors in that set and I'm going to then somehow aggregate them now once I've gotten that message which is now just a vector it's an n uh d-dimensional vector let's say then I will update the red node node vector using another function called update okay so I'll take the messages from my neighbors that my neighbors told me something about what they know about the graph and about the world and I'll combine that information with what I know about myself to update my own representation and now I can repeat this process k times and as you know K gets larger it's like going in depth in a neural network where I'm just updating my representation to be better and better and better until finally at the end I make a prediction okay so every application of that those two steps is going to essentially increase my receptive field by one so that red node is going to get information about the green nodes if I run this algorithm twice so I get a message that's passed from the green node to the blue node now to the red node on the second round okay so some of you will be familiar with message passes passing algorithms and hidden marov chains and so forth um and that's one perspective on this but uh I'll try to give a few different perspectives okay so what is the aggregate function and what is the update function okay let's make it a little bit more precise so the aggregate function is going to be any um set function that means it takes as input a set of items where the ordering doesn't matter so it's permutation and variant to the ordering and it produces uh an output vector now technically we would call this a multi-et function because um the set of inputs which are the node vectors h could potentially have duplicates so multi-et is just the technical name for a set that can potentially have duplicate items so if you come across this in the reading and so forth they'll call it a multi-et function but unless you're like deep into set theory then it's just a function from a set of items to a vector okay the set of items can have duplicates okay so let's look at some examples of aggregate functions so one example would be summation right a summation takes a set of any length and it produces a vector so summation and also is permutation invariant it doesn't matter how you order the items in that set so so summation is a multi-et function uh that produces a uh vector m as output that will be the message that is passed to the red node and another one could be average so on so on the left here I have um summation but then in gray I'm dividing by the number of neighbors so that would be the average as opposed to the sum it has interesting different properties we'll get into that in a bit and on the right is just another popular um aggregation function which is like um normalizing by the number of neighbors that both you and your uh where the messages coming from have okay so just another variation on the same thing okay so the key property is that aggregate is not a universal function it can't represent everything aggregate is constrained aggregate is a multi-et function it's per that's permutation invariant it maps a set of items to a vector and the ordering of the set doesn't matter okay another interesting aggregate function that satisfies those rules is max or min right this is this is a lot like the pling operations in convolutional networks so uh max will just take the largest item in the set it produces a you know fixed length vector regardless of the size of the set so it's a multi-et function uh min same same property and there's some interesting things you can do when you start to look at um different aggregate functions so here's uh here's a graph algorithm that can be implemented with min aggregation and uh this is the shortest path BMAN Ford algorithm so a lot of you will have seen this in intro algorithms but in case you don't remember uh try to find the shortest path with Bilman Ford what you do is you assign to every node in your graph a um an attribute vector which is just going to be the distance to the uh the target node you're trying to reach we're trying to find the shortest path to the target so every node will have an attribute which is just a scaler it's just the distance my estimate of the shortest distance or the cost of the shortest path to get to the target and I'll initialize those vectors D with infinity and then I will run this um recurrence over and over again until I converge where I'm trying to say I will um say that the the attribute vector for the target node uh is zero and everything else is infinity and then as I run this I'll look at all of my neighbors and I will um see if the distance between myself and the neighbor plus the estimate that the neighbor has of the distance to the target i'll see what that is and I'll take the minimum over that and that'll be the that will give me the shortest path if I if I kind of propagate out now from that target these distances initially it'll be zero and everything will be infinity and then all the neighbors of that target will be one if the cost is one to go one hop in the graph uh and then this will propagate outward and this will tell me the shortest the cost of the shortest path to that that target yeah so because the aggregate function is constrained like how would I guess this is more of a question of designing a problem to then use a graph uh a graph network how do you know that your information is needs to be permutation variant or how do you classify a set of problems to where you say okay I can I need to just aggregate my information together and it doesn't matter what order they come in like how do you know when to to to classify a problem like that yeah so the question is how do you know when you want permutation invariance and I think that's a that's a good question it's going to depend a lot on the the domain um but yeah so any problem where the kind of the ordering of inputs doesn't change the optimal thing you want to do the prediction you want to make is going to be something you want to apply this constraint and being permutation invariant to and we're seeing a lot of examples where structurally that actually does make sense like the shortest path in the graph has that has that property um I think that maybe a yeah maybe a clearer example of a case where you wouldn't want permutation invariance is if I'm doing image processing and I know that the top of the image is more likely to be sky than the bottom of the image i I if I have a graph which has these like local patches that I'm processing I want to know where I am in the image and I think Sarah talked about adding positional encoding to tell you where you are in an image if you don't want this translation invariance so translation invariance is kind of one type of permutation invariance but permutation invariance is a more general version of that okay oh yeah question clarifying question you mentioned the output of the aggregate function can you just quickly just dimensions of that vector yeah so the output of the aggregate function is a d-dimensional column vector okay so uh so yeah on your problem set you'll have some questions about this shortest path computation so you'll you'll get a little bit more intuition of it but it's just it's just a construction to demonstrate that you can represent classical graph problems like graph search and shortest path problems with these aggregate and update operations in in this particular case Bilman Ford can be implemented just as a GNN that only does aggregate and the update is kind of trivial in this case so Bilman Ford is on the left and the equivalent graphet is on the right the graphn net the aggregate um is going to be a min for Bilman Ford rather than the sum and uh the computation of you know the the the node embedding of your neighbor plus this cost could be represented as an MLP which just adds like one adds adds the cost of one if we're assuming that every edge in the graph has length one okay so you you'll work through that a little bit more in your problem set um so one one question you might have is well sum seems like a fine aggregation function min seems like a fine one uh is there a a family of aggregation functions that are universal in the sense that any multi-set permutation invariant mapping uh can be represented in that family and the answer is that yes uh this family of aggregation functions is universal in that sense okay so uh take my uh neighbors node uh embedding or my neighbors you know node vector and take my own node vector pass those through an MLP then sum up over sum of that operation over all of my neighbors and then pass the output through another MLP this is a universal approximator of the family of multi-et functions and one question you might have is wait a second didn't we say that MLPS on their own are universal approximators And they are if I increase the width to infinity but here we're not trying to approximate all functions we're trying to only approximate we want we want to have an approximator that can't fit functions that are not multi-et functions so the sum is the thing that makes it so it's one of these multi-et functions that uh the ordering of the edges sorry the ordering of the neighbors doesn't matter because we're just going to sumate over them okay so this has the property that it is a universal approximator to the family of multi-et functions but can't it can't represent um other functions okay so that's good it's a con constrain family okay the other thing that we need is what we call an update function and the update function will take our messages from our neighbors and combine them with our current node vector and update that node vector to be a better representation um of the graph of of the information that we're trying to process and for this one we're just going to use a very uh a simple update which is going to be like an MLP you know just a tiny one layer MLP it goes linear transformation of my each of my neighbors uh sorry linear transformation of myself plus linear transformation of the message coming from my neighbors plus a bias passed through sigma which is going to be a pointwise nonlinearity that could be a relu that's a canonical update function okay so once we have those two things then we have a graph net okay so I I also want to mention how we can aggregate that will give me um updated node embeddings and I can apply that over and over again to get my um node embeddings into a better and better state uh and then at the very end of the day I could just take those node embeddings as my prediction about properties of the nodes but if I want to instead predict something about the entire graph I just do one more step which is very simple which is I aggregate the node embeddings into a final prediction okay so here is my mapping from a graph to an embedding and it requires this final step which we call readout okay there's other ways that you could have decided on the conventions of how to break down the pieces of a graphn net i'm just giving you one that makes some of the analysis simple and clean uh but we're going to define this extra layer at the end which is going to take all of my node embedding so it's going to be a multi-et function again it's going to take a set of node embeddings possibly with duplicates and it's going to output a vector and this will be how I get a vector embedding of an entire graph okay it's just another aggregate operator has exactly the same form as aggregate but it's over all nodes in the graph as opposed to the neighbors of a node okay okay so this is kind of the message passing view of graphn nets it connects to classical uh theory of message passing algorithms and graphs but I actually think that there's a better and simpler way of understanding all of this um which will connect very intimately to what we'll see soon which is transformers so so here's here's how I like to think of it so if I unroll that those those message passing iterations for K steps I can just think of every step as uh taking a set of node vectors and updating them and here's my set of node vectors at the input that's like my input data the the observed data about the nodes in the graph okay again if they're drugs it might be the name of the drug if they're molecules it might be if if the graph is a molecule it might be the um atom like is it carbon or is it hydrogen okay if it's a social network then the input could be a description of the the user profile for every every person in that graph okay I take that set of node vectors and now I will do aggregate so what is aggregate going to do aggregate is going to say I'm uh I'm trying to make it so that the the vertically aligned nodes are corresponding to each other so my message is going to be passed to the red node is going to be uh that black vector which is the second one and as you can see the red node in the graph gets input from the teal the blue and the yellow node okay so the message is going to be coming from them and aggregate is going to take all of their um all the node vectors from those three neighbors and combine them into a single dimensional vector and that's my message that will be used to update the red the red nodes node embedding okay and then I will run update which is going to take the red nodes node embedding plus the message from those neighbors and provide uh an updated representation of the red node okay so what does this look like this now looks a lot like a neural network MLP just like we saw before we would have a set of neurons as input and then we would take a linear combination some aggregation of them and then we would do a pointwise nonlinearity now we have a set of vectors as input we for every um output vector we're going to aggregate information over the neighbors that it connects to and we're going to do a a pointwise nonlinearity but it's going to be a nodewise nonlinearity it's going to be this update function which will apply the same vector to vector mapping to every single um every single node in our graph okay and then we'll repeat the process again on the next step of this message passing algorithm but if we unroll it like this it just looks like an MLP ex the only difference is that the neurons in this MLP are going to be vectors they're going to be node embeddings and that means that the point was operation will be a vector to vector mapping the aggregate operation will be a vector a set of vectors to a vector operation uh but kind of topologically it looks very similar to just a regular old neural network okay a few questions let's go here how is this different from an attention heads how different so how is this different from an attention head um so this perspective is the kind of connection between graphn nets and transformers and transformers are a special kind of graph net so we're going to talk a lot more in detail about that and we'll get to attention then attention is just a special uh aggregation operator it's a particular kind of aggregation operator always nonlinear so update so you need to have some nonlinearities in here and in graph nets you can have nonlinearities in the aggregate and or the update and I think typically they'll both be nonlinear but uh theoretically you probably can prove universal approximation even if only one is nonlinear I have to think about that a little bit more y the optimal number of iterations for kooing the question how how should you set k what is the optimal k yeah So typically we will um K is like your depth now in graph nets the interesting thing is that we could choose to share parameters we could choose to do the same message passing algorithm at every iteration or we could choose to say that my aggregate function has a super script K so I use a different set of parameters for every um iteration of that aggregate function uh so now aggregate is going to be you know some parameterized function s here we said it could be this this uh in this form of uh an NLP and we could choose to make it time dependent dependent on K and use different parameters per K or we can choose to make it use the same parameters for every single layer if we choose to make it time dependent then we'll have to pick a fixed depth K that we're going to unroll to and train for that depth because we'll have a different set of parameters for each of these operations but if we choose to make it not depend on K and share parameters across time across depth then you can just run this as long as you want and it will keep doing computation and you might want to ensure that there's good asintoic properties like if you run it forever it will converge to a good value and those are two different settings that can be interesting to look at uh let's go in the back over here can you explain the intuition behind the update for again and specifically I'm curious why we use different matrices for the cell and the neighbors right so what is the intuition behind the update uh formula why do we use different weight matrices so I think what you could do is you could always just concatenate uh h your node embedding with your the message from the neighbors with m and then have one big w and then it will just look like a linear followed by relu layer uh it's just separated here for notational kind of clarity I suppose okay one more and then I'll move on how do you pass information if you have like edge information for example the distances between the nodes yeah that's a great a great question so what if you have edge information so we I I said you have node attributes and you just send messages based on which edges connect to which nodes so the edges only tell you about who your neighbors are to send messages to there's no other like weight on the edge or information on the edge and so um I won't get into it in detail but you you can also have edge attribute vectors and then have kind of a generalization of a GNN that has node attributes and edge attributes and then the update in the aggregation is going to also take into account the edge attributes okay okay so yeah so this is the picture of how I like to think about GNN's and you might be wondering wait a second like where is back prop coming in here is this message passing like is that an optimization algorithm no the message passing and this propagation that's like the forward pass through the network it's taking data about the graph and updating the representation of the nodes over and over again until finally having a readout at the end that makes a prediction so how would you train that you just back propagate through this computation graph okay nothing different so these message passing steps are just a new computation graph that looks like that when enrolled and I can back prop through the whole thing okay so same exact algorithm that we saw for training any any other neural network okay cool okay here's a this might be a slightly tricky question um but I said that convolutional networks can be represented as a graph and then you can observe you can just treat them like a graph net it's a graph that has this grid connectivity what is the graph net for an MLP what what does it look like what does the graph look like for an MLP let's uh somebody who hasn't spoken did you speak no okay yeah in the back there yes just just a chain so in particular what does the graph look like it's really just a node because the message passing algorithm so remember that an MLP takes a vector of neurons as input and then does linear coon linear linear so a node in a graphet comes with a vector so that has a generality of being like the vector of inputs to an MLP and then if we unroll the message passing algorithm it's just it's just this right it's just a chain of take that vector update it and in this case there's only one vector so the aggregate function is kind of trivial it can be identity and the update we said is just W times my node vector right here's what we said for the update there's no neighbors so it will just be welf times my node vector H passed through a relu or pointwise nonlinearity That's an MLP so an MLP is just like a graphet that's a single node compet is a graphet that is a grid okay okay so um throughout this class I'm going to try to point out like connections between different things because I think sometimes when you first learn about deep learning and just any topic it'll seem like everything is different but really everything is is the same really everything is you know densely connected to all other concepts and it's just like you can cast one concept as another concept with you know small manipulations and I find this really helps me to get inside and understand how these things work okay uh so there was a question about edge attributes and uh I'm not going to go into details on that but yes you can add vectors describing the information that an edge carries as well um we could have uh not just like a single message passing algorithm but maybe k or n different message passing algorithms in parallel that are passing different types of messages there's a lot of variations and riffs on these on these general themes uh one of the most important ones is that we can aggregate with a uh operation which is not just summing or averaging but instead is called attention which is like a a summation that's dependent on the value of your node embeddings we're going to talk a lot more detail about that because the name for that system is called a transformer so transformers are graphn nets with attention as the aggregation operator and uh you can read up on some of these other variations too but I'm not going to go into detail on them um the reading that I we assigned for today is um a chapter on graph nets uh but if you continue reading forward from the part that we assigned you'll see that there's a lot a lot more in that in that document it's a very good um kind of almost a book really manuscript okay so in the last section of this lecture I want to talk a little bit about the theoretical properties of graphn nets what types of um what types of problems can they solve and what types of problems can't they solve and it's going to be related to approximation theory so to set this up I'm going to give one more perspective on how graphn nets are operating and this is kind of the tree perspective of what a node will see so here we have a node of interest node A and it has neighbors B C and D and uh the message passing algorithm layer by layer will do the following this gray box is an aggregate function is going to take messages from all the neighbors combine them aggregate them together and send a message to node A and so um node A only is learning something from these these neighbors but if we run multiple steps of message passing or you know we unroll this graphn net deep uh uh so two two steps so two-layer graph net essentially now we'll get messages not only from BCD but also from A and C going into B and and so forth okay so the kind of information flow from the perspective suppose you're standing on the graph at node A this is who's talking to you and this is like the funnel of information coming into And every iteration of message passing will increase the depth of this tree by one okay so um okay so the layer 2 representation of a is going to be messages passed from the layer one representation of these other nodes which is going to be messages passed from the input raw layer zero node vectors for those those nodes uh question um like I guess when you're starting to train your your GNN um do you start with all the different nodes densely connected and then you set some sort of parameter to deconnect those certain nodes or or does it start where none of the nodes are connected great question so how do you know the connectivity of the graph do you start with a fully connected graph do you prune it so the answer is um the the vanilla answer for GNN's is the um the data tells you the connectivity that you're going to have to use like the molecule that you're going to be making a prediction about it has a particular graph structure which is just given by the data right if it's some hydrocarbon will have hydrogen connected to a few carbons and so forth and um that's not something you're going to optimize that's just given that's given to you so the graph structure is given to you and then you try to process that graph it's like that's the data now you're kind of alluding to could I somehow take nodes and learn the graph structure that makes me able to make good predictions this is like a meta like a this is another problem but we're not going to get to that today so for now you're given the graph structure and that tells you how to send messages to process uh information on that graph okay so this tree structure is important oh and additionally we're going to um just understand that every single aggregate function is the same so we're sharing the weights all those gray boxes are the same function they could be different functions at different layers but in parallel across um all operations of aggregation within one layer of the message passing algorithm are going to be using the same function okay so um that's the weight sharing idea and that has some really nice properties that means that if we add more nodes to the graph uh after having trained so we're learning the parameters of these gray boxes learning the parameters of the aggregate and update functions but after we've learned those parameters we could run the same GNN with the same aggregate function on a graph that has a different shape uh because in order to aggregate over a different set of nodes we just use the same function again on those on those new set of nodes just like in commons I can train on a small image and run the filters on a big image because I'm going to use the learn filter but just on a different set of inputs okay so that means that graph nets can be trained on graphs of one size but they'll generalize to graphs of other size uh they statistically they might only generalize um to graphs of a slightly different size because they'll have maybe overfitit to to that to the training data which came in a certain size but functionally they can process they can just repeat the same operation no matter how big the graph becomes okay so um yeah we're going to get to the the the theory that makes use of these trees in just a second um but let's quickly make sure we understand how to set up a GNN and train it on a problem so let's say I want to uh train a graphet on on you know some problem of interest like predicting the toxicity of molecules so what would a data point be what would a training data point look like so who can tell me what a training data point would look like for classifying toxicity of molecules let's go to somebody new over here yeah so like a set of emule toxicity yeah so um I I think you're roughly saying what I wanted to say here so uh if I'm trying if I have a supervised learning problem where I know the desirable uh prediction I want to make for every node like the label of every node then I would have node and label pairs and that would be my training data if I want to u predict a property of the whole graph like I was saying the toxicity of that entire molecule it'd be the whole graph uh plus the the label is it toxic or not maybe a binary label so that's my that's what my training data looks like so molecule toxicity or um a social network and for every single node every single person in the social network a prediction of what music they like to listen to and that would be a label of what music they'd truly like to listen to if I'm going to fit to that data okay so then what do we do how do we train on that what do we have to specify we have to specify the aggregate function the update function and the readout function the loss function if it's a classification problem maybe I'm going to have a cross entropy loss uh between the predictions at each node and the ground truth labels and then we'll train this with back propagation stoastic gradient descent okay yeah question oh for the node label pair uh we'll also take as input um the adjacy matrix so the the a single data point would be like a graph with a lab target ground truth label for every node and additionally the connectivity structure of that graph because that that's going to be used in the algorithm too the graph label pair would just be the the graph uh the same the same input but then the target output would be a label for the entire graph yeah yeah so I suppose I should have said node comma connect like adjacency matrix comma comma labels yeah but every node has a label is is the point there and then you you'd also need that other information okay uh one more question here you color these areas blue so does that mean that some nodes cannot have labels yeah i know this that's really just meant to refer to um we are isolating one node and looking at its set of neighbors and uh yeah don't read too much into the visualization okay sorry this this wasn't the best visualization um okay so I think I'm gonna I'm gonna skip past this this is just giving an example of you know in that paper that I showed at the start how they can use how they can set it up as a GNN to predict something about molecules and you can see on the on the left over there that we have these boxes which are like aggregating information from um all the different proteins and pharmaceuticals that are interacting the system you can read that more if if you're interested you know this is the same we can set up our Google maps problem um and there's a lot of connections for you to read up on if you're interested so of course graphs come up everywhere so uh in signal processing in proistic graphical models and inference uh and graph nets are intimately connected to these and here's a few examples okay but let's let's get into the final section which is the theory of what kinds of functions can GNN's approximate okay and the key question here is going to be uh which graphs can a GNN distinguish and we'll see that they actually can't distinguish all possible graphs from each other there will be these equivalent sets where there'll be two different graphs where a GNN will necessarily have to make the same prediction because according to the GNN those are the same graphs so what is the equivalence set okay so so uh here are a bunch of graphs and uh you can imagine that uh from the perspective of certain types of um algorithms uh certain sets of these graphs will be seen as the same and we want to ask which sets of graphs are going to be seen as the same as the graphet cannot make a separate prediction uh on one graph versus the other graph if they're part of the same equivalence class okay so the we're trying to understand what types of predictions and functions graph nets can approximate and the way we'll do that is by showing that graph nets cannot distinguish between certain types of inputs so they necessarily would not be able to approximate a function that has to distinguish between those those inputs okay so um yeah the the theorem is just a formal statement of that that if a graph net cannot distinguish between um two items in an equivalence class then clearly it must incur an approximation error if the function I'm trying to approximate does distinguish between those two those two graphs in that equivalence class okay so what is the equ set of equivalence class classes for um graphn nets uh what graphs are considered equivalent to each other okay so the way to understand this is is the tree structure so according to a graph net that graph on the left uh according to any node that's trying to update its representation based on messages coming in what it sees is this these tree structures so the red node is going to see this neighborhood structure uh that has that tree so it's connected on the first hop to the blue and green and on the second hop to all the other nodes and the green node is going to have that tree structure and the other green node is going to have that tree structure so from the perspective of a graph net these trees are the entire characterization of that graph that's all they can see because that's how the messages get propagated so if two graphs have the same tree structures for all the nodes then those two graphs are equivalent according to the graph net so this may not be obvious but it turns out we can find two graphs that are different and yet have the same tree structure and those two graphs will be two graphs that a graph net cannot distinguish cannot make different predictions for so here's an example and you're going to work on your problem set on trying to show some other examples but I'm going to show you this one in particular so first convince yourself that these are not the same graph okay structurally these are different graphs right one has these two kind of closed planer regions and the other has uh two regions but they're triangles uh these are these are uh structurally distinct graphs okay but let's look at the tree structure of the top left red node it has exactly the same tree structure for both graphs we can expand those trees going further down but this is enough to to make the point okay so the red node has a yellow node and a red node as neighbor and the yellow node has a red yellow and red node as neighbor and then we can just keep repeating that process you'll see they're entirely equivalent and yet they're not the same graph so two graphs can have the same tree structure but not be the same graph that means that if one of these molecules is toxic and the other is not let's say these are molecules and one of them is toxic and the other is not these are different physical things and so one of them could be different could be toxic and the other might not be a graph net will have to assign the same label to them each node in the graph like one hot encoding in addition to its node attribute then you could yeah so uh you're you're giving the answer to a question I was going to ask in a few slides so if you simply give each node a one hot encoding like tell it its position in the graph then the symmetry can be broken but the power of graph nets is that you want to be invariant to the ordering of the nodes and so by breaking that symmetry you break the permutation invariance so it's kind of a trade-off okay uh here's another case a bit harder to see but these two graphs are equivalent according to a GNN okay but they're not the same graph okay so those are equivalence classes according to the GNN's uh because uh a GNN can only distinguish uh two graphs that have different neighborhood tree structures now this actually connects to some deep and old theory in in graphs graph isomeorphisms where you're trying to say um how can I tell if one graph is the same as another graph and GNN's have the same power to distinguish graphs as this thing called the uh the one WL algorithm so um some of you might have seen this before i'm not going to go into all the details but you know this is going back to the 1960s this is really classical uh graph theory and the Weisfiller Leman algorithm is an algorithm that takes two graphs and decides if they're the same or different and uh the one Weisfiller Leman algorithm can distinguish certain kinds of graphs and it's the same kinds of graphs that a GNN can distinguish so so uh whatever the Weisfiller Leman algorithm can discriminate a GNN can discriminate and this is just a connection to some graph theory um so uh roughly the way that the wielder limit algorithm works is you assign colors to the nodes in the graph and then you do some kind of propagation and you decide if two graphs are the same if they result in the same coloring of the nodes uh and um let's see so yeah GNN can at best distinguish the same graphs that the one-dimensional vice fellow limit algorithm can distinguish but another interesting thing is that it's not only can can best it's actually you can achieve this so for any uh size graph there exists a a GNN uh such that you can distinguish two graphs that are um distinguishable by the WL algorithm so we have universal approximation within this within this family uh universal you know ability to discriminate within this family of graphs that can be discriminated by this classical theory and just uh like just kind of giving you the gist of why you get universality within that family is uh it's a two-part argument first part is that a arbitrary nonlinear some nonlinear function is a universal approximator of any multiset function and MLPS are universal approximators to arbitrary nonlinear functions so MLP sum MLP is the universal approximator to multi-et functions and um and then you yeah you can essentially show that the uh aggregate of a in this form that can take on a multi-et function can distinguish exactly the same class of algorithms as the WL algorithm yeah yeah great question so how common is it to find two graphs that cannot be distinguished in this way i think that last year I said "Oh it's kind of an edge case it's not too common." And then I realized I don't actually know um so I it's hard to come up with them uh on like pencil and paper but I so I think this is my my guess is that this is kind of a theoretically important thing it's like an adversarial like worst case thing like this could exist and then all your machinery breaks but I it'd be really interesting to know if like actual molecules that you're trying to classify the toxicity of would ever um have this issue would there ever be like a benign and a toxic molecule that uh would be indistinguishable according to graphnet but actually have different effects on the body and the answer is I don't know but I imagine that for like health and safety critical applications this is important to to understand yeah so this is a worst case i'm not sure how common it is in practice yeah okay okay so I I do want to bring this back to kind of empirical practice to show that this really does make an important difference if you understand the theory of uh what are what are the uh appropriate aggregation functions that will lead you to be able to approximate all the functions you care about uh within the set of multi-et functions well is that theory actually important in practice so let's let's look at this example so here's a paper where we have training training accuracy on the y- axis and we have the epochs of learning on the x- axis and higher is better so the red line is using uh an aggregation function which is MLP sum MLP okay so the G's are an MLP for the red line and I stated that that is a universal approximator to any multi-et function and indeed uh this is able to get 100% accuracy on fitting the training data this is just a training accuracy this is just about approximation not about generalization can I fit the training data but the interesting thing is that if I use a different aggregation operation like you might be just hacking away in PyTorch and think oh I'll I'll just I'm not going to do MLP sum MLP I'll just do uh sum and linear relu so linear relu is not a full MLP it's not a universal approximator right we have to have linear relu linear is a universal approximator but linear relu is not so let's say I just decided oh I'm going to save some computation I'm going to skip that line in pietorch well that's not a universal approximator and for this type of data this protein prediction problem I don't know the domain very well but for this problem you actually just plateau at a lower level of accuracy and I suppose that's because you can't even approximate the data you're trying to approximate because you're not using an approximation function that has that capacity so the theory actually is meaningful here and it sometimes I think that it's hard to see like why do we need to learn all this theory of you know what kind of kinds of functions can be approximated by what kinds of neural networks well it actually can matter in practice and if you use something that does not have the capacity to approximate the family you care about you can just be fundamentally limited the other curves are um even worse aggregation functions so here's one really interesting one look at this one that is I just use the blue curve is using the mean as opposed to the sum so I believe it's the same as um the equation at the top but rather than taking a sum I take the mean and suddenly it doesn't work and you might think like that seems like a tiny tiny difference instead of taking the sum over my node my neighbors I'm taking the mean over my neighbors surely that should work and you might be coding this way and just said "Oh you know I bet um taking the average as opposed to the sum is going to be more numerically stable i'll just do that." Well you the thing would fail completely anyone know why taking the the mean is going to lose information that the sum has let's see if we can go okay uh right back here yeah calculate how many yeah so maybe you can't calculate how many neighbors right so taking the sum knows something about the total number of neighbors because it's bigger with more neighbors taking the mean divides by the number of neighbors so it loses information and by losing that information on this problem you just get you can't actually learn the true function because you required knowing the number of neighbors okay so these tiny little details can actually make a big difference and it's important to know that okay so here's a few other properties of graphs that cannot be um learned with a conventional GNN as I've described it so far so you can't um estimate the longest cycle in a graph you can't estimate the diameter of the graph these are things that no matter how much training data you have uh the graph net is there's going to be cases where you'll get arbitrarily large error on these on these estimates because you can construct two graphs that are isomeorphic according to the GNN two graphs have the same neighborhood structure but different diameters okay so there's fundamental limits to what GNMS can do but how do we get around these fundamental limits okay we heard the answer already but anyone want to shout it out again what what would be a way of making a graph net that is not going to be um invariant to you know permutations of the of the nodes when hot encoding yeah so more generally positional encodings so just like we can make a convolutional network which is not translation equariant we can make a graph net that is not invariant to permutations by adding telling the each node where it is what position it's at and this allows you to break some of these symmetries with these um these equivalence classes of two graphs that have the same neighborhood structure because now we actually can see that the the nodes have different uh positions in the graph and one of the common ways of doing this remember that Sarah talked about adding kind of a sine useal um positional encoding where every position in your image is going to get um it's going to get a a vector that tells us where it is is it upper left is it middle is it right well in a graph we can get a kind of generalized version of where it you are in the graph using the igen vectors of the graph lelosian so I'm not going to talk in detail about that but this is just a generalized coordinate system for your location of a node within a graph and if you add those to your um graph node inputs the node vectors at the input layer to the graph net then you will uh break some of these symmetries and that can be good or bad because now you might not generalize to new permutations but you will be able to discriminate things you couldn't discriminate before so it's a trade-off okay so yeah that's just like positional encoding and CNN's which we saw on the previous day so I'm going to end there um we saw that graph nets are appropriate for processing graph structured data it turns out that a special case of the graphn net is called the transformer and that's just the dominant architecture these days and we're going to hear a lot more about that in a week or so okay and I can take questions up here at the end but feel you're welcome to all leave now thank you
