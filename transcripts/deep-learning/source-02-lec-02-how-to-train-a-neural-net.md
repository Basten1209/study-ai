# Lec 02. How to Train a Neural Net

Source: https://notebooklm.google.com/notebook/1974010f-eec5-485b-b074-000c70149926
Notebook: MIT 6.7960 Deep Learning, Fall 2024
Source order: 2
Extracted: 2026-07-02

## Transcript

okay so welcome to to lecture two of deep learning uh today we're going to talk about how you actually train a neural network um and I imagine that some of this might be review for some of you but uh hopefully kind of the perspective or the way we talk about um the process of training a neural network might be slightly reformulated from something you've seen before i definitely think that this slide is a nice way to characterize um the process so today um we're going to talk about how you actually train a neural network um first we're going to start doing a bit of review of gradient descent SGD um high level some optimization ideas um then we're going to introduce the concept of a computational graph um we'll talk about back propagation through chains and through multi-layer perceptrons we'll talk about back propagation through DAGs um and then we will get into the concept of sorry differential programming um okay so just again as a bit of a refresher when we talk about a neural network it's usually something that follows the sort of rough structure of this where you will have some input data X um and during training time you'll have some ground truth uh for that input data this is the thing that you want the model to learn to predict so in this particular instance that is a clown fish we're asking this model to predict maybe a visual category and that's why and then we have some model this is a sort of a chain of different blocks or or layers of a neural network um each of those layers will have parameters associated with them and then once you send this input through all of those layers you'll calculate a loss based on the difference between what the model predicts and that ground truth and all of those parameters that are sort of going through the network those are actually what's getting learned and what when you say getting learned essentially we just mean we wiggle the values around in numerical space until we get something that is at least reasonably close to optimal based on a set of data um that we actually have access to um cool so essentially what you're trying to find is some optimal set of theta that minimizes that loss function um over all of your available data set now when we talk about gradient descent here um finding that sort of uh optimal set of parameters what we often will talk about is that that's going to be um the set of parameters that is optimal for some cost in this particular sense that cost function is the sum of the loss over all the data points in our training data set and then how do we actually fiddle those weights around what's the mechanism um that's that's really just using optimization um to try to find those optimal parameters for that cost function so what's the knowledge that we have about J well first um we know that we can actually evaluate J of theta for any given theta so this is something that we can actually evaluate right you can calculate the loss over your over a training data set or a test data set um we also know that we can evaluate the gradient of theta um this is sort of first order optimization um you might sort of take some approximation of the gradient based on a specific point and you can evaluate that as well and additionally we can also do something like evaluating second order optimization you could actually calculate the hessen um in practice this isn't something that we often actually do um usually we really just focus on that first and then that first order optimization where we're just looking at that linear approximation to the gradient at a given point and then what is gradient descent so if you're trying to find this theta star that's this um this optimal value what you're going to do is you're going to start somewhere on the what we call the loss landscape or the surface that represents that cost function and then you're going to move in the direction of the gradient um sort of the maximal gradient um and take steps over your data and so for each of these you'll start moving sort of in that downward direction on that loss landscape and eventually find a minimum now of course you might note if this loss landscape is non-convex if it's quite complex and maybe has multiple minima this may not be guaranteed to be the global minimum it might just be a local minimum um and how bad that local minimum is relative to the global minimum often we don't know so if you assume you're taking a single iteration of gradient descent um and trying to find sort of better parameters what we'll do is we will start with our you know kith step of those parameters theta and then we will define some learning rate in this case ADA um and that's going to say how far we're going to step in the direction of that gradient and then you calculate essentially what is the direction of change um that will optimize that loss or that cost function so now what you do is you take the parameters of your model and you move them in the direction of the sort of maximal slope of your gradient with some sort of learning rate step value and that gives you the update to your parameters now this might be something that you could reasonably calculate over all of your existing training data um you'd like to ideally maxim minimize the overall loss function um which would be some sum of the individual losses over every single example in your training data but realistically this is often not possible to calculate um mostly just due to the time the computational complexity especially these in this day and age when data sets are getting larger and larger so um you know when I started in machine learning we were lucky if we had sort of imageet scale data sets like a million images now we're dealing with image data sets in the billions and text data sets even larger than that so instead in stochcastic gradient descent you assume that taking some sub the gradient over some subset of your data is a reasonable approximation to taking the gradient over the entire data set um so here what you do is instead you compute a gradient on a subset of the data at any given point and we call this a batch so if your batch size is one what that would mean is you would actually um update your parameters after seeing every single example in the data set you can imagine that also might be kind of suboptimal in terms of the computational complexity of doing those updates if your batch size is n where n is the full set of data then this is just standard gradient descent um but of course it might be possible this isn't easy to fit into the memory of your GPU or might just be really slow computationally interactable so also if your gradient direction is kind of noisy relative to kind of averaging over all um of the examples which is standard standard gradient descent um this is pretty normal right so taking a batch you're not necessarily guaranteed that that gradient over the batch is going to match the gradient over everything and actually in a lot of the data that I work with where we have things like significant imbalance over the types of categories we're interested in predicting um this sampling can have a pretty strong effect in terms of how stable that gradient is right because potentially you have some categories that are not seen for multiple batches at a time and then you start you see them and at that point it actually massively shifts your gradient um so so this instability is actually often related to um how kind of uniformly distributed uh your data set is but there are advantages of doing that stoastic gradient descent first it's faster um it's been shown to approximate the total gradient even with small samples um and that noise it's actually an implicit regularizer so one thing that can be um a benefit of stoastic gradient descent is it can actually kind of bounce you out of local minima um and help you sort of in reasonably find closer to what might be a global minimum there are disadvantages like we said there can be high variance there can be unstable updates and I've found experimentally that these updates are more unstable uh the more unbalanced uh your training data set is over the set of categories that you're interested to predict on cool any questions about SGD how many of you seen SGD before okay that's what I thought so this is hopefully a bit of a refresher for most of you um so I don't we also want to introduce the concept of momentum um and this is something that we might sort of implement within our loss or within sort of our gradient descent so essentially the idea of momentum is kind of the physical idea of momentum if you put something heavy on a slope it will gain speed as it rolls down the slope and what we what we do when we think about momentum in gradient descent is we essentially are biasing our gradient steps to continue in the direction of the previous update you're giving it that momentum based on its past um direction of movement and so here what that looks like is you're essentially just adding in again this parameterized by alpha momentum term that is capturing the direction that you moved in in the past and this can actually help or hurt um and the strength of that momentum that alpha is a hyperparameter um a popular example of this type of momentum in a gradient um in in a in gradient descent is atom if any of you have heard of sort of the atom optimizer um so this is something that is pretty commonly used but how this might actually help or hurt um so here this is just looking at uh optimizing for something that is quite sharp and you can see that if the momentum is zero you'll kind of roll sort of steadily towards that maximum um if you actually have a momentum of 0.5 you're getting to that minimum um much quicker right um maybe about half the time the number of sort of training steps but if your momentum is too high you sort of start bouncing past and then you have this almost oscillation and wiggling that happens as you're trying to find that minimum and it ends up taking longer to optimize your your function um and there's like a really nice uh uh blog post on momentum that has some really fun um interactive uh capacity if you guys are interested the link is down here it's from back in 2017 but I found this really helpful for building intuition cool so talking about sort of loss landscapes um which of these are differentiable so raise of hands give me one that's differentiable yes the first one yeah the top left yes thank you right and left still hard for me um yeah yeah uh the third one exactly it's flat but it does have a derivative right um so yeah exactly these two are differentiable um so now which of these have defined gradients in PyTorch raise your hand yes yes all of them have defined gradients in PyTorch um that's because PyTorch has autograd right it has this nice mechanism that helps us um calculate gradients for any function which of them would be hard to optimize over second one uh why it's got multiple local minima yep multiple local minima so it might be you might end up depending on where you start uh not in the global minimum uh yeah the third and the fourth the third and the fourth since they're both flat yeah so uh the third one is just flat everywhere essentially there's no signal in terms of the gradient the fourth one it's flat everywhere except for discontinuity but um anywhere on the actual graph again you have no signal um yeah and the fifth one as well um because the learning rate should be very very small otherwise we're just going to diverge right so so this one uh in the center of the bottom this is also difficult because the learn the gradient is very steep it's very very high as you get close to the minimum and so the challenge there is that as you kind of get close to that minimum the gradient is really large so then it's going to bounce you somewhere really far away um so essentially it's really difficult to actually ever hit the minimum because of what we call exploding gradients yeah so definitely um and one kind of nice intuition around um kind of easy versus hard to optimize you can kind of think about uh flowing water so you know in this example over here um you would actually no matter where you started you would actually end up getting to that minimum because essentially you can think of water kind of flowing no matter where it came from it would sort of still get to that middle of course that really only helps you um thinking about kind of is it minimizable so that really is only filtering out this one that has multiple minima um so one thing that's true um is that most existing popular neural network components um will kind of exhibit certain properties so like we said these would be hard to optimize but now I want to go through sort of what these different landscapes would look like kind of the the pros and cons of them and then we'll get into some high level intuition and talk about where the field seems to be going in terms of the types of loss um that we're calculating so something like this this is like the maybe the simplest case um this is a case that if you're a theoretician is really great because you can assume convexity um you have a single minimum all the gradients point to it everywhere and the gradient will gracefully go to zero as the minimum is is approached here we have discontinuity but it's well defined um in terms of the derivatives on both sides of that discontinuity and this is not a problem at all for pietorrch um here even if this is not completely flat which you can see here it isn't um it's what we call a vanishing gradient the progress would be really slow and noise in your sort of batches for example might end up dominating over the signal um so this would be very difficult to optimize here this one just zero gradient everywhere basically completely uninformative as to how to make progress and so it's really difficult to ever hit any low loss region there was a question Yeah oh yeah sorry sometimes I assume something is intuitive and it's not so thank you so much for asking um so this is uh this is like for um different sort of parameters assume like all of these are single versions of parameters right so this is now looking at that plot um and then it's now here this is um optimizer steps going from zero to like 100 so assuming you're starting from the top and it's basically stepping through um assuming this was your loss landscape and you had some sort of uh standard uh learning rate where the model would actually sort of move throughout gradient descent or throughout stocastic gradient descent so in this case there's no signal it just doesn't go anywhere whereas in the previous slide or like this one for example you might start out over here and it might bounce across and then go back and forth but it will actually sort of eventually find that minimum thanks for the question it was a good one um yeah so zero gradient this is what we were talking about in terms of instability um this one has what we call an exploding gradient the gradient goes to infinity as the minimizer is a minimizer is approached so you have really unstable updates and you can really overshoot it can be very difficult to ever actually hit that minimum and then here when we're talking about multiple local minima where you initialize matters a lot um this is often you know one one way that people handle the fact that many of our loss landscapes are not guaranteed to be convex we're not guaranteed that they only have a single local minima this is why you might for example uh try experimentally um a bunch of different random seeds and try to then select a model that ends up looking better and the variance in your performance based on different random seeds will tell you something about kind of how unstable your loss landscape is so there's a couple things that we wanted to bring up yeah well so PyTorch can make anything differentiable that doesn't mean it will be easy to optimize so for example that one that's flat like it's differentiable in PyTorch but that doesn't actually mean that it's going to find a local minimum because it doesn't exist so it's computationally possible to calculate a gradient there but the gradient will still be zero so essentially pietorch and autograd they mean that even if you take something that would traditionally be non-ifferiable you can still calculate derivatives in pietorch so we assume things like directional derivatives around discontinuities but that doesn't actually mean that every loss landscape is equal that's kind of what we're getting into here is sort of these ideas around what makes a good versus a bad loss landscape does that make sense cool okay so one thing that we wanted to bring up was this idea of an evolution strategy um so these are sort of gradientlike um they find locally loss minimizing directions in parameter space and the way that they do that is they sample small perturbations around your existing parameter values and then move towards the perturbations that achieve lower loss um so for example you could take some um sort of perturbation or error that's normally um normally distributed and then when you're actually calculating your values you take um you calculate them of the loss function plus some amount of perturbation in that normal space and then when you're doing your updates now you have kind of this um kind of almost robustness that comes in and it will actually select successfully minimize um even something that looks like this uh so here even if we started up here in this sort of um nonminimal uh situation um because we're kind of perturbing the values of the function it might bounce us over into a better value just by sampling this cost function these different things um so this is something uh these sort of evolution strategies is something that sometimes people will take into account and then another important one is this idea of gradient clipping um and this is a mechanism that helps us with uh basically peakiness or pointiness in our loss function so we'll successfully minimize this function and so here it's quite simple if the gradient gets too high above some large value you just scale them back to that value so you essentially just put a like a clipping filter on top of your gradient and this is a useful and commonly used hack um so here essentially what you do is you just calculate uh the change in your parameters and then you move in the direction of that maximal change but clipped to some uh maximum value so you won't let your model sort of move too far in any direction and this then helps us you know with not oscillating back and forth across something so what then is important in a loss function um so a few things that are sort of consistently showing up um one though this is of course not required everywhere continuous another one everywhere differentiable and a third one would be everywhere smooth so one of the most commonly used um nonlinearities uh is relu and this is something that is everywhere continuous almost everywhere differentiable but it's not everywhere smooth right um we have this kind of uh sort of kink in it um now uh recently there's been um a lot of work that will instead use something like Gillu which is a a Gaussian error linear unit um and this one essentially just takes the parameters times fi of the parameters where this is the cumulative distribution function for the Gausian distribution it looks something like this in two dimensions um and this one is everywhere continuous everywhere differentiable and everywhere smooth um the important thing I want to point out here is that we can kind of agree that these three things might make optimization easier and even if we don't precisely know experimentally or theoretically which properties are needed um for training neural networks trends do seem to be moving towards functions with satisfy all three so things like this gilo any questions about optimization and SGD yeah um so uh evolution strategies yeah essentially they're um instead of always moving in the direction of the gradient um you just randomly sample around uh your current parameter values and then you move in sort of a the random direction that maximizes or that minimizes your cost um uh it essentially it's helping you maybe handle things like vanishing gradients or like um some of these sort of uh parts of your loss landscape that might be um not actually functional to optimize super efficiently um and I think you can almost think of it like one one intuition is it's almost like regularization um in your loss if that makes sense also follow how common is it that people use like everything So um yeah I mean I think everything uh everything's a bit of like a a a big like soup pot these days right like there's a lot of these different experimental tricks um some of them even if they seem quite different um almost seem like they achieve the same ends when you talk about the experimental performance um I would say that a lot of the really top performing state-of-the-art models on you know different types of benchmarks are often ones that incorporate a lot of these tricks different sort of hacks and um mechanisms to try to get a little better at finding you know some maybe slightly better optimum um but I don't think we actually have a really great recipe for you know given a certain data set given a certain architecture what is the right way to optimize that architecture can you please explain how relates to this question yeah so um again I'm not I'm not saying here that like Gillu is much better than RU at all the time because that's not necessarily true um it's just pointing out that sort of the the trend in in these different loss terms for neuroral network training does at least seem to be moving towards uh moving away from functions that don't satisfy all three of these um but again like we don't necessarily know or we haven't sort of decided as a community or um sort of even gotten close to some sort of theoretical guarantee as a community as to what is the optimal um mechanism are these functions like should activation functions these are activation functions yeah yeah h sorry should those functions also not be uh well it uh it depends on the way you've set up your loss you you definitely can have loss functions that will have negative values um it just I mean that depends right and and I guess what you're saying is uh if you're trying to minimize a cost you want the gradients to sort of move in the direction of that but that's not guaranteed if you can't guarantee the loss landscape is convex everywhere right there will be parts where it goes up and down um is there anything is there is there any issue with having a non-monotonic function a non-monotonic sort of activation function or or loss function activation function um I mean there definitely exist like in theory activation functions that are non-monotonic um but I don't think that they're often widely used i mean it it seems to make sense right it's not right yeah it has this this part where it dips down right here yeah um but then this is kind of the question right is it better to be smooth or to be monotonic and that's kind of where I think the jury is a little bit still out yeah I guess we could put monotonic here but I I feel like these three are the ones at least from my perspective that seem to be kind of captured most consistently across the the um activation functions that people tend to use or are moving towards yeah I was wondering for the um pursuation can you talk about it from like a numerical mathematical equations what are the strength of levers that affect that like making it the magnitude bigger or smaller and also from the application sets if you're looking at data set you mean you mean this um I mean so definitely those parameters are something that you would need to experimentally find optimals for for any given data set or problem right like it's not like there's some sort of universal ideal here um and I think it'll probably depend a lot on on which data set architecture for example so I don't have any big rules of thumb for you there um though I would say uh one sort of generally good strategy is you take a paper that used it and you look at the values they used and you start there um just usually because that they've already done it right they've already done it to sort of define their their sort of optimal parameters and sometimes those can be really far off for a new data set but it's often not a bad strategy to at least start with something that has trained for someone in the past okay I'm going to move on great questions though guys i actually it's much more fun for a professor when people are engaged so it's it's great to hear from you cool so I'm going to move on to talking about computation graphs so we're going to sort of move uh to thinking of this all from this perspective of differential programming essentially here you're talking about a computation graph um here this is something like a graph of different functional transformations um each of those functional transformation is a node in the graph and then when you string all of those together you can perform some useful computation so intuitively maybe this is some sort of decision tree right that's a very simple version of a a graph of functional transformations so maybe I'm saying like all right I'm going to try to make it to the classroom uh so I'm going to go into a building um and then maybe on the right uh you've gone into building 45 which is correct and on the left you've gone into building 32 which is incorrect um and then sort of there's these different uh decisions you might make based on the the values of your input at any given time so if you did go into building 45 then you could maybe take the elevator or take the stairs um and that decision over what action you'd take could be a really really simple heruristic version of a functional transformation right because you're transforming your inputs to some output um of course uh the kind of what we actually sort of see here um in when we think about machine learning systems is each of these maybe blocks are something like a layer in a neural network or even an entire neural network right um and there's nothing that says that um what the size of these are you know what any sort of restrictions when you're thinking about it really from this high level perspective of a computational graph so deep learning primarily deals with DAGs these are directed as cyclic graphs so directed means the information only goes one direction on an even given edge as cyclic means you don't have loops or cycles um and we also assume that every single one of these nodes is differentiable though we did just say that using PyTorch you can calculate a derivative for almost anything so that becomes less of a hard constraint so if we now sort of think about this um from the perspective of for example a really simple multi-layer perceptron like we talked about yes um on Thursday um here you can kind of think of that now again as one of these directed asyclic graphs you have your input X you send that through this linear layer which is kind of one node in the graph and then that gives you some hidden unit Z you send that through an activation function or a reu that's again some computation that takes an input and changes it um to create an output known another hidden layer hidden state H and then you take that and run it through another linear layer and that gives you your output Y so you know even in MLP easy to represent as a computation graph so now let's talk about what a forward pass actually looks like and what's required to calculate a forward pass um so in the simplest sense um for any model right for any set of parameters for anything within that function you take in some input value you take in some theta which are the parameters of that component or of that sort of function and then you calculate the you map through that functional transformation the inputs the parameters and you get out the outputs of that layer so that's a forward pass for sort of any type of um computational function now you can have multiple layers right uh this computation graph for example could represent an MLP where now you have your inputs those you have your inputs and your parameters for that first component then you get those that's an output that becomes the input of the next component etc etc and then at the end maybe you have something like this loss um that you use to calculate your cost so then when you're thinking about learning now now here you have um in order to sort of calculate this learning that's where we think about computing the gradients of the cost with respect to all of those model parameters that happen all the way through the network right and so by design every single layer will be differentiable with respect to its inputs and therefore this is actually possible to compute it is possible to compute that gradient um with respect to all of these model parameters so you can figure out how you need to update those model parameters to move to more a more um optimal cost okay so a bit of an aside just in case any of you are not brushed up on your matrix calculus um because it this will show up a lot we're going to be doing doing a lot of matrix multiplications in this class hopefully this is super review for all of you but just a reminder so um we're going to assume that our inputs are for example column vectors of size n by one now if we define a function on that vector y= f ofx if y is a scalar then the derivative of that output with respect to x the input would be a horizontal vector where for each element of that vector you're getting the partial derivative of y with respect to that sort of first element or second element or third element of x and then that is a row vector of size 1 by n if y is a vector of m by one then we assume the jacobian formulation of the derivative of partial derivative of y with respect to x where now um you essentially end up with something that's a matrix of size m by n so m rows which is the sort of size of y by n columns is the size of x and each element of that matrix will be the partial derivative of that element of y with that element of x now if y is a scaler and x is a matrix so for example an image um of size n by m then the partial derivative of y with respect to that matrix x will be a matrix of size m by n we're now here um each cor each each component corresponds to that um that relevant component of X and note that by taking that derivative we flipped the rows and columns so it's been transposed and you know according to Wikipedia the three types of derivatives that have not been considered are those involving vectors by matrices matrices by vectors and matrices by matrices uh notation is not widely agreed upon we're not going to see any of this in this class cool so chain rule for the function h ofx is f of g ofx how do you take the derivative who remembers the chain rule yeah x times the derivative g x you're basically okay i don't think that's I don't think that's right it's all right man it's all right okay go for it gx prime with the frime of gx yes so the derivative of f x f with respect to g ofx times the derivative of g with respect to x yeah good job dude way to go for it no one's going to judge anyone for getting something wrong in this class all right so then actually if we write this out as z is um f of u and u is g of x then you can kind of write it out this way right um so essentially you can write this out as the multiplication of two partial derivatives so that means that p would be the length of vector u and m uh would be wait what the fuck is p what am I talking about all right i essentially what we want to get at is what size are each of these things so who can tell me what the sort of shape of the partial of Z with respect to X would be when X equals A i can give you one freebie if it's helpful so right if we say the size of u is um p the size of m z is m and the size of x is n um then the partial of z with respect to of with respect to x would be an m byn matrix so what size would the partial of z with respect to u be yeah uh m by p which I this this is really confusingly written I'm going to rewrite this next year p is the length of vector u equals the size of u is confusing um but yes m by p and then what's the last one p by n exactly cool so therefore if you said sort of the size of z is one the size of u is two the size of x is four then essentially what you're saying is um this derivative would be essentially equal to this product of two uh matrices though one is really a vector um all right we're bringing this up because we will see a lot of matrices times matrices in the next bit so that's the end of our little bit of a vector calculus or matrix calculus reminder so let's talk about back prop how does any of this become possible how do we calculate the update in those parameters essentially what we need to calculate is the partial derivative of J with respect to for example um those initial layers in the model theta 1 so by the chain rule you can essentially factor it out and you can kind of map that um back through all of these different layers um so the inputs and sort of outputs of of X as it goes through all these different layers and if you need to calculate sort of the partial D of J with respect to that second layer's parameters you'll note that um it's the same essentially but the thing that's different is here just these last terms are different but everything um for those two are shared and this is really the trick right um we could separately compute all of the derivatives using the chain rule but because these terms in the gray box are shared we only need to compute them once so back propagation is a pretty simple algorithm for propagating shared terms through the computation graph it's basically an efficiency trick but it's one that makes it computationally practical for very large models to be able to actually calculate these gradients so if this is a forward pass we're sending data forward through the network and then computing the outputs and calculating the overall loss then a backwards pass look like this which is that we send our error signal the gradients backwards through the network from the outputs and the loss back to the inputs and the parameters um does this make sense cool so what does a backwards pass look like for a generic layer we're going to keep track of two kinds of arrays of partial derivatives here so first L is the gradient of the layer outputs with regard to the layer inputs this will be a matrix right this is the the sort of what way does each of these parameters need to change um to sort of move in the maximal direction of um optim optimization so here um sorry So here this is essentially a matrix that's just telling you which way would these um would these sort of optimally would these parameters optimally move and then second we're going to keep track of g and this is the gradient of the cost with respect to the activations and this will be a row vector um so this is basically saying um yeah essentially uh what's the partial derivative of j with respect to x with respect to that input So now if we want to update the parameters that's actually really easy if we have both L and G um essentially it's just a multiplication right you have G out and you multiply that by L of theta and that matrix multiplication actually just updates your layer your your that tells you which way to move and so now you take your parameters and you change them by your learning rate um multiplied by that update so these so how do we actually get LNG for each layer So L comes from that derivative function of the layer which we assume is pro provided right we assume for any given layer that it is differentiable and that we have a function that lets us take that derivative and then G is something that can be computed iteratively um via recurrence right so you take the um the input gradient will always be equal to the output gradient times that that L for that layer so this is essentially the back propagation of the error signal back through the And then all of this machinery is used to then compute parameter update directions right so which way do we need to go so the full algorithm forward then backward first we're moving forward then we go backward and then we update and repeat and this is essentially how you train a neural network right you do a forward pass calculate loss you take that loss propagate it backward update all your parameters and do it again and again and again until you've sort of found some some likelihood that makes you think that your model is finished training often we'll do this we'll we'll talk more about mechanisms for this later but it's often using something like a validation set and looking for some sort of um plateauing of change um on that validation set so if we're doing back propagation um now what we've got here in the next sort of set of slides are essentially like like cheat sheets for a lot of this stuff um it's really just intended to be a pretty nice resource if you need to kind of refer back and think about you know how you might need to implement some of these so now a forward pass through um some through some hidden layer so now we have the layer before and the layer after we take this input we run it through the function and then we have this output now when we're doing that backwards pass we're taking the derivative with respect to the output we take the der the partial derivative with respect to the input and then we also are calculating how we need we're taking the parameters in and then we're calculating the change to those parameters going out so this means that layer L for this explicit layer we have um so all the inputs are going in with the the full lines and the outputs are always in the sort of dotted lines here so the inputs are then the previous layer's um output the change in the parameters with respect to um the next layer and the current parameters and then the three outputs of every layer um are going to be the output right you know running through the function um the change in the parameters with respect to the previous layer and the um actual update to the parameters and then so given those inputs we just need to evaluate those three things and this actually makes kind of training happen so now if we take a summary forward pass we'll compute the outputs for all the layers backward pass we compute the loss derivatives iteratively from top to bottom and then the parameter updates we compute the gradients with respect to the weights and then we update all of those weights okay so now what we actually do is we do this over batches right we're not doing this for a single data point at a time and technically what that means is we want to minimize the average cost over lots of data points you know the entire sort of size of a batch and in this day and age with very large memory um GPUs that that batch size could be thousands thousands of data points at once so then the gradient of the total cost is just the average of all the gradients of all the per data point costs right um because when you take a derivative it can move inside the sum so pretty straightforward so now if we actually have this as a linear layer let's talk through what that looks like because it turns out everything gets really nice and simple so first forward propagation is quite simple if we have a linear layer the forward um the forward the sort of weights of that layer are just a matrix so now if you want to calculate the output you just take that matrix and multiply it by our vector of inputs so pretty straightforward if you want to calculate back propagation to that input um this is essentially um the the input gradient is going to be the output gradient multiplied by the update to those parameters with respect to the input so essentially that just looks like this right this is just that matrix that we've defined that represents the direction that you would want to move in for those weights and then we look at the component of the output with respect to the J component of the input the i component of the output with respect to the j component of the input um it turns out that that um is wig and so that actually means that this is really just the weights again right so this is really nice and simple right you just multiply uh trans you know sort of in the opposite direction you multiply the gradient by the weights to get the gradient um coming in uh cool so your forward and backward paths are just multiplying by your weight matrix just in a different order pretty nice and finally um now let's see how we use those outputs to compute the weights update equation so how do we actually do that back prop through to the weights so that looks like this right we're taking the sort of trying to take the partial derivative of the cost with respect to those weights and so that's going to be the output gradient multiplied by this partial derivative if we look at the parameter wig j and look at how that parameter changes the cost only the i component of the output is going to change so this means that essentially you can break this down um through this kind of uh simple uh almost decomposition and you end up getting this because of what we just pointed out so essentially uh the partial derivative of the output at that I um component uh with respect to the weights matrix um the corresponding element I J is actually just going to be equal to the input of J right and so that means the following essentially when you want to calculate the update to the weights all you're doing is you're multiplying the input to the to the uh function by the output gradient again it's a really nice kind of simplification that comes out just because everything is linear so then when we actually want to update the weights you're literally just um taking ADA and transposing this and um multiplying it they're adding it in to the weights any questions about this sort of very nice simple reformulation that gives us essentially a bunch of matrix multiplications yes do we have enough time can you work through like a small example with computational or something um so at the end of the lecture I actually do have an example of that and so either we'll get to it at the end um I'm a bit worried about time or um it's in the slides and so you can actually work through it and there are the solutions um at the end sounds good cool all right so cheat sheet for a linear layer the output is equal to the weights times the input the gradient in is equal to the output gradient times the weights and the update to the weights is equal to the input times the output gradient and then you actually do that update based on adding in that um basically direction you want to move in multiplied by your learning rate so now let's look at a whole multi-layer perceptron what happens if we put all these operations together so first you have some linear layer then you'll have some relu some activation function in particular here we're talking about relu then you have another linear layer so now this will be sort of weights one weights two and then you calculate some lost based on the difference between your output and the ground truth so now to do backwards pass you essentially assume uh a slight change in convention um this will kind of clarify this nice connection between forward and backward directions so instead of representing gradients as row vectors we're going to transpose them and treat them as column vectors um and then that backwards operation for the transposed vectors will follow from that matrix identity that abrpose is bpose a ar a transpose right if you remember sort of basic matrix math um so essentially this then reveals this interesting connection between forward and backward um so backward for a linear layer is the same operation as forward but with the weights transposed so here you're going now you're going backward um you get again weights transposed times the gradient and then this is an interesting thing a relu wouldn't be a relu on the backwards pass right because um you don't want to sort of re pass them through a relu essentially what you need to turn it into is like a roughly like a gating matrix this will be parameterized by the fun uh by the functions of the activations from that forward pass um so these would be the sort of like a b and c sort of the the activations that you had sending those um weights through the relu on the forward pass what this is doing is making sure that you're not passing gradients for the components of the weights that corresponded to um the the parts that were sort of non uh non um like in that zero part of the relu right you don't want to send gradients for parts that should be masked out why is the that matrix diagonalized um that's just so that it's um operating on each element sort of independently right cool and then you know now you can kind of mask the values of the gradients make sure you're not sending um gradients for things that are going into that zero that negative part of the relu and then essentially what this means is that back prop is still a linear model right because even at ReLU this is this is still a linear operation that we've taken um and there's kind of a cool intuition phil told this to me and I really liked it um if you're thinking about a loss landscape no matter how complex that loss landscape is if we're taking a first order approximation of a derivative essentially what we're doing is we're fitting a plane to this complex curved loss landscape and then we're moving in the direction of the plane so by definition it must be linear right we're moving on a planer surface even if the actual underlying loss is not planer make sense cool so this means that forward and backward for a linear model is actually all just one really big kind of flipped out linear model right you can treat the entire thing as um a single iteration um as just one big linear model um and it can kind of be thought of as a single forward pass in a way even though the actual had a forward pass is only the first half and the backward pass is the second half um comparing forward and backward path it you show that in to comput backward path you need to have the x in at each layer so it mean that you need to have in memory all the for all the layers what is the like the value of the embedding whereas in the for you don't need it as you you only need it for the you can like discard the previous embedding just like the for instance with your image of bird or you can once it has passed through the first layer then you can just erase the birth from your memory as you're only going forward in the backward pass it means that you you need to save all of them yes so you do need to save the intermediate um representation so that you can efficiently compute back propagation that's true so there's not really a symmetry between the for no but but I mean you can actually even see that here right like there's this kind of uh asymmetry in where the information is flowing does that make sense yeah is there a specific reason why we don't see bias there oh that was just for simplicity here um so yeah uh in in practice there would be bias terms floating around everywhere but we just wanted to kind of capture the intuition yeah mhm um when we're going back to our reloo layer are we um like do we need to know the pre-activations like we need to have that in memory you basically need to save off what the value of the activations for were for each component of your input um and you keep those in memory and then you build that sparse matrix um that lets you actually kind of gate those going back through yeah cool okay so let's move on to talking about DAGs so what if your DAG isn't a chain right and actually this is pretty common um we often have uh connections um within our machine learning models that are not just chains um for example sort of sharing weights or splitting weights or having you know things kind of come together be concatenated from different heads or being sort of split into um different sort of objectives so let's talk about what this actually looks like um so there's actually only two operations that you need to make everything we just talked about work for any arbitrary directly graph and those are merging and branching so assuming you have some merge where here um you know then that merge could be a lot of different things it could be addition it could be concatenation what have you you have um you know some components some functional components that you're merging together um to move forward through your through your directed asylic graph um now if you actually need to propagate a gradient back through that um essentially you just need to make sure that you track the gradient with respect to the correct input variables right so if here you just only pass the gradient that corresponds to the sort of x superscript a and on the other path you only pass the gradient that corresponds to the components of that other um that other part of the path similarly if you're branching so now you're splitting up your um your information in some way um or even just duplicating it now if you're propagating a gradient back through that it's as simple as a sum uh you just sort of take the gradients from both and you sum them and you send that back through that input cool so there's a much more detailed derivation in the lecture notes um if you're interested um but so now let's talk about what this means for parameter sharing so say you have some chain um and now you have you know the parameters are sort of uh separate for each component of that chain you might instead have something where those parameters are actually shared across those different components uh maybe you're reusing parameters that were pre-trained on imageet or something and you're using them in multiple parts of your network um there again you can actually just kind of think about flipping this on its side all that is is a branch right so now if you're passing your gradient back through that branch um you just need to be summing the gradients that are coming in um from these different branch dimensions so parameter sharing some ingredients all right so towards the end of the lecture now I want to move on and talk about this kind of meta concept of what differential programming is and maybe why uh neural network training is essentially just differential programming so when we think about deep learning um generally we think about it this way right you have um some network again a DAG um and then you define sort of these forward and backward passes through that network differential programming takes the same perspective but now we basically just are actually programming um that model and this is really what all of these different um existing languages PyTorch TensorFlow if anyone still uses it um Jax uh these are all um basically libraries that enable us to really efficiently do differential programming um so deep networks are popular for a few different reasons first they're easy to optimize right everything is differentiable second they're compositional right it's essentially like blockbased programming and so differential programming sort of surfaced as an emerging term for general models with those properties um these tweets are a bit old now but I still think they're funny um Yan Lun said "Deep learning has outlived its usefulness as a buzzphrase deep learning amore the differential programming." essentially even years ago um this was kind of like ah maybe this is the new buzzword for what we're talking about here Tom Dietrich who is significantly less splashy in his tweets um said that deep learning is essentially a new style of programming um differential programming and the field is trying to work up what the reusable constructs in this style are um and there are some that we actually know some of these blocks have been kind of well defined things like convolution pooling LSTMs GANs VAE memory units routing units etc um so you know this is kind of taking that uh that perspective this is a paper from 2017 called neural module networks where essentially here you can see that there's these different components of the model and some of them um might be sort of architectural and some of them might have parameters that are learned or not um so you know you're sending in something like where is the dog and then maybe that gets routed to a parser and that parser might not be learned at all right it might just be a standard parser so that component might not have any differential um parameters that are actually being learned um but then potentially there's a CNN down here that's parsing the objects that are seen in an image and that might actually be something that's that you have updates being propagated to from your training data set and there's an interesting blog post from Andre Puckerathy that kind of talks about this as what he calls software 2.0 um essentially here he's saying that software 1.0 which is the software we sort of had originally where everything is defined um that might be just that little red dot software 2.0 you know is actually defining a space of possible um sort of software systems and then you're actually optimizing within that space to build the system that you need to um to solve your your problem so here instead of thinking about you know this DAG where every component is actually differentiable um or or every component is differentiable where every component is learned instead you might have a bunch of the components of your system be explicitly programmed by a human um a really obvious example of this is we often explicitly program the normalization values that we want to use for natural images um directly into the pre-processing of our data right this is something that we just define we don't learn what those normalization values should be and then there are these other components where this this part is actually programmed by back propagation um so for example program by tuning the behavior to match the training examples and the cool thing is because we assume that every piece of this system is differentiable you can actually optimize any node or any edge with regard to any scalar cost so here you could think about um calculating how the cost would change when the weights of the specific yellow function change you could also think about um looking at how the cost changes when the input data changes right you don't actually need to specifically do this for um for weights yeah so is this how we use or create a metric um that can determine what might be good for a human to train versus the computer to train or is there another qualitative or empirical metric that you might we might use so a big question um so he was asking if um if this is something that helps us sort of uh build metrics around what maybe we might want to optimize via like data versus what we want to just define directly um I think experimentally we often kind of find those things out for different applications um you know what parts is it actually good to what essentially anytime a hum human is programming part of the system it's constraining the system and that constraint can be useful but it could also be unusful i think about when I started in machine learning we often had uh it was built around I'm gonna date myself right now but we did a lot of what we called feature engineering which is essentially like us building a bunch of explicit constraints into how the data might be processed to then go through a very very simple neural network or um or even something more simple than that something like a um an SDM and those bottlenecks it turned out uh were not necessarily optimal right we were we were defining this we were programming it but it turned out our best ideas were not as good as just uh much more complex larger models that were learned somewhat end to end does that make sense so I think the jury's a little bit still out when you talk about metrics um that's really where that red square comes in right you define what that cost function is so um you have to kind of figure out how you are going to define the loss and what that then looks like for your cost and that definition will affect um what your sort of optimals are cool so let's talk a little bit about optimizing parameters versus optimizing inputs um so this is the kind of standard approach right this is looking at how much the total cost is increased or decreased by changing the parameters right this is sort of all the different parameters um throughout the the network this is what we sort of commonly think of but you can also think about oper um optimizing the input for a fixed set of parameters um so here this could be something like how much the chameleon score is increased or decreased by changing the input pixels um and so here now if you think about this um from sort of a a way where you're optimizing maybe class logits before the softmax or optimizing the class probabilities after the softmax um the maybe easiest way to increase the probability softmax um given to a class is often to make the alternatives unlikely rather than to make the class of interest likely um whereas if you optimize presoftmax logits this tends to actually be a bit more stable so what we find is if you want to actually make an image that maximizes the cat output neuron um again we are doing this maybe pre softmax um it might look something like this so this is a an interesting uh again blog post back in 2017 but on feature visualization so this is telling you what a given trained model thinks is most cat-like and this is something you can learn or you can actually also do things that say for example make an image that maximizes the value of a neuron J on a layer L so finding something about like using this as a mechanism to probe what the model is paying attention to so that might look something like this right this is saying that like this particular neuron and this particular layer is looking for stuff that looks like this in kind of a handwavy way and this is how this is kind of the idea behind for example deep dream i don't know if any of you guys saw this a couple years old now but I think these are kind of psychedelic and beautiful um and this is kind of looking at what uh these models I guess uh dream of um and this is also uh this is kind of an idea of what's to come but if you're kind of comfortable with the idea that you can optimize anything with respect to anything here we're sort of looking at maximizing um clip as a model that tries to make descriptions um and images that correspond to them close together in a joint embedding space so you want to build a text encoder and an image encoder where things that are similar semantically from text to images are quite close together in the sort of learned embedding space that projection that you're projecting each of them into now if you have something like clip then you can for example tie your clip model together the image encoder and the text encoder from your clip model together with a GAN um something like a generator upper serial network and then you can optimize the embedding or so that input parameter that goes into that image generator and you can figure out for a given input how to optimize this so that your output of your image generator ator is the thing that's closest in clip space to any sort of given prompt and if you actually look at what that learns over time you can get stuff that's kind of fascinating right so this is a way to actually generate an image from text um and here you know again all these trapezoids are neural networks you can plug them together you can take the components trained in one way and use them in another way um really the idea is you can optimize modules with respect to all the other modules And um the world's your oyster cool so what we covered today review of gradient descent and SGD computation graphs backrop through chains backrop through MLPS back prop through dog DAGs and differential programming um any questions yes so essentially what we're doing here is you had clip you have this text encoder and this image encoder and they were already trained and now they're fixed right so the idea is here the text and the image encoder actually we're not backropping through them at all they're now they're now defined they're not being learned and similarly you've separately trained an image generation model that takes in any given embedding and generates an image from from that embedding now that one is also fixed so now you have a lot of parameters in a model but all of them are fixed and the only thing you're optimizing is this um kind of you know hidden uh parameter that's going to be the input to the image generator such that this is as is maximal right so you're trying to find the the kind of Z value that maximizes this with respect to that input text sorry so your op Yeah so all the parameters here are fixed though you are going to need to um calculate the like how to back prop um the signal back through these models to get to that one yeah yeah yeah absolutely um so when I say embedding I might also say representation um these are all terminology that we often will like throw around in the machine learning community um but usually it means something like this so you have some neural network um and this might and we'll call them an encoder sometimes again just terminology um but this neural network is is essentially building a mapping from a highdimensional input or a complex input something like an image to a lowdimensional representation or a lowdimensional embedding um of that input data and so that might be you know a vector of length 2048 for example or 1024 those are both sort of common um embedding sizes uh but really when we talk about embedding it's essentially just saying we've trained the neural network already now we're using it to um give us like a low rank um representation of our data does that make sense no yes i noticed in some of the slides you used are they interchangeable uh no this is me being a little fast and loose um what the way we defined it is that the loss is like a function um over all of the data points and then the the cost might be sort of the overall um calculation of that loss over maybe all of your data or something but I think um I think it's a little semantic yeah uh I I think it it doesn't it's not the end of the world to sort of think of them somewhat interchangeably essentially both are you're defining some mechanism for evaluating uh optimality how easy is it to switch between optimizing parameters versus optimizing so lucky for us things like PyTorch exist and so actually this is just a matter of defining what you want to update uh basically where where you want to freeze your gradients and where you don't want to freeze your gradients um and that's not actually so hard to do okay yeah yeah yeah so so this is where kind of that modularity comes in right sorry guys if just if you could be quiet um so this is where the modularity comes in right so we we talked about um here right like you can have these models um where some parts are programmed by a human and some parts are actually being programmed by backrop but you can also think of the ones that are quoteunquote programmed by a human those might have just actually previously been programmed by backrop and now you're taking those weights and plugging them in yeah yeah sorry what sorry if if everyone could just keep it quiet so that I can hear the questions thank you so much yeah is that what I would call a what an attention block oh um so uh so this is this is a cosine similarity and cosine similarity is a component of attention but it's not it doesn't actually sort of make up all of what we would call an attention block so the attention block generally defined for a transformer or something will also include the projections into the shared space where you're going to take that similarity as well as a projection out into the space where you actually want to use um the information about what's valid yeah yeah um it'll be by the end of the day today sorry we were working on it at a meeting right before this and I thought it might be up but not quite yet yes did you have a question no okay yes you're going to need to be a little louder sorry if we can just be quiet as we leave thank you so much all right so um could you be uh like what's the difference between the backpack for NLB and uh D because they're both computational brands right yeah so so there's no difference right um and this is where uh these the only two things you need to take all the stuff we talked about um from backdrop through MLPS and translate it to backdrop through any DAG are these right you need a merging operation and a branching operation so now essentially they're just changed with merging and branching and that represents any DAG yeah yes is this just a different way of looking at properties um yeah so essentially all we're saying is that um all the really detailed examples we showed of back propagation those were all for a really simple chainbased um uh DAG is just like you have a component and it goes and then a component and it goes so it was all a chain and now what we're saying is that you can actually take everything we talked about it and apply it to any any structure of a DAG um because now it's just chains where sometimes they merge and sometimes they branch but there's really simple operations that we can use to pass the gradients through merges and branches um and so that's that's kind of how you expand that definition of back prop to any DAG yeah sorry what um sorry i just I'm having trouble hearing you why is it a sum um because uh because the uh the change in this with respect to kind of as it's coming in from that and that um it it you're able to just aggregate it and the reason for that is um essentially oh I'm I'm not going to talk this through really simply right now um come to office hours and we can talk about it yeah yes so I know that PyTorch like builds its computation graph dynamically for instance like imagine that in your forward pass you have a conditional which is like if the magnitude of the gradient exceeds whatever then I want to apply a clip operation um I have a slightly different use case for research but like do you know how it handles if you apply like a non-torrch operation to a tensor like if you apply some sort of UDF which is just written using like normal Python arguments does it basically just suddenly say in the computation graph like this anything even if I use torch operators before that node in the graph it is now not optimizable and I'm just going to treat that basically as like the way I would treat a a data input for instance so there might be there might be hacks around this but generally if you want to um have any operation within a neural network defined in PyTorch you have to define it as a torch operation which is basically like the you are required to define a gradient for that operation right so you have to assume that everything's differentiable and this is where things kind of get nasty because if something doesn't have a simple gradient so there's a lot of um built-in stuff in PyTorch that handles what those approximations to those gradients would look like but if you have something that's really complicated for example like an occupancy model uh of species which is something that we work with then you actually might have to define that um as a torch operation it also depends like sometimes there are kind of choices you can make based on whether or not that makes sense in the structure right you might not actually want to learn all the way through that thing if it becomes computationally intractable to calculate the gradient so I guess that's my question is like will pietorch if say you didn't define say you didn't define the gradient for that operation um or how to compute it can pietorch still optimize like the rest of the model and just treat what comes in from the output of your UDF as basically being like if you had input sum x subi x of like like basically a data vector i might um just define that separately like I might define that as part of your pre-processing for your data right so so um the pre-processing steps don't necessarily need to be differentiable um things like data augmentation so you could put that UDF in your like pre-processing your data loader um and then whatever comes out of it would then be the data that you were going to optimize towards that's probably the simplest thing to do um but again computational complexity can get really nasty with some of this stuff so um maybe that's not too bad but I've definitely run into issues where I've tried to like build like simple statistical models into u machine learning and with varying levels of success and often it's just like ah okay it's technically possible but it's intractable to actually learn on this exact uh for the superscript a and b what do they really represent oh it's intended it's intended to be um kind of it's intended to be open-ended um so they could literally just be a duplicative copy of both right or it could be some um splitting of the embedding vector or etc etc uh so it's any branching operation yeah any differentiable branching operation okay one last question it will be there soon i thought it was up already but it's not 3 p.m 3 p.m all right thank you guys so much uh see you soon
